# Optional operator briefing

This file is not a Steward startup input, tracker, or execution authority.
Current lane state lives in
`agent/playbooks/federation/steward/lanes.md`; WP contracts live in
`docs/program/issues/`; `origin/main` outranks every summary.

Populate this file only when the operator requests a briefing. Keep one current
block and stay under 250 lines. Delete stale text instead of annotating it.
Older requested blocks may move unchanged to the dated diary; git preserves the
full history.

Do not schedule a daily flush, delegate routine maintenance, or publish a
briefing-only commit. Procedure:
`agent/playbooks/federation/steward/briefing-flush.md`.
