---
id: RT-COMPOSED-RETURN-PRODUCER-SINK-COLOCATION
title: "D0-only feasibility: can the fresh declared-call result be co-located with a predeclared Ret sink at the TRUE `StaticWorker` producer, before its call emits, on both read and write products — establishing producer identity, sink-before-producer, Tail-member-at-producer, a disposable one-edge probe, and discriminating controls, or returning a clean NO. Lands no production; the one shape-(a) family Research left unmeasured (HS15)."
status: ready
owner: runtime
size: M
gate: none
tier: T1
depends_on: []
blocks: []
github: null
origin: "Architect HS15 ruling evt_2mhf7svjg76zk (binding base af1db1bfbd824c7733864516bd6c35c4bd86ea8c, tree b2a36983a44ed04975efdbd5e55d508c646ef51b; source.rs blob c39f82e7854f626244b4398ba9941ae38b25485e; core.rs blob eea98dc6ddb0ae2f7656b16fed7ee461b24de0a1), incorporating research advisory evt_3fkt4qswdx64t (artifact 791650eba4119fca737c5a0ab939446c9f3c82ab1f5a33451d7d3a8ce30e1143). The atomic D3 activation of RT-COMPOSED-RETURN-FORWARD-RET-EDGE at source.rs:4492 is REFUTED (HS15 clean stop evt_65rzza5s5hrnw); its D3 release is spent. This is a FRESH D0-only feasibility object for the one conventional shape-(a) family Research identified as unmeasured — true-producer/predeclared-sink co-location — NOT a respin of that atomic D3. Steward-filed and released per COORDINATION section 2."
---

> # READY — D0-ONLY FEASIBILITY. Released to the runtime ring (lane 1). NOT a respin.
>
> The funded shape-(a) D3 activation at `source.rs:4492` is refuted (HS15): its
> authority-bearing arrival is `Value(Carried(word))`, `core.rs:7715-7727`
> affirmatively settles `InlineNoCall` and returns the transported environment,
> no application occurs, and forwarding that word moves both witnesses from the
> `ResourceBodyResult` mismatch only to a `Result` mismatch. That release is
> SPENT — no candidate, no QA route, no production respin at that seat.
>
> Research (`evt_3fkt4qswdx64t`) returned a qualified NO that left exactly ONE
> conventional shape-(a) family unmeasured: co-locate the fresh result with a
> predeclared Ret sink at the TRUE `StaticWorker` producer, before its call, so
> the real return value is the block argument. The Architect grounded a qualified
> YES to feasibility (`evt_2mhf7svjg76zk`): Ken already holds the three local
> compiler seams this needs. What is UNPROVED — and all this node measures — is
> the decisive dynamic correlation: whether the exact Tail member and exact sink
> are both available at the product's actual `StaticWorker` producer, in the same
> emitted function, before its call.
>
> **This is D0-ONLY.** It authorizes disposable instrumentation and a restored
> scratch probe only. It lands NO production, routes NO Runtime QA, opens NO merge
> gate, and confers NO D3A/D3B authority. A production increment requires a later
> explicit Architect ruling and a fresh Steward release AFTER this D0. The runtime
> ring's standing hard-stop protocol applies; a design fork hard-stops to the
> Architect. **Runtime remains parked until this release; it IS the release.**

## Exact base and coordinates (Architect `evt_2mhf7svjg76zk`)

Binding object: `af1db1bfbd824c7733864516bd6c35c4bd86ea8c`, tree
`b2a36983a44ed04975efdbd5e55d508c646ef51b`; `source.rs` blob
`c39f82e7854f626244b4398ba9941ae38b25485e`; `core.rs` blob
`eea98dc6ddb0ae2f7656b16fed7ee461b24de0a1`. Re-measure the exact seams before any
probe — prior campaign increments moved coordinates.

The three local compiler seams the ruling grounds as already present:

- `core.rs:12669-12683` creates the strict shared `return_body` block before
  per-case lowering and installs it in the active carried-elimination record;
  `resolve_composed_return_ret_sink` at `:12215-12272` retrieves it by exact
  frame/body/binder coordinate.
- The real `StaticWorker` arm alone assembles the ordinary envelope and
  continuation inputs, calls `call_declared_unit_target`, and owns the fresh
  result at `core.rs:7854-7863`, with the same `FunctionBuilder` passed through
  the lowering call.
- `RecursiveBackedge` is already a compiler-control marker, and
  `source.rs:1644-1656` already bypasses `ConstructArgument` when that marker is
  returned. An actual producer can, in principle, branch its local SSA result to
  a block created earlier and return the existing sealed marker before the lossy
  constructor transfer — needing neither preservation across the general Rust
  return closure nor added runtime state.

The negative seat (the refuted D3): `core.rs:7715-7727` settles `InlineNoCall`
and returns the carried environment word; `source.rs:4429-4496` forms the static
authority then calls the helper but does not prove which arm executes or mint a
dynamic result.

## Deliverable — D0 feasibility report

One D0 investigation, held as one continuous turn through the A-E probes below or
a genuine hard-stop NO, establishing all five on BOTH exact read
(`fs-read-at-offset-single` / `rt_read_offset_stage`) and write
(`fs-write-at-offset-single` / `rt_write_writable_stage`) products. It commits no
production, promotes no durable test, and routes no Runtime QA. It may use
disposable instrumentation and a single restored scratch probe (deliverable D
below); the tree is restored afterward. The report returns a per-product verdict
and the evidence for each of A-E, or the exact first NO condition hit.

## Acceptance criteria (Architect-specified; each carries its own control)

- **AC-A — Producer identity.** Record every relevant helper arrival by stable
  `ContinuationCallIdentity`, defining function, transport source/destination,
  and helper arm. The qualifying event is `StaticWorker` followed by the actual
  `call_declared_unit_target` result. `CarriedEnvironment`/`InlineNoCall` is the
  negative control. Cursor numbers, current counts, map order, proximity, and
  aggregate equalities are NOT identity.
- **AC-B — Sink-before-producer.** For the same stable Tail identity, prove the
  exact `{active_frame_origin, ret_case_body_origin, binder 0, return_body}` sink
  is already installed, or can be predeclared from those exact static
  coordinates, before the call emits. Producer and sink must belong to the same
  Cranelift function and the same `FunctionBuilder`. Every value the sink body
  uses other than the fresh result must already dominate there through existing
  function/block inputs. Needing a new runtime operand or block/header parameter
  is D0 NO under the current closed axes.
- **AC-C — Tail member at the producer.** Re-run the D2 confluence/member join at
  the actual `StaticWorker` producer using that producer's own selected
  transport. The exact Tail plan must form there before emission. It may not be
  borrowed from the later carried arrival, reconstructed by a second lookup,
  inferred from the sink alone, or selected by coordinate coincidence.
  `DirectInvocationReturn`, non-Tail, and non-governed populations remain
  distinct and unchanged.
- **AC-D — Disposable edge proof.** If A-C are YES, a scratch-only probe may
  consume one move-only proof immediately after `call_declared_unit_target`:
  require the returned operand to be carried, emit `jump(return_body,
  [returned.word])`, switch to an unreachable block, and propagate the existing
  `RecursiveBackedge` marker so `ConstructArgument` cannot materialize or replace
  the result. Restore the tree afterward. The Cranelift verifier must accept the
  CFG, and the event ledger must show one producer, one result, one edge, one
  sink, with no later carried/fallback claim for that identity.
- **AC-E — Discriminating controls.** Keeping the true call but substituting the
  carried environment word must reproduce the observed `Result` mismatch.
  Suppressing or duplicating the call/edge must trip its own exact census.
  Mutating member, transport, frame, body, binder, direction, or delivery must
  refuse before emission at the corresponding natural boundary. Ordinary Ret
  remains independently green. A green product alone is NOT the causal proof.

## D0 returns NO immediately if

- the Tail plan becomes available only AFTER the true producer;
- producer and sink are in DIFFERENT emitted functions;
- required sink operands do NOT dominate without new transport; or
- bypassing `ConstructArgument` needs a wider general return protocol.

On any NO, at that point HS14's boundary is complete under the funded
constraints, and the disposition is the operator fork: return/control-protocol
widening versus reopening another closed axis. Return the exact NO condition and
its evidence to the Architect; do not attempt a remedy.

## Prohibitions (absolute — on NO or otherwise)

Do NOT add a compiler receipt, preserve the SSA through constructor material,
reapply `K`, move production after Tail selection, widen the active header/ABI,
add a second `Ret`, or weaken the downstream `Result` wall. Do NOT reinterpret
`CarriedEnvironment` or treat the carried environment word as a fresh result. Do
NOT land production, route Runtime QA, open a merge gate, or take D3A/D3B
authority — this is D0-only. The closed axes (Produced-transfer, D3 salvage,
Direct-only salvage, recovery, storage, runtime tags, general carriers,
active-header ABI widening, a second Ret lowering) stay closed.

## Reviewers, sequencing, contention

- **Reviewer:** Architect, on the D0 report (report soundness and the per-product
  A-E verdict). No Runtime QA route, no Conformance Validator, no Decision — D0
  lands no production. A production increment is a SEPARATE later object gated by
  a fresh Architect ruling and a fresh Steward release.
- **Sequencing:** runtime ring, one continuous turn from the binding base through
  re-measure → A-E probes / scratch edge proof → restore → report, or a genuine
  hard-stop NO. On the report, the Architect reviews; the Steward routes the next
  step (production object on YES, operator fork on NO).
- **Contention:** disposable instrumentation and a single restored scratch probe
  only; nothing lands. No crate or catalog contention with the concurrent lanes
  (kernel `conv.rs`, verify CI scripts). Targeted builds only via
  `scripts/ken-cargo`, never `--workspace`.

## Shared-predicate answer (entries 13-15, Architect)

The defect across HS13-15 is that producer identity, dynamic result occurrence,
and continuation sink are split across compiler phases whose representations
retain only one or two of the three; static endpoint identity, or a value
surviving inside runtime constructor material, is then mistaken for the missing
compiler-control edge. The structural closure is co-location at the true
producer — not another later endpoint. This node measures exactly whether that
co-location is constructible at the current base.
