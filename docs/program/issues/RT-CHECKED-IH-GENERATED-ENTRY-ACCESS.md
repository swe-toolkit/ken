---
id: RT-CHECKED-IH-GENERATED-ENTRY-ACCESS
title: "RT checked-IH generated-descendant-entry access — the ONE complete planner-owned predecessor that replaces the repeated last-gap decomposition (Architect HS7 evt_1z1p9t4tdyd2v), RECUT onto the confluence/quotient branch (Architect HS8 evt_14hwxs5q2j087 + mechanism ruling evt_3myrd8sp2tp8n, on Research graph advisory evt_2ffs47ax5gbnn). Build a planner-owned CheckedIhGeneratedEntryConfluence certificate quotienting the governed continuation-inheritance population by CheckedIhGeneratedEntryCoordinate — context id, enclosing specialization, worker body, binding, invocation, call, callee — where every member of a class must agree by full typed Eq on one CheckedIhGeneratedEntryProjection (destination owner/body, binding, exact CheckedIhImmediateKBindingLocator, exact CheckedIhFreshResultDestination). Source identity and projection are NOT key members. Validate by SET equality over five explicit closures, never counts. Install a SANITIZED CheckedIhGeneratedEntryAccess — projections only, no member identities, no retarget caller, no transport, no ancestry — in define_continuation_context_bodies as COMPILE-TIME function-definition authority beside the exact ContinuationContextView, NEVER a runtime parameter. Lowering reads it through ONE exact BTreeMap::get that fails closed. intern_generated_contexts, its key, context counts and existing retarget callers are UNCHANGED: read one context, write two. Static splitting and region cloning are WITHDRAWN."
status: active
owner: runtime
size: L
gate: none
depends_on: [RT-CHECKED-IH-K-AVAILABILITY-LOCATOR]
blocks: [RT-RESULT-CONTINUATION-BINDING-PROVENANCE]
github: null
origin: "Runtime hard stop 7 on RT-RESULT-CONTINUATION-BINDING-PROVENANCE (runtime-implementer evt_dqa3989tfmm2; leader routing evt_7fhdmyaqk4371). Architect HS7 component ruling evt_1z1p9t4tdyd2v ruled the repeated last-gap decomposition itself the design defect and forbade a fourth narrow predecessor. The first cut of this node carried a static context-split contract; Runtime hard-stopped on it (HS8, runtime-implementer evt_799ckhz4kgtcd, leader evt_27rha2k9cyv2v) and Architect evt_14hwxs5q2j087 independently reproduced the refutation at exact 1403722a2e70d192a6c4d283ca4dd2e6345c3c55. The Steward then triggered one bounded Research graph advisory (trigger evt_1cr1trmmzpr54, advisory evt_2ffs47ax5gbnn), which measured the closed six-row relation and found the sole destination-coordinate collision agrees on the consumer projection — selecting the confluence/quotient branch by the ruling's own stated condition. Architect evt_3myrd8sp2tp8n supplied the mechanism this recut is written to. Steward-owned frame. D3A stays frozen and non-landable; after this predecessor lands through ordinary gates the Steward must EXPLICITLY re-release the same atomic D3A+D3B consumer — landing alone authorizes nothing."
---

> # SYMPTOM INVENTORY — FIVE HARD STOPS ON ONE ACCESSOR. READ THIS FIRST.
>
> This node exists because the same accessor produced five consecutive stops,
> each of which looked like a missing field and was not. Seeded per Architect
> `evt_3myrd8sp2tp8n`; it is the frame's memory, not decoration.
>
> - **HS5 — result successor.** Which `K` survives and which invocation uses it.
>   Closed by [[RT-ITREE-CHECKED-IH-RESULT-SUCCESSOR]].
> - **HS6 — K-availability locator.** Where `K` is immediately readable. Closed
>   by [[RT-CHECKED-IH-K-AVAILABILITY-LOCATOR]].
> - **HS7 — absent entry key.** The generated-context entry carries no selected
>   source-call identity, so the existing full accessor cannot lawfully be called
>   there. The Architect ruled the *decomposition* the defect and forbade a
>   fourth narrow slice; this node is the complete replacement.
> - **HS8 — cross-frame injectivity premise.** THIS NODE'S OWN FIRST CUT. It
>   instructed a static context split keyed on source identity with callers
>   retargeted. That presumes the source identities are distinguishable *at the
>   retarget seat*. They are not. `intern_generated_contexts` mints ctx0 from the
>   call whose emission owner is specialization 3 and producer result is body
>   1238; `continuation_context_for` is keyed by `(enclosing specialization,
>   worker body)`; `define_continuation_bodies` asks with `(unit.id,
>   unit.worker_body_origin)`. Call-target injectivity makes call 4 the one
>   incoming identity for specialization 3, so **ctx0's incoming set is `{call
>   4}`, not `{A, B}`**. A census proving non-functionality in the
>   destination-inheritance frame does not license a remedy that must execute in
>   the caller frame. The two frames share no discriminator.
> - **HS9 — consumer-adjacent representation premise.** THIS NODE'S OWN SECOND
>   CUT, and the reason section 5 is currently unsatisfiable as written. Section
>   5 requires an in-range `Value(Carried(_))` at the exact immediate locator.
>   On exact `56c51fe315ba9ac4d8c020f20b40e2631d6018b4` / tree
>   `6401ac7df7b370de1c5162e5d4286f15e5e31216`, the binding at that locator is
>   `Value(Specialized(_))`, so the governed command fails closed before
>   emission (runtime-implementer `evt_24fwbvyh76hr3`, leader
>   `evt_7fegbj3w2gj6h`).
>
> **HS9 falsifies section 5's representation premise and NOTHING ELSE — the rest
> of this recut was confirmed by reaching the stop.** The planner-owned
> seven-field quotient, the typed confluence projection, exact source-specific
> reopening, the audited retarget caller, the five set closures, sanitized
> per-context access, the three-field install, and the single exact
> `BTreeMap::get` all compiled, and that sanitized route selected the seat and
> indexed the named environment with the exact governed facts: context 0;
> binding `(frame 301, position 1)`; invocation/call/callee `305/304/303`;
> locator domain `ImmediateInvocationEnvironment`, index 0; environment length
> 13. **Confluence, source identity, lookup, and seat selection all held.** This
> is a positive result for the mechanism, reported as a stop.
>
> **The stop is clean and Runtime was right to take it.** No candidate, no `K`
> application, no `R2` binding, branch restored. Accepting `Specialized`,
> converting it to `Carried`, moving environments, or choosing another seat
> would each change or weaken the frame, and Runtime refused all four rather
> than closing the gap by weakening its own acceptance condition.
>
> **HS9 IS NOW RULED — section 5 is recut below, not held.** The mandatory
> ninth-stop Research advisory landed (`evt_6q5zq12zmftf4`) and the Architect
> ruled the representation (`evt_2kgb55azx3ej3`): inherited `K` IS the complete
> `Value(Specialized(ComputationalRecursorClosure { .. }))` capsule at the exact
> locator, directly applicable, and its `Carried` residual is the value `K`
> continues over rather than `K` itself. No conversion, no residual extraction,
> no environment move, no other seat. The advisory was accepted within its
> stated reach — this predecessor and the two unchanged witnesses only, not a
> theorem about arbitrary programs and not a discharge of successor totality.
> Evidence preserved outside the
> worktree: note SHA-256
> `e55a964c1e24bb6b7c9fa7b1e8b3b13623c9d93da8a54328e603afefbbc2ea00`, 1,142-line
> implementation/probe diff
> `a504efc558a87e1fd2921f29e7e019b361ba27de53ce389f3241c6c302a96bc4`, run log
> `932b08b730c64b27e2877d11513ede7a923de25eee7e9dd33af4bff4a03db950`.
> D3A+D3B remain frozen.
>
> **The HS8 defect was the Steward's, not the measurement's.** Research answered
> the destination-frame question it was asked, inside a stated boundary, and
> explicitly disclaimed the selection-strategy question. The frame carried the
> instruction across that boundary without checking.
>
> **`AC-SPLIT-DESCRIPTOR` caught it before implementation** — a control folded in
> from a prior-art hazard, guarding a deliverable that turned out to be
> unsatisfiable. Design-time controls derived from prior art earn their cost even
> when the design looks settled; that is the transferable lesson.

> # WHAT IS WITHDRAWN — do not resurrect it from an older revision
>
> **Static context splitting is withdrawn.** Refining the
> `intern_generated_contexts` key by source-call identity and statically
> retargeting callers is FALSIFIED, not deferred.
>
> **Region cloning is withdrawn and was never authorized.** Cloning
> specialization 3 plus call 4 without a proved static discriminator reproduces
> the same two-definitions/one-caller defect one layer up. No region cloning,
> tail duplication, new occurrence identity, source rewrite, or runtime
> selection.
>
> **Nobody has measured whether static cloning is available.** Research's item 3
> had a false precondition — the colliding projections agree — so it correctly
> declined to derive a region and declined to classify static cloning as either
> available or unavailable. **Do not record it as ruled out.** It is unmeasured.
>
> All split-specific controls from the first cut (`AC-SPLIT-POSITIVE`,
> `AC-SPLIT-CROSS-PAIR`, `AC-SPLIT-CLOSURE`, `AC-SPLIT-REBUILD`,
> `AC-SPLIT-DESCRIPTOR`, `AC-SPLIT-ROOTS`, `AC-SPLIT-MUTATION`) are WITHDRAWN,
> including cross-pair installation. Per-member disagreement plus membership
> closure replaces them. `AC-SPLIT-PERMUTE` survives in substance as
> `AC-CONF-PERMUTE`.

## Fixed inputs, measured at exact `1403722a2e70d192a6c4d283ca4dd2e6345c3c55`

Tree `79bac0874b1aa009dd96574bb3e0929073c04185`. The HS8 ruling, the Architect's
mechanism ruling, and the Research graph advisory are all grounded on that exact
ref, and the Architect independently reproduced the graph there twice with its
own probes (`ddff92b0a60b06fe2441499046b37244bf4340c81d88daa94d293f738286e3a1`,
then `73736be89ca09cc25d2809cc7492c344f3dc08c9fd11b58a0da010f58c50b7d3`).
Re-verify the base before starting; a base claim decays.

**The closed six-row relation (Research advisory `evt_2ffs47ax5gbnn`).**
`S(owner, result, construct, alternative, sequence, target)` is the source
`ContinuationCallIdentity`; `G(context, owner specialization, body, raw owner)`
is the generated context; `C(...)` is the exact incoming call to the
specialization seat whose definition can retarget its worker call. Every source
and incoming call has `emission_owner = Predeclared(owner)`. These are compact
reports of planner-issued opaque identities, not reconstructions.

| row | source `S` | generated context `G` | retarget-seat incoming call `C` |
|---|---|---|---|
| R0 | `(4, 930, 798, 1, 0, 0)` | `(0, 2, 941, 4)` | `(5, 958, 951, 1, 0, 2)` |
| R1 | `(4, 939, 676, 1, 0, 1)` | `(0, 2, 941, 4)` | `(5, 958, 951, 1, 0, 2)` |
| W0 = A | `(8, 1227, 1107, 1, 0, 0)` | `(0, 3, 1238, 8)` | `(9, 1257, 1250, 1, 0, 3)` |
| W1 = B | `(8, 1228, 1043, 1, 0, 1)` | `(0, 3, 1238, 8)` | `(9, 1257, 1250, 1, 0, 3)` |
| W2 | `(8, 1236, 904, 1, 0, 2)` | `(0, 3, 1238, 8)` | `(9, 1257, 1250, 1, 0, 3)` |
| W3 | `(9, 1257, 1250, 1, 0, 3)` | `(1, 5, 1259, 9)` | `(10, 1280, 1273, 1, 0, 5)` |

W3's source identity is the same planner-issued call reported as the W0/W1/W2
retarget-seat incoming call. That is a graph chain, not an equality inferred
from coincident numbers.

**Classification.** All six rows resolved exactly one generated context and
exactly one retarget-seat incoming call. There is no absent case. Source
identity to retarget caller is **total and single-valued, but many-to-one**:
`{R0, R1}` to the one read caller, `{W0, W1, W2}` to the first write caller,
`{W3}` to the second. **The join was never missing information. It was never
INJECTIVE** — which is precisely the property the falsified split assumed
without measuring.

**SHARING A CONTEXT AND SHARING A COORDINATE ARE DIFFERENT RELATIONS HERE, and
this is what decides the control set.** W0, W1 and W2 all resolve to generated
context `G(0, specialization 3, body 1238)`, yet only W0/W1 collide on the full
coordinate. R0 and R1 likewise share `G(0, specialization 2, body 941)` without
colliding on coordinate. **A certificate keyed on the context rather than the
coordinate passes its positive case and silently merges rows that are not
equal.** One witness pair therefore supplies both the positive confluence case
and its negative controls.

**The sole destination-coordinate collision, and the frame's decisive input.**
W0/A and W1/B at `(ContinuationContextId(0), invocation 741, callee 739)`. Their
consumer-relevant projections were compared structurally inside the planner
module, by typed `Eq`, not by numeric display — and they are EQUAL:

- destination owner `Specialization(3)`, destination body origin `1238`;
- `CheckedIhBinding { frame_origin: 737, recursive_position: 1 }`;
- `CheckedIhImmediateKBindingLocator { invocation_origin: 741, callee_origin:
  739, environment_domain: ImmediateInvocationEnvironment, environment_index: 0
  }`;
- `CheckedIhFreshResultDestination { active_frame_origin: 737,
  ret_case_body_origin: 904, constructor_child: ConstructorChild { frame_origin:
  737, field_position: 0 }, closure_environment_record: 348, closure_origin: 899,
  closure_body_origin: 888, closure_parameter_count: 1, capture_ordinal: 0,
  capture_occurrence: 898, body_capture_reads: [886] }`.

A and B differ in upstream source/transport and self-resumption derivation
ancestry — source results, constructs, targets, workers, path depths, and
nonfinal construct/child facts are not equal. **They do not differ in any field
D3 consumes.** That equality is what selects this branch.

**REACH BOUNDARY — carried verbatim from the advisory; do NOT round it up:**

> "This is exactly two unchanged, independently compiled read/write product
> witnesses and their six issued rows. It is not a theorem over arbitrary
> programs or the corpus, and it cannot discharge the successor's totality gate."

The advisory also records that the test's shifted-read compilation ran as its
unchanged control but was **excluded** from the six-row census. Method:
observation-only test-support fields in a detached scratch worktree; each
incoming call reopened through the existing exact four-field
`continuation_call_binding_for`; `scripts/ken-cargo test -p ken-cli --test
rt_parity_native
checked_ih_continuation_inheritance_derives_read_and_write_independently --
--exact --nocapture` passed 1/1; full log sha256
`b90259526307ccd0cbc5f0ec72d34e23e92fa27784f7c87145d2efc93c99fe95`; evidence
extract `8faeb6d3f4223f98bc48c1f820864a36d1440ef59075e104b790a38899f9148e`;
observation diff `6948e0f163ea568f55d90bfa791942e399f79d3b13cdb6f91b2eee61c557dbd9`;
scratch removed. **The census DECIDES THE BRANCH; it is not a totality proof.**
This node owes its own totality and closure re-derivation over the full governed
population and must not cite the advisory in its place.

## Objective

Prove, in the planner, that every source identity arriving at one generated-entry
coordinate authorizes exactly ONE typed consumer projection — and hand lowering
that projection alone, stripped of the identities that authorized it. The node
mints no `K`, no locator, no transport, and no result destination. It replaces a
selection problem the entry boundary cannot solve with a quotient the planner
can validate.

## Authorized mechanism — Architect `evt_3myrd8sp2tp8n`

Read the ruling in full before starting; this section is operative, not a
substitute for it.

### 1. The planner certificate

In `planning/static_transition/aggregates.rs`:

```text
CheckedIhGeneratedEntryCoordinate
  context: ContinuationContextId
  enclosing_specialization: ContinuationSpecializationId
  worker_body_origin: StaticOriginId
  binding: CheckedIhBinding
  invocation_origin: StaticOriginId
  call_origin: StaticOriginId
  callee_origin: StaticOriginId

CheckedIhGeneratedEntryProjection
  destination_owner: ContinuationEmissionOwner
  destination_body_origin: StaticOriginId
  binding: CheckedIhBinding
  immediate_k_locator: CheckedIhImmediateKBindingLocator
  fresh_result_destination: CheckedIhFreshResultDestination

CheckedIhGeneratedEntryConfluence
  members: BTreeSet<ContinuationCallIdentity>
  retarget_caller: ContinuationCallIdentity
  projection: CheckedIhGeneratedEntryProjection
```

`StaticTransitionPlan` owns
`BTreeMap<CheckedIhGeneratedEntryCoordinate, CheckedIhGeneratedEntryConfluence>`.

**The coordinate is the quotient key, and its membership is load-bearing in both
directions.** Source identity is NOT a key member — including it reproduces the
falsified split. Projection is NOT a key member — including it turns a
disagreement into two green classes instead of a rejection. Context alone is
insufficient, because W0/W1 and W2 share write context 0 without sharing a
coordinate, as do R0/R1. Plan ownership supplies plan identity: **mint no
`PlanId` and combine no maps across plans.**

### 2. Production and validation

```text
build_checked_ih_generated_entry_confluences(
  &StaticTransitionPlan
) -> Result<BTreeMap<Coordinate, Confluence>, CraneliftBackendError>

validate_checked_ih_generated_entry_confluences(
  &StaticTransitionPlan,
  &BTreeMap<Coordinate, Confluence>
) -> Result<(), CraneliftBackendError>
```

In `construction.rs`, build and validate immediately after
`validate_checked_ih_continuation_inheritances`.

For each existing inheritance whose destination specialization/body resolves
through `continuation_context_for` to `Some(context)`:

1. **Reopen through the existing accessor.** Call
   `checked_ih_continuation_inheritance_for_invocation` with the full opaque
   source identity, destination owner, `Some(body)`, and final binding. Require
   the returned view to EQUAL that inheritance.
2. **Form coordinate and projection only from that returned typed view.** No
   parallel locator or fresh-destination derivation anywhere.
3. **Accumulate by full typed `Eq`.** Insert the source identity into that
   coordinate class. The first projection is provisional; every later member
   must equal it. Publish only after the complete governed population and its
   closures validate. Initializing an accumulator from one row is permitted;
   **selecting that row without total agreement is not.**
4. **Derive the retarget caller as an audited graph consequence.** Derive the
   exact call targeting the enclosing specialization from the validated call
   population, require exactly one, reopen it through
   `continuation_call_binding_for`, and store it as `retarget_caller`. Iteration
   is authorized ONLY here; it is never a source or `K` selection route, and it
   is hidden from lowering. All classes for one context name the same caller.

A raw inheritance resolving to no emitted context is outside this population,
**but the validator re-derives that exclusion.** If a context exists, absence
from the certificate is an error.

### 3. Closure by SET equality

The validator rebuilds the full map and requires exact equality. It also closes
these explicit sets:

- governed `(coordinate, source identity)` pairs;
- certificate `(coordinate, member)` pairs;
- certificate coordinate keys;
- context-installed coordinate keys;
- consumer-adjacent observed coordinate keys.

The first two are equal AS SETS; the last three are equal as sets over each
governed compile. **Counts do not substitute.** Closure is over PAIRS because
one source identity may someday authorize multiple coordinates.

**The certificate claims no equality of transport, ancestry, or runtime `K`
values. It proves only common typed consumer authority at one exact seat.**
State it that way in the handoff; a stronger claim is false.

### 4. Sanitized lowering authority

Derive per context:

```text
CheckedIhGeneratedEntryAccess
  context: context id + enclosing specialization + worker body
  projections: BTreeMap<binding + invocation + call + callee,
                        CheckedIhGeneratedEntryProjection>
```

It contains **no member identities, no retarget caller, no transport, no
ancestry.** `define_continuation_context_bodies` obtains it beside the exact
`ContinuationContextView`, clones it into `OwnedContext`, rechecks all three
context-identity fields, and installs it in that function's `FunctionLocalRefs`
before `lower_expr`. Other functions carry `None`. It is compiler state only:
never ABI, frame, environment, carried value, `SourceControl`, `RoutedAnswer`,
runtime tag, or artifact identity.

Authorize ONE exact `BTreeMap::get` over the installed object, keyed by
binding/invocation/call/callee, returning only a
`CheckedIhGeneratedEntryProjection`; **absence fails closed.** It never searches
raw inheritances, calls, environments, owners, or candidates. This is the SOLE
generated-entry consumer route.

The old source-specific accessor remains unchanged and mandatory for certificate
construction and validation, but **cannot be lowering authority, because HS8
proved source identity absent there.**

### 5. The consumer-adjacent seat

> **RECUT 2026-08-27 on Architect HS9 representation ruling
> `evt_2kgb55azx3ej3`**, grounded at exact `56c51fe31` / tree `6401ac7df`, on
> Research advisory `evt_6q5zq12zmftf4` accepted within its stated reach. The
> falsified `Value(Carried(_))` terminal premise is REPLACED below. **Sections
> 1-4 are unchanged** — the quotient, every member validation, the five set
> closures, source-specific reopening, sanitized access, the exact single
> `BTreeMap::get`, context installation, identity and the fresh-result
> projection all stand as written, and HS9 confirmed them by reaching this seat.

**Inherited `K` IS the complete compiler-local capsule**, at the exact
`CheckedIhImmediateKBindingLocator`:

```text
LoweringEnvironmentBinding::Value(
  LoweringOperand::Specialized(
    Lowered::ComputationalRecursorClosure { residual, activation, invocation }
  )
)
```

The capsule is **directly applicable `K`**. Its measured `residual` is
`LoweringOperand::Carried(_)`, but that residual is the recursive value `K`
continues over — **it is not `K`**. There is no `Specialized(K) == Carried(K)`
relation and no reification boundary to add.

Grounding, all at the exact base and all in
`crates/ken-runtime/src/cranelift_backend/lowering/`: `make_computational_recursor`
constructs the full capsule and returns it as `LoweringOperand::Specialized`
(`mod.rs:11353-11575`); the capsule owns residual plus activation and invocation
segment (`mod.rs:3352-3390`); source `Var` reads the exact environment binding
through `value_at` and clones the operand unchanged (`source.rs:684-700`);
`source_call_state` requires a specialized callee, matches the exact
`ComputationalRecursorClosure`, and consumes it through the existing checked
invocation/decomposition path (`source.rs:3775-3788`, `3845-4055`;
`mod.rs:8643-8660`); and the carrier boundary independently refuses this capsule
before descending into its residual because it names an in-flight activation
(`boundary.rs:1028-1052`, `1280-1283`; `mod.rs:6521-6545`).

### The terminal premise — SIX CONJUNCTS, all required

At the real D3A seat the predecessor instrument uses the returned projection to
require **all** of:

1. projection/current destination owner, body, binding, invocation, call and
   callee agree by their **typed identities**;
2. locator domain is exactly `ImmediateInvocationEnvironment` and its index is
   in range;
3. the callee child's **checked binder provenance resolves to that exact
   index**;
4. that binding is exactly
   `Value(Specialized(ComputationalRecursorClosure { .. }))` — **bare
   `Specialized(_)` is FORBIDDEN**;
5. for the bounded read/write population, the capsule's
   `CarriedInvocationCoordinates`, checked frame, computational-IH slot and
   current call template agree with the projection/current application, and its
   residual is `Carried`;
6. the fresh-result destination remains a sibling proof and is **only observed**
   here.

The exact compiler enum match is **compile-time representation refinement at an
already-authorized seat**, not runtime discrimination. It neither applies `K`
nor binds `R2`. The W0/W1 seat is visited ONCE with one certificate whose
membership is `{A, B}`; **membership closure, not duplicated execution, proves
both sources authorize it.**

> **DO NOT turn Research's diagnostic `capsule_equal` into authority.** It
> compares a *rendered projection* — residual kind and some ids and counts — not
> every residual, unwind or dynamic-edge member. The lawful relation is stronger
> and structural: the exact callee child is the binder-provenance `Var` resolved
> to the locator's immediate-environment index, and `value_at` forwards that
> operand directly into `source_call_state`. **Pin that typed graph/binder/call
> relation.** Do not compare debug strings, scan environments, compare runtime
> words, or infer identity from numbers or payload shape.

### Forbidden

Scanning `continuation_calls` as a selection strategy;
`checked_ih_environment_transports_owned_by(owner)` or any singleton / first
match; a descendant-only capability accessor; carrying identity through the
runtime environment, ABI, frame header, `RoutedAnswer`, or source control;
tracing `R1`; re-running binder/layout derivation; comparing runtime words;
adding a fourth semantic or suppression axis; minting a `PlanId`; combining maps
across plans; any context split, region clone, tail duplication, new occurrence
identity, source rewrite, or runtime selection.

## Prior-art hazards — status after the recut

The first cut folded in five hazards from Research Q2, all keyed to specialization
cloning and caller retargeting. **Most are now moot because the mechanism they
guarded is withdrawn**, and saying so explicitly is part of the recut:

1. **Under-keyed specialization caches return wrong code** (Lean
   `specialize_cache_miscompile.lean`). Still live in substance: interning ORDER
   must not change association. Answered by `AC-CONF-PERMUTE`.
2. **Pre-retarget reachability becomes false after retargeting** (LLVM #154668).
   MOOT — nothing is retargeted. `intern_generated_contexts` and its existing
   callers are unchanged.
3. **Reused clone identity leaves stale auxiliary state** (LLVM #58759). MOOT —
   nothing is cloned and no context-indexed projection is rebuilt.
4. **Bulk replacement violates target type/ABI** (LLVM #55000). MOOT as a
   retarget hazard. Its descendant is the recheck of all three context-identity
   fields before installation (`AC-CONF-INSTALL`).
5. **Recursive specialization diverges when keys grow with traversal history.**
   MOOT — the coordinate is a fixed seven-field record and does not grow with
   traversal.

## Deliverables

- The three planner types and the plan-owned
  `BTreeMap<Coordinate, Confluence>` (mechanism 1).
- `build_checked_ih_generated_entry_confluences` and
  `validate_checked_ih_generated_entry_confluences`, invoked from
  `construction.rs` immediately after
  `validate_checked_ih_continuation_inheritances` (mechanism 2).
- The five set closures in the validator (mechanism 3).
- The sanitized `CheckedIhGeneratedEntryAccess`, installed in
  `FunctionLocalRefs` before `lower_expr`, and the single fail-closed
  `BTreeMap::get` consumer route (mechanism 4).
- The consumer-adjacent completeness instrument at the real D3A call seat, which
  neither applies `K` nor binds `R2` (mechanism 5).
- The controls below. **No D3A application, no `R2` binding, no fresh-result
  destination change, no fourth axis.** D3A remains ABSENT on `main`.

## Acceptance criteria

- **AC-CONF-POSITIVE** (the REAL collision is the mandatory positive) — the
  governed write witness's actual W0/W1 collision at
  `(ContinuationContextId(0), invocation 741, callee 739)` forms ONE coordinate
  class with members `{A, B}`, distinct source identities, one equal projection
  by full typed `Eq`, and unchanged context and retarget caller. A synthetic
  context-sharing mutation is NOT a substitute for the governed population.
- **AC-CONF-CONTEXT-NOT-KEY** (the negative that the first cut had no analogue
  for) — W2 remains a SEPARATE singleton class despite sharing write context 0
  with W0/W1, and R0/R1 remain separate classes despite sharing read context 0.
  A control that keys the quotient on context rather than coordinate REJECTS.
  This is the control that catches a certificate which passes its positive case
  while merging unequal rows.
- **AC-CONF-IDENTITY-NOT-KEY** (the falsified split cannot come back) — adding
  source identity to the quotient key REJECTS, because W0/W1 then split into two
  duplicate certificates for one coordinate.
- **AC-CONF-PROJECTION-NOT-KEY** (disagreement must reject, not partition) —
  adding the projection to the quotient key REJECTS. A disagreeing member must
  fail the build; it must never become a second green class.
- **AC-CONF-MEMBER-DISAGREE** (per-component, independently) — INDEPENDENTLY
  mutate one W0/W1 member's destination owner; destination body; each
  `CheckedIhBinding` field; every `CheckedIhImmediateKBindingLocator` field
  including `environment_domain` and `environment_index`; and every
  `CheckedIhFreshResultDestination` member including constructor-child role and
  coordinate, closure environment record, closure origin, closure body origin,
  closure parameter count, capture ordinal, capture occurrence, and
  `body_capture_reads` membership. **Each disagreement REJECTS before
  publication. None may split the class, choose the other row, or erase the
  disagreeing row.** Every mutation restores BYTE-IDENTICALLY.
- **AC-CONF-SET-CLOSURE** (SET equality, never counts) — INDEPENDENTLY: removing
  A while B remains; duplicating A; and filtering the disagreeing row are ALL
  rejected by set closure. The five closures of mechanism 3 are asserted as
  sets; a counts-only assertion is NOT accepted, because counts permit an A/A
  substitution for `{A, B}`.
- **AC-CONF-TOTAL** (the node's own totality obligation) — the validator
  re-derives the full map from the same planner graph and requires exact
  equality, over EVERY governed inheritance whose destination resolves to an
  emitted context. If a context exists, absence from the certificate is an
  ERROR; the exclusion of context-less inheritances is re-derived by the
  validator, not assumed. The six-row advisory decided the branch and does NOT
  discharge this.
- **AC-CONF-REOPEN** (through the existing accessor, view unchanged) — every
  coordinate and projection is formed ONLY from the view returned by the
  existing `checked_ih_continuation_inheritance_for_invocation`, and that
  returned view must equal the inheritance it was opened from. A control
  asserting no parallel locator or fresh-destination derivation exists anywhere
  in the diff passes.
- **AC-CONF-CALLER-AUDITED** (iteration is a graph consequence, not a search) —
  the retarget caller is derived from the validated call population, requires
  EXACTLY ONE call targeting the enclosing specialization, is reopened through
  `continuation_call_binding_for`, and all classes for one context name the same
  caller. A control proving the caller is unreachable from lowering passes.
- **AC-CONF-PERMUTE** (numbering is not identity) — permuting inheritance and
  context interning order may change dense `ContinuationContextId` numbers, but
  the coordinate/class/member/caller ASSOCIATION must not change. A control
  asserting a fixed numeric id is NOT accepted as evidence (hazard 1).
- **AC-CONF-SANITIZED** (lowering cannot see what authorized it) — the installed
  `CheckedIhGeneratedEntryAccess` contains no member identities, no retarget
  caller, no transport, and no ancestry. **Type and privacy boundaries make
  those unavailable to lowering — a raw source-text assertion is NOT accepted as
  the proof.** Exactly one `BTreeMap::get` consumer route exists; absence fails
  closed; it never searches raw inheritances, calls, environments, owners, or
  candidates.
- **AC-CONF-INSTALL** (three-field recheck, one function only) —
  `define_continuation_context_bodies` rechecks context id, enclosing
  specialization, and worker body before installing into `FunctionLocalRefs`,
  and installation happens before `lower_expr`. Other functions carry `None`; a
  control asserting that passes.
- **AC-INTERNER-UNCHANGED** (the mechanism this node does NOT touch) —
  `intern_generated_contexts`, its key, context counts, and existing retarget
  callers are UNCHANGED. **Read compiles to one context, write to two.** No
  clone, no caller rewrite, no split. A control asserting the context count per
  compilation passes.
- **AC-ENTRY-COMPLETE** (consumer-adjacent, and the gate the narrow predecessors
  lacked) — an instrument immediately at the REAL D3A call seat inside the
  generated-context lowering path observes, on the unchanged read program and
  the independently derived write program, that the seat is reached with the
  projection obtained through the single sanitized route, and that the
  projection satisfies **all six conjuncts of section 5's terminal premise** —
  typed-identity agreement, exact `ImmediateInvocationEnvironment` with an
  in-range index, callee-child binder provenance resolving to that exact index,
  a binding of exactly
  `Value(Specialized(ComputationalRecursorClosure { .. }))`, capsule
  coordinate/frame/slot/call-template agreement with a `Carried` residual, and
  the fresh-result destination observed only. **The W0/W1 seat
  is visited ONCE with membership `{A, B}` — per-identity arrival is NOT the
  shape here and must not be asserted.** The instrument does NOT apply `K` and
  does NOT bind `R2`. Read and write are derived independently from their own
  planner facts and never cross-used.
- **AC-CAPSULE-NEGATIVES** (HS9 ruling controls — INDEPENDENTLY reject) — each
  of these must reject on its own: an outer `Carried`; **every non-recursor
  `Specialized` sibling class**, used as a mutant, so that a bare
  `Specialized(_)` arm cannot pass; `StaticWorker`; a wrong locator domain or
  index; a wrong binder, frame, computational-IH slot, invocation, call or
  callee; a wrong destination owner or body; a non-`Carried` residual for these
  governed rows; and a locator/callee provenance mismatch. **Deleting the guard
  and accepting bare `Specialized(_)` are EXPLICIT NON-DISCHARGES** — neither
  may be offered as evidence, whatever the suite reports. **Mutate the governed
  property or population, never the detector.**
- **AC-NO-TRANSFER** (the refusal that already exists must stay, and must not
  become the route) — the existing carrier-boundary refusal of this capsule is
  preserved, and a control proves that **neither residual extraction nor
  `transfer_into_carrier` becomes the application route**. The capsule is not
  converted, its residual is not extracted as `K`, no environment is moved, and
  no other seat is selected.
- **AC-GUARD-DISABLED-IS-NOT-EVIDENCE** (bounds how HS9's own probe may be
  cited) — the write compile in Research's advisory was reached by DISABLING the
  frame's Carried-only refusal, and the governed command then passed `1/1`.
  That number is observation reachability only. **A green governed command
  obtained with any acceptance guard removed discharges nothing here**, and the
  positive must pass with the six-conjunct premise of section 5 in force. Note
  that the falsified Carried-only predicate itself cannot remain — what remains
  intact is fail-closed rejection OUTSIDE the ruled admissible arm.
- **AC-CONSUMER-ADJACENT-CLOSURE** — consumer-adjacent read and write
  observations independently close raw `(coordinate, source identity)` pairs,
  class members, installed keys, and reached keys, as sets.
- **AC-KEY-NEGATIVES** (wrong facts reject) — INDEPENDENTLY: a wrong context id;
  a wrong enclosing specialization; a wrong worker body; a wrong final
  invocation, call, callee, or binding; and a wrong locator domain or index each
  REJECT at the sanitized `get`.
- **AC-BLAST-RADIUS** (name the outward surface, gate on it) — this node adds a
  plan-owned map and a `FunctionLocalRefs` field. Enumerate every consumer of
  `StaticTransitionPlan` and of `FunctionLocalRefs`, name the suites that cover
  them, and run those suites as part of this node's gate. A census that looks
  only AT the new types is not sufficient; look OUTWARD. State the enumerated
  count in the handoff.
- **AC-INERT** (behaviorally inert at the product) — D3A is still ABSENT on
  current main; both read and write products stay at their existing fail-closed
  defaults; the emitted ABI, call, and artifact surfaces are BYTE-IDENTICAL;
  `CheckedIhFreshResultDestination` is unchanged; D1 and the erasure blob are
  unchanged; context counts are unchanged. The node mints no fourth semantic
  axis, second binder catalog, second ABI lane, continuation identity, or result
  path.
- **AC-NO-RUNTIME-CARRIER** (the carrier is compile-time, structurally) — the
  installed access object appears in generated-context definition/lowering state
  ONLY. A control asserting its absence from every ABI slot, frame header word,
  environment binding, carried value, `RoutedAnswer`, `SourceControl` member,
  and runtime tag passes, and `SourceControl` remains identity-free across the
  function-root boundary.
- **AC-NO-REGRESSION** — whole-suite green in CI; local targeted
  `scripts/ken-cargo -p ken-runtime` / `-p ken-cli` / `-p ken-verify` only,
  never `--workspace`.

## Reviewers

Architect (required — M-series). The review turns on: the coordinate is the
quotient key with source identity and projection both EXCLUDED, and the three
key-shape negatives all bite (`AC-CONF-IDENTITY-NOT-KEY`,
`AC-CONF-PROJECTION-NOT-KEY`, `AC-CONF-CONTEXT-NOT-KEY`); coordinate and
projection are formed only from the reopened existing view
(`AC-CONF-REOPEN`); closure is SET equality over pairs
(`AC-CONF-SET-CLOSURE`, `AC-CONF-TOTAL`); the retarget caller is an audited
graph consequence hidden from lowering (`AC-CONF-CALLER-AUDITED`); the lowering
object is sanitized and reached by exactly one fail-closed route
(`AC-CONF-SANITIZED`); the interner is untouched (`AC-INTERNER-UNCHANGED`); and
the certificate's claim is stated no more strongly than common typed consumer
authority at one seat.

runtime-qa. The review turns on: `AC-CONF-MEMBER-DISAGREE` mutates every
projection component INDEPENDENTLY with byte-clean restore and each rejects
before publication; `AC-CONF-SET-CLOSURE`'s three set mutations each bite;
`AC-CONF-PERMUTE` proves association stability rather than a fixed numeric id;
`AC-ENTRY-COMPLETE` observes the W0/W1 seat ONCE with membership `{A, B}` and
does NOT assert per-identity arrival; `AC-CONF-SANITIZED` is proved by type and
privacy boundaries, not by grepping source text; `AC-BLAST-RADIUS` enumerates
outward consumers and gates on their suites; `AC-INERT` and
`AC-INTERNER-UNCHANGED` hold at the product.

**If the confluence certificate CANNOT be obtained under this shape** — if it
needs a runtime carrier, a search route, an owner singleton, a descendant-only
accessor, or any context split — **STOP and route to Architect/Steward. Do not
weaken the shape and do not frame a narrower slice.** That the last narrow slice
always looks available is the defect HS7 named, and reaching for one is what
produced HS8.

## Capability tier

T1 — a soundness-bearing planner component reviewed on an argument, not a
differential diff: a quotient whose key membership is load-bearing in both
directions, a closure argument that must be set-valued rather than counted, a
sanitization boundary that has to be enforced by types rather than asserted, and
a consumer-adjacent completeness proof at a seat that must not perform the
operation it is proving reachable. Size L.

## Sequencing

Lane-1 (runtime, priority). This is the HS7 replacement predecessor for
[[RT-RESULT-CONTINUATION-BINDING-PROVENANCE]], downstream of the merged
[[RT-CHECKED-IH-K-AVAILABILITY-LOCATOR]] whose locator it consumes through the
existing accessor without re-deriving it. It is independently landable and lands
FIRST.

**Runtime remains held until this recut lands and the Steward EXPLICITLY
re-releases the node.** Frame landing alone authorizes nothing.

**D3A stays frozen and non-landable throughout.** The atomic D3A+D3B contract
and the THREE independent suppression axes (inheritance, application,
fresh-result binding) are UNCHANGED and are not reopened here.

**What the HS9 ruling fixes about the eventual successor, so it is not
re-litigated later** (Architect `evt_2kgb55azx3ej3`): this predecessor validates
that the existing exact callee IS the lawful capsule before the ordinary
consumer proceeds. When D3A+D3B is later released, **D3A authorizes exactly ONE
application of this exact capsule to produce fresh `R2`, and D3B binds that
fresh result at the already-proved destination. The existing call, continuation
and result are NOT duplicated.** That is a constraint on the successor, not an
authorization of it — nothing here releases D3A.

After this predecessor passes Architect review, QA, its merge Decision, and
publisher CI,
and lands, the **Steward must EXPLICITLY re-release the same atomic D3A+D3B
consumer — landing alone authorizes nothing, and neither frame landing nor
predecessor landing authorizes consumer work.** The consumer branch stays clean
and held at the exact base; no fallback, no partial, no application-only
checkpoint.

Single Runtime lane object at a time; PX8 stays blocked until the whole native
carried-value program lands.
