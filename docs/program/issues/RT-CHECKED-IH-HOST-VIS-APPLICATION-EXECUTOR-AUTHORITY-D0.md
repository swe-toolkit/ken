---
id: RT-CHECKED-IH-HOST-VIS-APPLICATION-EXECUTOR-AUTHORITY-D0
title: "Scratch-only Host-Vis application-executor-authority D0 (no production, no candidate, no QA, no Decision, no merge): answer ONE question before any application-executor design — does the later CheckedHostVisContinuation application at marker/call 147/146 (owner PredeclaredFunctionId(5)/funcid45, template 4, callee origin 145 Var(4), sole argument origin 144 Var(0) at environment index zero = the existing sole HostResult under the FSOp::ctor_488 branch at match origin 268) already carry a COMPLETE application recipe for worker body 662: an exact planner-issued route and target; explicit operand origin 144 as HostResult; captures 0..7 from record 608 in planner order; and any route-mandated suffix? Force eligibility is narrowed structurally to the exact 147/146 CheckedHostVisContinuation; the zero-argument OrdinaryApplication at 474/473 remains environment materialization and MUST be an explicit negative/no-call control. Do NOT widen CheckedIhEnvironmentTransport to Host-Vis (it stays environment-only; source_call_identity() stays the materializer identity). Trace the current funcid45 -> funcid50 call and identify precisely where the HostResult is retained, consumed, or lost. ONLY IF the exact route survives may one scratch call be made through the existing target-table mechanism under a separate move-only application authority, typed CheckedIhApplicationResult, Trap read before Result, paired naturally to match 451; otherwise return the exact lost-route/missing-binding outcome without selecting funcid43 by number/declaration/body-equality/table-coincidence/proximity. Restore byte-clean and report."
status: closed
owner: runtime
size: M
gate: none
tier: T1
depends_on: []
blocks: []
github: null
origin: "Architect ruling evt_68ce7w8x6nb75 (thr_3fykd2macwy3t, 2026-08-30), accepting the runtime-implementer's RT-CHECKED-IH-CARRIED-WORKER-EXECUTOR-AUTHORITY-D0 outcome 4 (report evt_5tm8gbxs7584g, SHA-256 1b4fd13b68a1..., scratch-diff 59dfbc78a6b5...). The Architect reproduced all seven product blobs and every listed digest and confirmed: 474/473 is the zero-argument OrdinaryApplication escape that constructs ITree::Vis's carried checked-IH environment (record 608 reconstructs captures 0..7 but supplies neither body 662's explicit argument nor a call route; at carried funcid50 the environment has no StaticWorkerBinding, hence no carried StaticWorkerCallRoute; coincident worker_calls[662]/raw_worker_calls[662] do NOT restore authority). The semantic application is LATER at CheckedHostVisContinuation 147/146 (owner PredeclaredFunctionId(5)/funcid45), whose sole argument origin 144 is the existing sole HostResult produced after host dispatch — the arity-one value body 662 needs. The current funcid45 calls funcid50 seven times and raw body funcid43 zero times, so the semantic seat is identified but no executor route there is yet proven. Recut narrows force eligibility to 147/146. Steward-recut per COORDINATION section 2. Base origin/main e1ac1f27b5d55c6011db0d88347b479a093a1ff4, tree 9e4f8b20f3384ab4d4bf6f9046a929aba02c2cad; all seven reviewed product blobs Steward-verified identical to accepted base 0be25235b. @steward owns close/reframe/release; runtime parked until this named kick. Recurring predicate: authority for one semantic operation used as authority for another — this D0 tests whether the RIGHT semantic seat (147/146) carries its own executor authority, rather than borrowing another's."
---

> # CLOSED — D0 COMPLETE, OUTCOME 3 ACCEPTED (147/146 is a real executor but binds
> # outer body 941 / GeneratedContext / funcid50, NOT body 662; body-662 RawWorker
> # route absent at the seat). Architect ruling `evt_9rvkwywwxms1`
> # (thr_7f2m78epewqv1, 2026-08-30).
> #
> # Measurement node — never `merged`; report `evt_6rvrzpg80c02j` (SHA-256
> # `7568d07b1780...`, scratch-diff `1bf9cab156f4...`) restored byte-clean at base
> # `0ec00c1ed`. The Architect reproduced all seven product blobs and every digest
> # and accepted the outcome. Finding: application 147/146 owns a complete route
> # only for outer body 941 / GeneratedContext / funcid50 and owns origin 144's
> # HostResult; body 662's distinct specialization-1 RawWorker authority stays
> # upstream at funcid47; record 608 owns captures 0..7 but not callable authority;
> # 474/473 is environment materialization. No lawful operation recovers dropped
> # body-662 authority from declaration, body identity, table presence, outer route,
> # payload shape, or captured word. No scratch call, no manufactured controls —
> # correct.
> #
> # Third consecutive hard stop on the body-662 executor-authority question. The
> # Architect held the ruling for a Research prior-art advisory (`evt_1c6ed7gntxg9s`
> # — qualified positive on a planner-issued affine application capsule with typed
> # nested placement, or a staging-boundary change) and then RULED
> # (`evt_9rvkwywwxms1`): SELECT the planner-issued affine checked-IH application
> # capsule with typed nested generated-context placement.
> #
> # Next: scratch-only successor
> # **`RT-CHECKED-IH-AFFINE-APPLICATION-CAPSULE-FEASIBILITY-D0`** on the same base
> # `0ec00c1ed` — a FEASIBILITY test of the selected representation, not a fourth
> # executor-seat search. Everything below this banner is retained as chronology.
>
> # READY — SCRATCH-ONLY D0. Released to the runtime ring (lane 1) on `origin/main`
> # `e1ac1f27b`. Runtime is parked; this IS the release.
>
> This is a MEASUREMENT node. It lands NO production, opens NO candidate, routes NO
> QA, and needs NO Decision or merge. It returns a report plus digests (and a
> scratch diff only if a call is authorized), and the branch is restored byte-clean
> at the end. The Architect reviews the D0 report and chooses the next design.
>
> **Why we are here.** `RT-CHECKED-IH-CARRIED-WORKER-EXECUTOR-AUTHORITY-D0` closed
> at outcome 4: `474/473` is environment materialization, not worker execution, and
> it has no route authority at the carried seat. The Architect identified the
> ACTUAL semantic application as the LATER `CheckedHostVisContinuation` at
> `147/146`, whose sole argument is the existing `HostResult` body 662 needs. This
> D0 asks whether THAT seat already carries a complete executor recipe — testing
> whether the right semantic seat carries its own authority, rather than borrowing
> another operation's (the recurring predicate this chain keeps surfacing).
> **Do not widen `CheckedIhEnvironmentTransport` to Host-Vis; do not select
> `funcid43` by number/declaration/body-equality/table-coincidence/proximity; do
> not treat `474/473` as the executor.** Measure first.

## The one question this D0 answers

Does the later `CheckedHostVisContinuation` application at `147/146` already carry
a **complete application recipe** for worker body 662 — exact route and target,
explicit operand origin 144 as `HostResult`, captures 0..7 from record 608 in
planner order, and any route-mandated suffix?

## Exact base and coordinates (Architect `evt_68ce7w8x6nb75`)

Base `origin/main` `e1ac1f27b5d55c6011db0d88347b479a093a1ff4`, tree
`9e4f8b20f3384ab4d4bf6f9046a929aba02c2cad`. All seven reviewed product blobs are
identical to accepted base `0be25235b` (core `eea98dc6`, source `c39f82e7`,
planner aggregates `e7bc3628`, lowering aggregates `eaf1019b`, calls `fa010fed`,
units `ccc6ddb2`, parity test `6b2f14a7`). **If main advances before pickup,
recheck those seven blobs before edits.**

The narrowed force target: `CheckedHostVisContinuation` at owner
`PredeclaredFunctionId(5)`/`funcid45`, marker/call `147/146`, template 4, callee
origin 145 `Var(4)`, sole argument origin 144 `Var(0)` at environment index zero —
the existing sole `HostResult` (`Result::Ok`/`Result::Err` constructors), produced
after host dispatch under the `FSOp::ctor_488` branch at match origin 268. The
demoted occurrence: zero-argument `OrdinaryApplication` `474/473`, which remains
environment materialization.

## Required D0 evidence (each mandatory)

1. **Confirm the narrowed force eligibility structurally.** Show that the exact
   `CheckedHostVisContinuation` at `147/146` is the eligible application and that
   the zero-argument `OrdinaryApplication` `474/473` is excluded — the latter is an
   explicit negative/no-call control, not the executor.
2. **Route and target for body 662 at the `147/146` seat.** Identify the exact
   planner-issued `StaticWorkerCallRoute` and target for body 662 reachable from
   this seat, and whether it survives here. A declared `funcid43`, body-origin
   equality, target-table presence, arity, or shape is NOT route authority (the
   route type's law is carried-only).
3. **Operand recipe.** Show explicit operand origin 144 as the `HostResult`;
   captures 0..7 from record 608 in planner order; and any route-mandated suffix.
   Name the one arity-one explicit argument by producer/origin/domain/word.
4. **Trace `funcid45 -> funcid50` and locate the `HostResult`.** The current
   `funcid45` calls `funcid50` seven times and raw body `funcid43` zero times.
   Identify precisely where the `HostResult` is retained, consumed, or lost across
   that call and the generated-unit boundary.
5. **Only if the exact route survives**, make ONE scratch call through the existing
   target-table mechanism under a move-only application authority SEPARATE from the
   environment transport. Its result must be typed `CheckedIhApplicationResult`,
   Trap read before Result, paired naturally to match 451. Preserve the
   carried-environment crossing as no-call. `CheckedIhCapturedEnvironment` remains
   unrepresentable as that result.
6. **If a call is authorized,** route/table-swap and explicit-argument
   suppression/replacement controls are owed (each reaches its own exact refusal,
   then restores). If the route does NOT survive, do NOT make or invent a call or
   controls.
7. **Restore byte-clean** and return the report, scratch diff if any, all
   commands/statuses, generated CLIF, and evidence digests.

## Exhaustive outcome routing (land in exactly one)

- **147/146 carries a complete, surviving executor recipe** — return the exact
  route/target/operand recipe and the natural (successful or failing) result at
  match 451 for a later design ruling.
- **Route/target exists but the operand (HostResult origin 144) is not available at
  the seat** — name where the `HostResult` is retained/consumed/lost across
  `funcid45 -> funcid50`; do NOT call.
- **Route is absent or ambiguous at 147/146** — report the lost-route/missing-binding
  outcome; do NOT select `funcid43` by number, declaration, body equality, table
  coincidence, or proximity.
- **147/146 is not itself the executor seat either** — narrow further and identify
  the actual executor occurrence; do NOT force here.

## Closed axes remain closed (do NOT)

Do not widen `CheckedIhEnvironmentTransport` to Host-Vis (it stays environment-only;
`source_call_identity()` stays the materializer identity). No tag/store/scan,
environment-as-result, synthetic `ResourceBodyOk`, `ResourceBodyResult` weakening,
Trap reinterpretation/reordering, Host-Vis re-application, spent caller-success
seat, or a callee chosen without existing route authority. Preserve
`ResourceBodyResult`, natural match semantics, typed trap 36, and all prior C/I/E/S
and source/member/projection/direction/delivery controls. This D0 is a measurement
of existing authority at the corrected seat, not a design.

## Reviewers, sequencing, contention

- **Reviewer:** the Architect reviews the D0 report, digests, and any scratch diff,
  and chooses the next design per the outcome routing. No Runtime QA (scratch-only
  measurement), no Conformance Validator, no Decision, no publisher CI. Never
  `merged`.
- **Sequencing:** runtime ring (lane 1), single continuous turn to a complete report
  or a genuine blocker. Size M, tier T1 (deep route/authority reasoning across the
  Host-Vis continuation seat and the generated-unit boundary). Restore byte-clean at
  the end regardless of outcome.
- **Contention:** reads and (only if authoritative) scratch-prototypes
  `crates/ken-runtime/src/cranelift_backend/lowering/{core,source,calls,units}.rs`,
  the static-worker target-table / `worker_calls` machinery, the generated-unit
  planner, and the `rt_parity_native.rs` evidence wrappers; produces no landed
  change, so no crate/catalog contention with the concurrent lanes. Targeted builds
  ONLY via `scripts/ken-cargo` scoped to `ken-runtime`, never `--workspace`.
