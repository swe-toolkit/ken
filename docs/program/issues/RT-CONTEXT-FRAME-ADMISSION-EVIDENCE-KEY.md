---
id: RT-CONTEXT-FRAME-ADMISSION-EVIDENCE-KEY
title: "RE-HOMED from section 3 of the REFUTED RT-CONTEXT-FRAME-SLOT-HOLDS-ONE-PER-FUNCTION, which would have deleted it. The two readers of constructed_context_frame key differently and that is DELIBERATE and SOUND -- measured, not argued: the permission seat's gate has exactly one caller crate-wide, it refuses outright when two contexts own one body so claims is DETERMINED by body_origin rather than selected, and the frame gates WHETHER and never WHAT because both the queried and the returned origin come from the closure structure with the frame nowhere in the data path. What is open is one step further in: the permission seat is safe BECAUSE the data seat re-checks at calls.rs:1008-1052, three fail-loud Err(unsupported) returns against two independent authorities -- and that dependency is recorded ONLY at the seat that depends on it. calls.rs is 2499 lines with zero hits for recursive_position_captures or planner_recoverable, and its filter comment explains the condition entirely in terms of the gather's own correctness. An editor relaxing those three checks sees no reason not to, and the licensing sentence is the obvious one: the admission query already matched on body origin and both cardinalities, so re-checking here is redundant -- true about what the other seat does, false about what it means. Verdict node plus about four lines of comment at the ENFORCING seat. No key change, no relaxation."
status: draft
owner: runtime
size: S
gate: none
depends_on: []
blocks: []
github: null
tier: T1
origin: "Steward, 2026-09-17. RE-HOMED from RT-CONTEXT-FRAME-SLOT-HOLDS-ONE-PER-FUNCTION section 3 at that node's REFUTED closure (evt_339x9h7ys4pe9): the hazard never depended on the refuted premise and a deleted node is a common way for the only statement of a hazard to vanish. Population measured at b0421afd0 by the Steward, all occurrences not a sample: mod.rs:1249 declaration, mod.rs:964 per-function init to None, core.rs:10866 sole write, calls.rs:986 and core.rs:13577 the two readers, plus five None initializers in core/tests/. RE-AIMED before kickoff by the Architect (evt_3pptybqw28dgx), whose measurements the Steward re-ran independently from the objects: the original question -- is the subset key sound -- is now ANSWERED YES and closes on being asked, because the gate carries a uniqueness refusal at core.rs:13538-13549 that makes claims determined rather than selected, and the frame is absent from the data path at core.rs:13672/:13680. The live question is whether the delegation to calls.rs:1008-1052 is load-bearing and whether it is recorded where its enforcer reads it. AC-0 is a mutation control at the enforcing seat, gated behind a REACH precondition because the four RT-CARRIED-RESIDUAL-IH-ARITY rows are refused upstream of that seat and are therefore the least likely programs to reach it -- a green mutation there would mean nothing ran, not that nothing depends on it. QUEUED behind the L1 objective; not released."
---

> # FILED, NOT RELEASED. Queued behind the L1 depth census.
>
> Frame: `docs/program/wp/RT-CONTEXT-FRAME-ADMISSION-EVIDENCE-KEY.md`.
>
> ## THE ORIGINAL QUESTION IS ANSWERED. DO NOT RE-ASK IT.
>
> *"Do the two readers key differently?"* — **yes, deliberately, and soundly.**
> Measured at `b0421afd0` by the Architect and re-run independently by the
> Steward. Re-asking it gets "yes, correctly" and closes the node.
>
> ## THE LIVE QUESTION
>
> **The permission seat is safe BECAUSE the data seat re-checks. Is that
> delegation load-bearing, and is it recorded where its ENFORCER will read
> it?** It is currently written down only at the seat that *depends* on it.
>
> ## CARRY THE DISAMBIGUATOR AS A PROPERTY, NOT AS COORDINATES
>
>     THE PERMISSION SEAT   returns Result<bool>.   Answers WHETHER.
>     THE DATA SEAT         returns the operands.   Answers WHAT.
>
> A grep on the field name returns both and distinguishes neither. **Use the
> return type.** `core.rs` is the lane's hottest file and line numbers go stale.
>
> ## A GREEN MUTATION HERE HAS TWO CAUSES AND THEY ARE OPPOSITE
>
> AC-0 mutates the three checks at `calls.rs:1008-1052`. **AC-1 gates it:
> establish your program REACHES that seat first.** The four
> `RT-CARRIED-RESIDUAL-IH-ARITY` rows are refused **upstream** of it, so they
> are the least likely programs to exercise it — a null result from them means
> *nothing got there*, not *nothing depends on it*. **State which cause your
> null result has, or it is not a finding.**
>
> ## `RTPROBE-WRITE = 1` IS FOUR ROWS
>
> Not a claim about the planner's reachable states. **The predecessor died of
> that promotion.** Cite it with its reach attached or not at all.
>
> Verdict node plus about four lines of comment. **No key change, no
> relaxation.** Independent of
> `[[RT-CONTEXT-FRAME-REFUSAL-DEPTH-CENSUS]]`; neither blocks the other.
