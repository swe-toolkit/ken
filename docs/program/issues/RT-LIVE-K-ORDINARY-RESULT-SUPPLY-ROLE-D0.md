---
id: RT-LIVE-K-ORDINARY-RESULT-SUPPLY-ROLE-D0
title: "Scratch-only ordinary result-supply-role D0 (no production candidate, no QA, no Decision, no merge): the LIVE-K-INNER-APPLICATION-AUTHORITY D0 proved outcome D and the Architect CLOSED the affine body662/body888 executor route for this defect — inside the live k, read selects application473 (unit2/body941) carrying body662 binding/target metadata but NO explicit argument demand/producer/type/edge with zero emitted calls to body662, and write selects application486 (unit5/body1259) whose owner names body1238 not body888 (body888 sits behind LexicalClosure1246 outside the live source-control closure and hits zero); the seam is a nullary captured-environment materialization versus a later source Call applying that environment, and no such Call exists before the ordinary downstream result consumer, so no independently represented affine (target, explicit argument) application exists and the executor route, future-endpoint capsule route, and entry-HostResult substitution are all closed. This successor LEAVES the executor axis and starts at the actual ordinary value edge now proven to run: read app473 materialization -> body941 result -> specialization2-emitted call to body452 -> Match451/trap36, and write app486 -> body1259 -> body465 -> Match464/trap37. For BOTH witnesses, using NO body/table/shape/count/proximity inference, no environment-as-result, no synthetic success, and never consulting the withdrawn body662/body888 executor route as authority: derive the complete actual operand lineage from the materializer's carried environment through every source-machine value/control transition, construct/project, producer Result slot, emitted downstream call argument, callee formal, and match scrutinee; independently derive the downstream semantic demand from the source continuation formal and the closed ResourceBodyResult match family (never from the actual word or trap); enumerate every live source-derived producer that can satisfy that demand and reconcile it with the reference executor's normal ResourceBodyOk path. The fixed expectation is the downstream continuation formal plus the semantic result-family demand; actual supply is the real source-machine value lineage and emitted CLIF argument; they meet at one consume site but must have independent provenance. Mutate the actual operand producer against unchanged demand; separately drop/duplicate the actual producer-to-consumer edge population; any redirect retains the consumer key and cardinality one while replacing only the supplied payload; a comparison clone, changed lookup key, invented constructor, or detector mutation is no evidence, and if no legal mutation subject exists report that honestly. Preserve live apps138/175, materializers473/486, exact effect order, Trap-before-Result, identities36/37, capsule/raw zeros, the 474/473 no-call materializer, and marker-disabled CLIF identity. Land in exactly one of four outcomes. Restore byte-clean and report generic derivation, exact rows, controls, and hashes."
status: closed
owner: runtime
size: L
gate: none
tier: T1
depends_on: []
blocks: []
github: null
origin: "Architect ruling evt_5kqhv68j9bxgb (thr_6pp5xa792rpmb, 2026-08-31), ACCEPTING the runtime-implementer's RT-CHECKED-IH-LIVE-K-INNER-APPLICATION-AUTHORITY-D0 as a scratch measurement with outcome D for both witnesses and CLOSING the affine body662/body888 executor route for this defect. The ruling binds byte-clean pickup 8b3e2d7378073bd1e6ae628579e2081599be695f, tree 4f44ae2f3483d621163f790c97acec9c27133e70, and report/diff/map/evidence-manifest/marked-CLIF-manifest/preserved-CLIF-manifest SHA-256 92bbf5797c208cb9b83cc89cc8843bd8aa7c78f85909682dd067a28641fb050d / 911bfea41865176ba9496faede38353a432e422a98d875c96eb54c875836b4c6 / c22b62b5bbc2b84eb4693a3cd08cbe27641990d635621a8993361a63b348049a / b424cece3c7494b31e971b56ed8876422316d860b34034a3492952e4644495fb / c4814999269031e9b0e11241ff46cc572bf309113177f065d2522432b7b338ae / 041366350ea63eb60a4cc2ab53aa647a93c9f8428494c1d3a2c69ad7970eec19. The Architect verified all six hashes, all 34 evidence-manifest members, both 32-member CLIF manifests, applied the 13-path diff clean, ran scripts/ken-cargo check -p ken-runtime --features px8-ds-test-support green, and confirmed the marker-disabled read/write CLIF byte-identical to the accepted VIS-D0 CLIF. Finding: outcome D — read selects application473 (unit2/body941) with body662 binding/target metadata but no explicit argument demand/producer/type/edge and zero emitted calls to body662; write selects application486 (unit5/body1259) whose owner names body1238 not body888, with body888 (application699) behind LexicalClosure1246 outside the live source-control closure at zero; the seam is nullary captured-environment materialization versus a later source Call applying that environment, none of which exists before the ordinary downstream result consumer; so no independently represented affine (target, explicit argument) application exists and the body662/body888 executor route, future-endpoint capsule route, and entry-HostResult substitution are all closed for this defect. Accepted controls: actual-target StaticWorkerBinding mutation (body662->body766 read, body1238->body1075 write) reaches the distinct target-authority refusal; drop/duplicate reach cardinality zero/two; the actual-argument and redirect runs are honest CONTROL_NO_SUBJECT absence observations (fixed demand0, actual edge population0, no legal redirect sibling), credited only as fail-closed reporting, not as mutation proof. This completes hard stop 8 on the body-662 executor/staging question (EIGHTH consecutive hard stop: force D1 NO evt_7e6jprw80srj8; carried-worker outcome 4 evt_5tm8gbxs7584g; Host-Vis outcome 3 evt_6rvrzpg80c02j; capsule feasibility outcome 2 evt_1zpgs2h0kd74q; staged-call static outcome 1 evt_6qkn80ag2tq89 / evt_4maaydhem9esy; runtime-reachability outcome 1 evt_5ke73vgnrn / evt_7q7w05ag61zpw; vis-response outcome C evt_3hcsstqp13rxy; live-k-inner outcome D evt_5kqhv68j9bxgb / this frame). The executor hypothesis is now retired; the shared predicate — a valid static fact repeatedly placed where the consuming dynamic value/source-control choice does not coexist — resolves here as: body662/body888 carry target metadata but no source Call ever applies the environment as an argument. The chain now leaves the executor axis for the ordinary result-supply role. HARD STOP 9 GATE: any successor result other than routing A is hard stop 9, and before the Architect selects another technique or production repair it must obtain the mandatory Research prior-art advisory on the exact reported seam. Runtime production remains blocked until the successor is ruled. Base origin/main 8b3e2d737 (this doc-only recut commit only advances it; the accepted runtime-net product blobs are unchanged — recheck the seven at pickup against the accepted base, per the prior D0). @steward owns close/reframe/release; runtime parked until this named kick. Steward-recut per COORDINATION section 2."
---

> # CLOSED 2026-08-31 — READ A ACCEPTED (post-production supply loss); WRITE
> # NON-A (pre-production control loss) → HARD STOP 9, ADVISED AND RULED. Do NOT
> # relabel write as B. Architect ruling `evt_2k0rk8nwpn97c` (thr_51y9b78x92wz3),
> # binding byte-clean pickup `d9ed530dbf596b057003ff1363f3f1dbf8f0c8c2`, tree
> # `4bddffb5cdb0aa9e04ef1265883d7a41486f1054`, the reviewed
> # report/diff/maps/address/evidence/marked-CLIF/disabled-CLIF/base-CLIF
> # artifacts, and the HS9 Research advisory `evt_6w61wvkzgm8aq` (artifact
> # SHA-256 `f842adcb2d50b2921789599c09a4b95af7b2740c70223100e7637eef1592ad59`).
> #
> # **What was measured.** The runtime-implementer's fresh-process rebuild
> # (`evt_1yyytw4nkfs9f`) reported outcome A for both witnesses; the Architect
> # reproduced all eight advertised hashes, all 35 evidence-manifest members,
> # both 28-member CLIF manifests, applied the 11-path scratch diff clean, ran
> # `ken-runtime` check green, and confirmed disabled-versus-independent-base CLIF
> # byte-identity. **Read A is accepted** — LLDB observes producer705
> # `ResourceBodyOk` before materializer473; its downstream demand derivation
> # (call identity, target formal, closed `ResourceBodyResult` Match, scrutinee,
> # ordinary-envelope position, real assembled operand) is sound, and the actual-
> # producer / drop / duplicate controls reach the distinct disagreement and the
> # exact cardinality refusals; redirect is an honest `CONTROL_NO_SUBJECT`. **But
> # the existing read marker fires before `finish_source_constructor`**, so
> # completion, first ownership, and forwarding remain unmeasured. **Write A is
> # rejected** (`evt_41qd0sawvmg6z`): the write LLDB installs breakpoints for all
> # three claimed producers — origin507 (`ken_continuation_context_1`), origin720
> # (`ken_continuation_context_0`), origin940 (`ken_unit_5`) — and NONE fires; the
> # only write stops are materializer486, target entry, the demand-bearing local
> # call, and Match464, and the runtime address map carries no write producer
> # step. `build_ordinary_result_supply_roles_d0` computes `source_reachable` by
> # rooting `ordinary_d0_control_closure` separately at each producer's own
> # `function_body`, so it proves LOCAL reachability if that Function executes, not
> # that the Function is SELECTED on the native write path. Producer940 is locally
> # reachable in body979 and is selected by the reference InvalidOffset path, but
> # the native write path never enters that Function. **A statically present
> # constructor in an unexecuted Function is not a live runtime value a production
> # edge can route.**
> #
> # **Disposition (Architect).** Across the two-witness obligation the result is
> # non-A → HARD STOP 9. The mandatory Research advisory was obtained
> # (`evt_6w61wvkzgm8aq`): read is post-production supply loss, write is
> # pre-production control loss, and a carrier cannot repair a value that was never
> # produced. **The write artifact must NOT be recut merely by changing "write A"
> # to "write B"** — original outcome B requires one represented-but-unexecuted
> # transition and its exact first missing edge, and the present evidence
> # establishes the CLASS but not that coordinate; outcome C (selector/Function
> # edge never emitted) also remains open. No production design, candidate, QA,
> # Decision, CI, publication, or merge is authorized. **HS9 shares the chain's
> # governing predicate** (symptom-inventory entry 9 below): the structural closure
> # is now binding — no producer census rooted at its own Function body is
> # execution authority.
> #
> # **The recut successor is `RT-LIVE-K-RESULT-SELECTION-OWNERSHIP-D0`** (frame
> # filed the same day, released to the held runtime ring): a scratch-only,
> # phase-separated selection/ownership D0 with two arms in one measurement — a
> # READ arm that begins AFTER production (finish producer705, bind first owner,
> # follow to the body452/Match451 seam) and a WRITE arm that begins at exact
> # semantic Function/control selection (the emitted selector entering body979/unit5
> # for producer940 only, then its `Result::Err` → `InvalidOffset` successors). Any
> # successor result other than a genuinely-live A is HARD STOP 10. The READY
> # banner and body below are preserved as chronology.
>
> # READY — SCRATCH-ONLY ORDINARY RESULT-SUPPLY-ROLE D0. Released to the runtime
> # ring (lane 1) on `origin/main` `8b3e2d737`. Runtime is parked; this IS the
> # release.
> #
> # This is a MEASUREMENT node. It lands NO production candidate, opens NO PR,
> # routes NO QA, and needs NO Decision or merge. It reuses the accepted runtime
> # net, returns a report plus a scratch diff, source/CLIF/address maps, runtime
> # logs, and digests, and restores the branch byte-clean at the end. The
> # Architect ALONE reviews the D0 report and rules on the production design.
> # Like every prior D0/D1 in this chain, it is never `merged`.
> #
> # **The executor axis is CLOSED; this starts a NEW axis.** After eight hard
> # stops, outcome D closed the affine body662/body888 executor route: it carries
> # target metadata but no source `Call` ever applies the environment as an
> # argument. This D0 leaves that axis and asks the ordinary question — on the
> # value edge that PROVEN-runs (read app473 → body941 → body452 → Match451;
> # write app486 → body1259 → body465 → Match464), **where is the semantic
> # `ResourceBodyResult` supplied from?** Start at the actual ordinary value edge.
> # Do NOT consult the withdrawn body662/body888 executor route as authority, do
> # NOT revive it by inference, and do NOT treat environment material as a result
> # or an argument.

## What this D0 does

For BOTH read (app473 → unit2/body941 → body452 → Match451/trap36) and write
(app486 → unit5/body1259 → body465 → Match464/trap37) witnesses, using NO
body/table/shape/count/proximity inference, no environment-as-result, and no
synthetic success:

1. **Derive the complete actual operand lineage** from the materializer's carried
   environment through every source-machine value/control transition,
   construct/project, producer Result slot, emitted downstream call argument,
   callee formal, and match scrutinee.

2. **Independently derive the downstream semantic demand** from the source
   continuation formal and the closed `ResourceBodyResult` match family — never
   from the actual word or the trap.

3. **Enumerate every live source-derived producer** that can satisfy that demand,
   and **reconcile it with the reference executor's normal `ResourceBodyOk`
   path.** Do not consult the withdrawn body662/body888 executor route as
   authority.

## Expectation and actual supply must be independent

The **fixed expectation** is the downstream continuation formal plus the semantic
result-family demand. The **actual supply** is the real source-machine value
lineage and the emitted CLIF argument. They must meet at one consume site but be
derived from independent provenance — a control that rebuilds one from the other
is detector-side and proves nothing.

## Mutation controls

- **Mutate the actual operand producer** against unchanged demand; it must be
  refused at the distinct authority disagreement.
- **Separately drop and duplicate** the actual producer-to-consumer edge
  population, reaching the exact cardinality refusal.
- **Any redirect must retain the consumer key and cardinality one, replacing
  only the supplied payload.** A comparison clone, a changed lookup key, an
  invented constructor, or a detector-side mutation is no evidence.
- **If no legal mutation subject exists, report that honestly** (a
  `CONTROL_NO_SUBJECT` fail-closed observation) and do not claim the control.

## Preservation invariants

Preserve the live applications138/175, materializers473/486, exact effect order,
Trap-before-Result, planned identities36/37, the capsule/raw zeros, the 474/473
zero-argument no-call materializer, and marker-disabled CLIF identity. Restore
the branch byte-clean at the end.

## Exhaustive routing — land in exactly one

- **A.** A live semantic `ResourceBodyResult` producer already exists and the
  actual ordinary edge forwards the environment instead. Report the producer and
  the independent edge pairing; only then may a later production design route
  that existing result.
- **B.** The required producer exists only behind one source-control transition
  that is represented but not executed/emitted. Report the exact transition and
  the first missing edge; do not synthesize its result.
- **C.** No semantic result producer exists in the native plan although the
  identical reference path has one. Attribute the first planner/erasure loss
  precisely; do not revive body662 by inference.
- **D.** Downstream demand and actual supply cannot be represented independently.
  Name the missing role/edge representation.

## Deliverable and the HS9 gate

A report plus a scratch diff, source/CLIF/address maps, runtime logs, an evidence
manifest, and digests — restored byte-clean — handed to the Architect, who alone
reviews it and rules on the production design. **Routing A is the first
production-enabling outcome. Any result other than A is HARD STOP 9: before the
Architect selects another technique or a production repair, it obtains the
mandatory Research prior-art advisory on the exact reported seam.** Runtime
production stays blocked until this successor is ruled.

## Symptom inventory — the static-fact-as-runtime-authority defect

Added on the Architect's HS9 ruling (`evt_2k0rk8nwpn97c`). Every stop in this
chain instances ONE governing predicate: **a valid static or local fact is
repeatedly placed where the consuming dynamic value or source-control choice
does not coexist.** The inventory is the running record of the exact form each
stop took, so a successor does not re-place the same fact under a new name.

1. **force-D1 NO** (`evt_7e6jprw80srj8`) — a forced-reduction static fact where
   the dynamic reduction did not select it.
2. **carried-worker outcome 4** (`evt_5tm8gbxs7584g`) — a carried-worker binding
   present statically, not the worker the dynamic dispatch used.
3. **Host-Vis outcome 3** (`evt_6rvrzpg80c02j`) — a Host/`Vis` response
   association valid in the type, absent on the live control edge.
4. **capsule feasibility outcome 2** (`evt_1zpgs2h0kd74q`) — a capsule endpoint
   representable statically, not reached by the live request boundary.
5. **staged-call static outcome 1** (`evt_6qkn80ag2tq89` / `evt_4maaydhem9esy`)
   — a staged call declared, never emitted on the selected route.
6. **runtime-reachability outcome 1** (`evt_5ke73vgnrn` / `evt_7q7w05ag61zpw`) —
   a runtime-reachable body that the native path does not enter.
7. **vis-response outcome C** (`evt_3hcsstqp13rxy`) — the `Vis e k` binds the
   ordinary continuation Function; the capsule pairings 146→body662 / 159→body888
   are off-chain Match alternatives at zero, so environment transport (not
   execution control) was mistaken for the live response.
8. **live-k-inner outcome D** (`evt_5kqhv68j9bxgb`) — body662/body888 carry
   target metadata but no source `Call` ever applies the environment as an
   argument; the affine executor route is closed. Static target metadata placed
   where no dynamic application consumes it.
9. **ordinary-result-supply write non-A** (`evt_2k0rk8nwpn97c`, this node) —
   `ordinary_d0_control_closure` restarts its may-reach traversal at each
   producer's own `function_body`, converting "reachable if this Function is
   entered" into apparent live supply. Producers507/720/940 are all locally
   reachable and statically emit `ResourceBodyOk`, yet every native producer
   marker is zero while app486's environment reaches Match464. **The structural
   closure is now binding: no producer census rooted at its own Function body is
   execution authority.** Every claimed semantic supply must include, in order, a
   dynamically selected emitted Function/control predecessor, the exact completed
   producer value and its first owner, and the exact emitted argument/use that
   reaches the demand. Constructor-family membership and local dominance are
   insufficient.
