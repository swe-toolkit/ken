# WP frame — `DOC-CAT-PROPERTY-LAWS-AS-BUILT`

Bounded as-built correction following the Librarian's post-landing
source-attestation finding for `CAT-PROPERTY-LAWS`.

## Objective

Correct the Property reference's public-surface/law description, its
`none-declared` law row, and matching declarations/laws aggregations for the
current private attached law surface. Correct the three learning passages so
concrete witnesses remain computations while private checked laws state runner
behavior; retain the formal tagged `tested` construct as unavailable.

## Scope and acceptance

- Modify only the named Property reference, catalog declarations/laws rows,
  reading-assurance passage, exercise question/solution, and this frame.
- Ground additions in current source blob
  `953e192fc7430f7d8e97697f8df51831fdbaa08f`: no public surface; private laws
  for successful lookup soundness/completeness, first-counterexample recovery,
  and two `gen_from_list` coherences.
- Preserve correct fragments/provenance/assurance/effects claims. Do not amend
  landed candidate, source, or the pre-existing source-ledger drift; carry the
  latter to terminal release-point re-stamp.
- Librarian exact-SHA as-built review is the sole gate.
