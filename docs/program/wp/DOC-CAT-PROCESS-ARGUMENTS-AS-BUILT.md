# WP frame — `DOC-CAT-PROCESS-ARGUMENTS-AS-BUILT`

Bounded as-built correction following the Librarian's post-landing
source-attestation finding for `CAT-PROCESS-ARGUMENTS`.

## Objective

Update the derived Arguments reference and matching laws row from the prior
`round_trip`-only description to the current public attached proof surface for
`argument_slice_location`: `soundness`, `completeness`, and its explicit
missing-argument, unordered-endpoint, and out-of-bounds refusals. Carry the
current source and affected derived pages through the terminal release-point
re-stamp.

## Scope and acceptance

- Modify only the Arguments reference, corresponding laws row, required
  attestation/re-stamp artifacts, and this frame.
- Ground every addition in source blob
  `27684d5c7aca3a5c0882976442c9d5ef8e5da711`; retain the existing no-trust-
  delta/effect-boundary statements only if re-verified.
- Do not amend the prior landed candidate, make a ledger-only edit, alter source,
  or add `proc`/`visits` claims.
- Librarian exact-SHA as-built review is the sole gate.
