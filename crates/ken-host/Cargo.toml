[package]
name = "ken-host"
description = "Ken's audited Linux host-ABI policy boundary."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
rustix = { version = "=1.1.4", default-features = false, features = ["std", "fs", "process", "try_close"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { version = "=0.2.186", default-features = false }

[build-dependencies]
cc = "=1.2.41"
linux-raw-sys = { version = "=0.12.1", default-features = false, features = ["std", "general", "errno"] }
sha2 = "=0.10.9"

[dev-dependencies]
linux-raw-sys = { version = "=0.12.1", default-features = false, features = ["std", "general"] }
sha2 = "=0.10.9"
