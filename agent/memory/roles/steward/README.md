# roles/steward — Steward-specific lessons

Loaded by the Steward, in addition to `fleet` and `enclave`. Fleet coordination,
watchdog, worktree, and infra lessons specific to the operator's primary proxy
into the federation.

| Lesson | One-line |
|---|---|
| [a-blocking-verdict-short-circuits-the-remaining-acceptance-claims](a-blocking-verdict-short-circuits-the-remaining-acceptance-claims.md) | QA blocked early and never reached the handoff's "expected retained true-base failure" claim, so it was owed by NOBODY and rode into the next round as settled. MEASURED OUTCOME: at the zero-WP merge-base the control PASSES — the claim was false and the WP owned the regression. And the anchor is where the check fails: the candidate's parent AND a "preservation-only" seam (+16451/-3845) both already contain the change. Use `origin/main` or the merge-base, and measure the anchor's diff before trusting it |
| [a-ctx-scan-greps-your-own-report-of-another-seats-ctx](a-ctx-scan-greps-your-own-report-of-another-seats-ctx.md) | The mandatory watchdog ctx scan reported a LEADER at 88% and it was MY OWN SENTENCE — I had posted "ctx 88% to 0%" about the implementer, and the leader's pane echoed it while relaying. The text most likely to contain a `ctx N%` string is a Steward report about some seat's ctx, landing in the pane of whichever seat was told. No window fixes it: `-S -6` misses real footers, `-S -200` buys the false positive, `-S -15` still catches it. Discriminate by HARNESS — Codex (`gpt-*`) panes never emit `ctx N%`, so any match there is quoted text by construction |
| [a-durability-claim-in-an-artifact-was-never-checked-against-origin](a-durability-claim-in-an-artifact-was-never-checked-against-origin.md) | Four nodes asserted work was "preserved" and nothing had ever resolved the ref — including the LIVE BASE of the in-flight candidate, on one local branch. Resolve every ref as you write it; a cited SHA needs a durable ref only when an artifact says RESUME from it; divergent seams do NOT nest; and ancestry cannot tell a superseded branch from one holding unlanded work, because the publisher squashes |
| [fetching-during-the-publisher-window-freezes-the-NEXT-publish](fetching-during-the-publisher-window-freezes-the-NEXT-publish.md) | Racing the publisher's post-merge fetch does not just produce a bad read — it writes `.git/ken-publisher-FROZEN`, which blocks every SUBSEQUENT publish and is SHARED across all worktrees. The damage lands on the next unrelated publish and names the previous PR, so it reads as a failure of the one you just launched. The verify instinct is what trips it: verify only AFTER the background task reports completed, never off a sleep. Clear by hand only after `git diff <published-sha> origin/main -- <paths>` is empty and prior-work markers survive |
| [a-stranded-delivery-is-corroborated-by-the-recipients-own-status](a-stranded-delivery-is-corroborated-by-the-recipients-own-status.md) | A review request sat unsubmitted in the Architect's composer while its own status read "awaiting Architect review request" — the status was true when set, and the strand is what stops it being updated, so the stalled seat emits a status that AGREES with the stall. The convo log proves a message was sent, never that the model received it; only the pane sees the gap, and a clean `steward-pane-sweep.sh` did not catch this one. Probe with `zz` (appends = real strand, replaces = placeholder), BSpace it off, Enter, then verify `Working` plus a true empty-composer placeholder |
| [a-seat-can-stop-receiving-deliveries-with-a-clean-composer](a-seat-can-stop-receiving-deliveries-with-a-clean-composer.md) | A seat can go fully unresponsive with a CLEAN composer — no stranded paste, so a bare `Enter` is a no-op; diagnose by a FROZEN finished-turn counter while events arrive to it, repair with a typed `tmux send-keys` prompt naming the missed event ids |
| [a-queued-delivery-waits-on-a-next-tool-call-an-idle-seat-will-never-make](a-queued-delivery-waits-on-a-next-tool-call-an-idle-seat-will-never-make.md) | THIRD stranding shape: the pane shows "Messages to be submitted after next tool call" while the seat sits IDLE at the prompt, so the delivery condition can never fire. It self-reports as healthy in BOTH directions at once — the queue looks pending, the seat looks correctly event-driven — and the convo log is complete, so only the pane sees it. `Enter` is a no-op; clear with `Escape`, and ONLY when there is no `Working` footer, since Esc interrupts a live turn. A sweep for the marker string matches your own pane |
| [a-working-idle-grep-keyed-on-one-harnesses-footer-reports-false-idle-on-the-other](a-working-idle-grep-keyed-on-one-harnesses-footer-reports-false-idle-on-the-other.md) | The working/idle discriminator is HARNESS-SPECIFIC: grepping for `esc to interrupt` reports FALSE IDLE on a Claude Code seat mid-tool-call, which renders a spinner plus an elapsed timer instead — it nearly drew a nudge onto a seat running a targeted `ken-cargo` test. Codex also prints `Worked for Nm` AFTER a turn ends, so a `Work` grep gets it backwards. Match several alternatives; the elapsed timer generalizes across both. Failure direction is toward ACTION, so the recovery is what does the damage |
| [closing-a-node-strands-sequencing-prose-that-no-edge-check-sees](closing-a-node-strands-sequencing-prose-that-no-edge-check-sees.md) | Closing a node strands sequencing PROSE in other frames, and a clean `depends_on`/`blocks` edge check cannot see it — the edges were correct and the surviving sentences still told a ring to sequence against a node that no longer exists. Caught on RT-CONTSRC-CALLABLE-CONTRACT hours after RT-UNIT-CLOSURE-CONVERT closed with a green edge check. Grep the closed id across `docs/program/`, not just the edge fields |
| [a-node-can-be-fully-framed-inside-its-own-issue-file](a-node-can-be-fully-framed-inside-its-own-issue-file.md) | `test -f docs/program/wp/<ID>.md` is NOT the §4e frame check — a node can carry deliverables, ACs, scope and contention inside `docs/program/issues/<ID>.md` with no `wp/` file at all. Two false negatives (2026-08-08, 2026-08-12), the second on a node whose own head block records the first. A frame is a set of PROPERTIES, not a location; grep the headings. Both orphan directions cost differently — false-unframed wastes a turn, false-framed idles a ring. A correction living only in the artifact it corrects teaches nothing |
| [a-node-status-can-be-right-about-the-edge-and-wrong-about-the-seat](a-node-status-can-be-right-about-the-edge-and-wrong-about-the-seat.md) | `KERNEL-NESTED-IND blocked by RT-DYNAMIC-ARM-SCALAR-MERGE (status: active)` — the EDGE was true and the ENDPOINT'S STATUS was stale, so an idle Kernel ring read as unblocking. An edge-currency sweep passes straight over this because nothing about the edge is wrong. `active` is the ONE status that is a claim about SOMEONE ELSE, so it goes stale with no file touched: run the `active` set against the SEATS, not the frames — three of four had none. Only flip the ones where another status is fully true; two blocked nodes with landed partials have no legend state, and forcing them trades one lie for another. Lead the correction with what is still landed so it does not read as a rollback |
| [an-atomic-sibling-node-needs-active-not-a-dependency-edge](an-atomic-sibling-node-needs-active-not-a-dependency-edge.md) | Two nodes building as ONE candidate on one branch: one was `active`, the other `ready` + `depends_on: []`, so `gen-progress.sh` listed an IN-FLIGHT node on the RELEASABLE FRONTIER as unassigned work. The fix is `active` on every member, NOT a dependency edge — an edge encodes *after*, and these are siblings in one atomic set |
| [anchor-an-armed-trigger-on-an-event-not-on-an-index](anchor-an-armed-trigger-on-an-event-not-on-an-index.md) | An index-shaped trigger ("next pull `#30`") is not late when an append is missed — it is UNEVALUABLE, and reads as a confident "not due". The same block failed identically twice. Key it to an event id ("the 3rd hard stop after `evt_…`"), and never put two counters over different populations in one line |
| [an-out-of-band-merge-leaves-your-branch-on-a-reverting-base](an-out-of-band-merge-leaves-your-branch-on-a-reverting-base.md) | A merge you did not perform moves `main` with no signal your watchdog sees — `git merge-base --is-ancestor origin/main HEAD` is the test, and the deleted-path control must probe the MERGE RESULT (`git merge-tree`), not your tree, or it false-alarms on every candidate whose base predates a deletion |
| [adversary-is-not-a-per-candidate-merge-gate](adversary-is-not-a-per-candidate-merge-gate.md) | There is no "Adversary §10a" merge gate — the code-candidate gate set is QA + CV + Architect, and the adversary is NOT a fourth approval. Asking it to hunt a candidate violates the report-only edge (`COORDINATION §10⁻a`) and is the exact token sink that law prevents: the only sanctioned adversary compaction is the M8 merge notification (`§15`), so per-candidate and per-respin hunts have no compaction seam and accumulate in one context. Measured 2026-08-24 — three stacked hunts on one Component B partial before the operator ruled. The Steward's only outbound traffic to the adversary is that M8 notification |
| [atomic-capstone-freeze-excludes-concurrent-work-on-frozen-artifact](atomic-capstone-freeze-excludes-concurrent-work-on-frozen-artifact.md) | An atomic whole-artifact freeze (e.g. a catalog reformat capstone) is EXCLUSIVE over that artifact — hold every lane whose builds land in it until the freeze lifts, even if the lane is otherwise independent and unblocked; the deciding axis is where the lane's output lands, not where its logic lives |
| [audit-a-detector-against-the-case-whose-answer-you-already-know](audit-a-detector-against-the-case-whose-answer-you-already-know.md) | "Have I enumerated every case?" is unfalsifiable; find one input whose true answer you know independently and assert on it every run — give every detector arm a positive AND negative control, and run the self-test from a seat that is not the author's |
| [awk-length-miscounts-multibyte-wrap-check](awk-length-miscounts-multibyte-wrap-check.md) | `awk length()` byte/char-counts a line, which over-counts multi-byte UTF-8 (`→`, `Ω`, `—`) against the 80-display-column wrap rule — delegate to the `wrap-md-80` skill or count codepoints, never raw byte length |
| [bash-cd-main-repo-vs-steward-worktree](bash-cd-main-repo-vs-steward-worktree.md) | Bash `cd /workspaces/ken` targets the main repo, not the steward worktree |
| [brief-settled-input-enabler-must-be-probed](brief-settled-input-enabler-must-be-probed.md) | A WP brief's "settled input" that asserts an elaborator/toolchain enabler must be empirically PROBED on the landed code, not inferred from a spec clause + a code read, before it is pinned as decided |
| [bundled-frame-doc-goes-stale-when-mechanism-flips](bundled-frame-doc-goes-stale-when-mechanism-flips.md) | A bundled frame doc goes stale when a WP's mechanism flips mid-build |
| [committed-is-not-reachable-publish-then-verify-on-main](committed-is-not-reachable-publish-then-verify-on-main.md) | A Steward doc/corpus edit is NOT done at `git commit` — build rings read `main`, so an edit on `steward/work` reaches nobody. Done = the phrase greps on `origin/main`, by content, never by SHA or exit code |
| [dont-publish-derived-code-measurements-state-the-property-let-the-ring-measure](dont-publish-derived-code-measurements-state-the-property-let-the-ring-measure.md) | State the property in the brief and let the ring produce the derived number — a hand-derived split (e.g. "of which N production") smuggles an unstated convention; a whole-file count under one convention at both ends is fine, the gloss on top is the defect |
| [entrypoint-abi-change-is-never-corpus-disjoint](entrypoint-abi-change-is-never-corpus-disjoint.md) | An entrypoint-ABI change rewrites every example's `main` — grep the touch-set before calling a lane catalog-disjoint |
| [exported-name-migration-needs-whole-harness-consumer-inventory](exported-name-migration-needs-whole-harness-consumer-inventory.md) | A WP that renames/retires EXPORTED names must inventory the whole consumer harness — test assertions that pin the old names by string — not just verify producer-side fidelity; frame the coupled-consumer grep INTO scope up front |
| [dont-gate-pipeline-fill-on-credit-cost](dont-gate-pipeline-fill-on-credit-cost.md) | Spend the weekly token window — idle capacity is the waste; keep every unit busy, parallelize aggressively |
| [fleet-model-rollout-stagger-restarts](fleet-model-rollout-stagger-restarts.md) | Stagger restarts when rolling the fleet onto a new model |
| [frame-must-ground-substrate-obligations-not-just-names](frame-must-ground-substrate-obligations-not-just-names.md) | A frame pinning an abstraction over landed types must ground the SUBSTRATE, not just check the names exist — the instance lives with the CARRIER it is over, never with the abstraction, or the frame ships a dependency cycle. Cosmetic symmetry is what creates it |
| [llm-proxy-is-build-tier-only-anthropic-runs-direct](llm-proxy-is-build-tier-only-anthropic-runs-direct.md) | All agents run Anthropic models direct; the llm-proxy is retired |
| [migration-sweep-glob-must-enumerate-every-ken-source-root](migration-sweep-glob-must-enumerate-every-ken-source-root.md) | A corpus-wide surface migration (glyph/keyword/name) must enumerate EVERY Ken-source root — `examples/rosetta`, `tooling/highlight-js` samples, playbook `.md` fences — not just catalog+crates+tests; a glob covering the obvious roots silently misses the rest |
| [my-own-tracker-capability-landed-line-can-be-stale](my-own-tracker-capability-landed-line-can-be-stale.md) | Your own tracker's 'capability X landed' line can be stale |
| [orphan-watchdog-timer-record-id](orphan-watchdog-timer-record-id.md) | Watchdogs use a private CronCreate timer, not the convo schedule_call |
| [playbooks-state-mechanism-not-intent](playbooks-state-mechanism-not-intent.md) | Playbooks must state the mechanism explicitly, not just the intent |
| [consecutive-publishes-need-fetch-prune-branch-is-deleted-on-merge](consecutive-publishes-need-fetch-prune-branch-is-deleted-on-merge.md) | The publisher DELETES the head branch on merge — the next push fails "stale info"; `fetch --prune`, then re-anchor onto main by reset + cherry-pick (squash trap), never force, never rebase |
| [publisher-flags-are-description-not-body-and-failure-is-silent](publisher-flags-are-description-not-body-and-failure-is-silent.md) | There is no `--body`/`--body-file` on the wrapper: it dies `unknown argument` and the wrapping task STILL reports exit 0 — and a `nohup … &` launch's own exit-0 is the SHELL's, not the script's. Verify on `origin/main` BY CONTENT — never by SHA, task status, or exit code — and pick a discriminator the WP actually ADDS, not one it merely touches |
| [representative-file-review-only-covers-constructs-that-file-has](representative-file-review-only-covers-constructs-that-file-has.md) | A one-file tool review certifies only that file's constructs; spot-check distinct constructs before a catalog sweep arms a gate |
| [splay-gate-only-as-good-as-its-detector-verify-the-check](splay-gate-only-as-good-as-its-detector-verify-the-check.md) | A style spot-check is worthless unless the detector provably fires on a known-bad and targets the over-width failure mode; awk `\s` is non-portable — use `[[:space:]]`; prefer an automated CI gate |
| [stale-base-candidate-silently-reverts-everything-landed-since](stale-base-candidate-silently-reverts-everything-landed-since.md) | Title overstates it (see the out-of-band-merge lesson for the correction) — a squash-merge applies merge-base→branch, so a stale base does NOT silently revert untouched files; the real hazard is confined to the file INTERSECTION with what `main` moved, and the definitive check is `git merge-tree --write-tree` plus a predicted post-condition |
| [steward-coldstart-infra-checks](steward-coldstart-infra-checks.md) | On a Steward cold-start, check fleet-wide infra before concluding stalled |
| [steward-must-relay-publisher-merges](steward-must-relay-publisher-merges.md) | The Steward must relay publisher-path merges |
| [steward-work-is-stale-immediately-after-every-publish](steward-work-is-stale-immediately-after-every-publish.md) | `steward/work` is stale the instant a publish merges — the publisher squash-merges AND auto-deletes the head branch, so the next commit needs `git fetch --prune` + `git reset --hard origin/main` before anything else |
| [the-summary-generalizes-past-the-measurement-the-artifact-stated](the-summary-generalizes-past-the-measurement-the-artifact-stated.md) | Restating your own accurate artifact is where scope creep enters — quote the narrow sentence, name the operand ("deleting the RULE" vs "the INVOCATION"), and claim only the mutations you ran |
| [the-gate-that-protects-a-ring-is-the-instrument-that-deletes-a-standing-seat](the-gate-that-protects-a-ring-is-the-instrument-that-deletes-a-standing-seat.md) | A standing seat (adversary, architect, research, librarian) has no ring, so nothing publishes its branch and no gate ever reaches it — its ctx grows unbounded, and the gate you finally run to fix that is the thing that deletes its unpublished work. Found at ctx 94% with 69 commits. Publish first, then gate; the script's `preserved/` ref guards against loss but reaches no reader. After a squash the source branch reads unlanded forever — prove it by blob identity, never ancestry, and tell the seat where its commits went |
| [the-publish-description-is-the-one-artifact-no-reviewer-reviews](the-publish-description-is-the-one-artifact-no-reviewer-reviews.md) | Your PR description becomes the permanent squash commit message and is authored AFTER the last gate — nobody reviews it. Every verification-flavored clause ("with a positive control") is a factual claim about the diff: grep for it. State what the mechanism PINS, not what it evokes |
| [a-nodes-status-is-a-claim-about-a-node-not-evidence-about-the-tree](a-nodes-status-is-a-claim-about-a-node-not-evidence-about-the-tree.md) | Nothing flips a node to `merged` when its PR lands, so `status: ready` is equally consistent with never-started and shipped-and-forgotten — kicked Language onto work that had merged hours earlier. The COMPOUNDING is the cost: every claim the kick derived from the frame went stale with it while each read as freshly measured, including a fixture warning the same PR had already repaired. Check the TREE before any kick (grep the node id against merged PR titles); the one-call corpus sweep is cheap. Sibling of [[a-node-status-can-be-right-about-the-edge-and-wrong-about-the-seat]] — that one is status-vs-SEAT, this is status-vs-TREE |
