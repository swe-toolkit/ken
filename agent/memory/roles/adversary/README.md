# Adversary role memory scope

Lessons specific to the **Adversary** seat — the standing red-team that hunts
recent changes + their blast radius for flaws, gaps, leaky abstractions, and
undesirable behavior (see `agent/playbooks/federation/adversary.md`).

Read this scope plus `fleet/` and `enclave/` (the Adversary hunts
soundness-adjacent surfaces and reads references under the clean-room recheck, so
it sits on the enclave-facing edge). Record a lesson here when it is
Adversary-specific: a failure *shape* worth reusing across changes, a
false-alarm pattern to stop re-filing, a blast-radius-scoping trap, a
grounding/repro technique. A genuinely cross-cutting lesson belongs at the
broadest scope where every reader must apply it (much of the fleet corpus's
`verify-the-mechanism-not-a-proxy` family is exactly this), not here.

| Lesson | One-line |
|---|---|
| [a-narrowed-check-lands-on-the-node-not-on-the-value-that-reaches-it](a-narrowed-check-lands-on-the-node-not-on-the-value-that-reaches-it.md) | An over-broad check corrected to the right POSITION ends up keyed on that position's own node rather than on what can flow into it, so one transparent wrapper (`If`, `Let`) defeats it — attack every narrowing by wrapping the excluded thing in a transparent parent |
| [auditing-conformance-silently-ratifies-the-artifact](auditing-conformance-silently-ratifies-the-artifact.md) | A conformance audit is scoped strictly *below* the artifact's existence — I checked `test_support.rs` against every rule governing its contents while the Architect asked whether it should exist at all (it shouldn't: LCA is computed from final ruled homes, never transient mid-split locations); check the derivation's INPUTS, and treat "current location" as untrusted during a migration |
| [a-repro-is-evidence-not-a-completion-oracle](a-repro-is-evidence-not-a-completion-oracle.md) | A repro answers *does this defect exist?* and is discharged once believed; a completion oracle answers *what would make this correct?* and binds until the fix merges — file the repro as evidence, let the ring build its own oracle, and leave the AC a weakening clause |
| [no-option-works-name-the-axis-you-enumerated](no-option-works-name-the-axis-you-enumerated.md) | I listed three escapes from a ruling bind, showed each forbidden, and declared it unsatisfiable — all three varied ONE axis (production visibility) and the answer varied another (`cfg(test)`); name the dimension you enumerated over, or say "no option along axis X" rather than "no option exists" |
| [rank-subclaims-by-load-bearing-not-by-checkability](rank-subclaims-by-load-bearing-not-by-checkability.md) | Handed a claim to refute, I verified the tractable sub-claim and confirmed a false one whose disproof was one grep — rank by what the claim RESTS on, not by what is quick to check; and index a lesson by its SHAPE, not the venue that taught it, or it won't fire in new clothes |
| [the-post-merge-yield-is-vantage-not-seat-quality](the-post-merge-yield-is-vantage-not-seat-quality.md) | When a post-merge pass finds what a careful ring missed, the cause is the VANTAGE (reading without a candidate in front of you, asking what the mechanism is *for*) not the seat — correct credit framed as seat quality, or the lesson won't transfer |
| [verbatim-is-not-faithful-when-selection-is-wrong](verbatim-is-not-faithful-when-selection-is-wrong.md) | A relay quoted verbatim, attributed, and judgment-free still misled — it transcribed an earlier acknowledgment instead of the bound verdict below it; relay safeguards all govern TRANSFORMATION, none govern SELECTION, and a wrong selection is invisible downstream because the resolving evidence is exactly what was omitted. Suspect the selection before the author; "quoted accurately" ≠ "quoted completely" |
| [close-a-class-partition-the-declared-population](close-a-class-partition-the-declared-population.md) | To close a defect class, prefer the artifact's OWN declared enumeration (an explicit import list beat my inferred 75-item grep closure) and partition it on a property the rule is silent about — splitting 70 owned declarations by item kind found the 2 `const`s as the cell §10.2's type/helper/entrypoint families cannot name; searching for "more like the first" is shaped by the example and closes nothing |
| [forecasting-a-merge-is-not-evidence-about-it](forecasting-a-merge-is-not-evidence-about-it.md) | I called a pending rebase "trivial, the modified lines are disjoint" and it conflicted — my window was the file *as it existed*, my claim was about the file *as it would exist*, and the in-flight side landed into that block; path-set disjointness is structural and safe to state, line-disjointness inside a shared block is a snapshot claim that is never a prediction |
| [an-attestation-is-only-as-durable-as-its-ref](an-attestation-is-only-as-durable-as-its-ref.md) | I surfaced "commit your verdict to your own branch" as the outbound path for a seat whose convo client was dead, verified it carried the CONTENT independently of any relay — and never asked what the BRANCH was worth: the fleet's own handoff gate hard-resets it, and a `preserved/` ref proved it already had. Check the substrate, not just the payload; and confirming a rescue means listing the TREE at the tip, since ancestry survives a later deletion of the file |
| [a-conjunction-finding-gets-silently-decomposed](a-conjunction-finding-gets-silently-decomposed.md) | My finding was "neither layer catches it"; the AC written from it planted a test in ONE layer and would have gone green with the defect half-open. A finding operationalizes into a criterion keyed on whichever construct it named most vividly, and the conjunction evaporates because it is not a place in the code — say the conjunction aloud, then read the resulting AC as an adversary reads any gate |
| [a-disclosed-deferral-gets-guard-rails-written-for-the-whole](a-disclosed-deferral-gets-guard-rails-written-for-the-whole.md) | When work splits and one half is deliberately deferred, the artifacts AROUND the deferred half get authored as if the whole thing shipped — the normative doc claimed "both reifiers" while only one was fixed, the comment at the fix site argued the defect away (two senses of "narrow" colliding), and the only suite that could catch it is skipped in CI. Each independently REMOVES a reason to look. On any disclosed deferral audit that triple before the second half starts, and file it as an input, not a post-mortem |
| [pre-emptive-yield-is-at-the-gate-layer-not-the-code-layer](pre-emptive-yield-is-at-the-gate-layer-not-the-code-layer.md) | Filing into imminent work, I led with a code-level blind spot the implementer found independently in their own pre-work read, and buried the part only I could give: the AC was unsatisfiable, its classification vocabulary had no cell for "left in place", and its scope was defined by line range not cfg-condition. A careful seat finds code defects in the file they're about to open; nobody re-derives the GATE, because passing it *is* the definition of done — ask which items survive the assumption that the implementer is careful |
| [an-error-in-the-safe-direction-is-a-claim-about-what-you-did-not-measure](an-error-in-the-safe-direction-is-a-claim-about-what-you-did-not-measure.md) | I corrected a line-count as "cfg-conflated, and it errs in the SAFE direction — the real surface is smaller still". It was larger: `:442-492` were ungated production `impl` blocks I never opened. A direction is a COMPARISON and needs the far end measured; reporting the near end and inferring the sign is a different, unsupported claim. Worse, "errs safe" is maximally disarming — it tells the reader not to look — and mine travelled as *support for someone else's correction*. A reassurance is a finding with the falsifiability removed; say the sign is UNKNOWN, not SAFE |
| [a-clean-parallel-result-must-be-withheld-until-the-other-seat-reports](a-clean-parallel-result-must-be-withheld-until-the-other-seat-reports.md) | Attacking a candidate in parallel with its gates is sanctioned; posting a *clean* parallel result before the gate reports is not — it hands the verifier your conclusion and their later agreement carries no information. Timing, not content, is the contamination vector, and it is invisible precisely when your result is correct and your intent is to help. Found nothing ⇒ early disclosure is pure downside; hold, then post as corroboration |
| [hunt-the-correction-it-inherits-the-defect-class](hunt-the-correction-it-inherits-the-defect-class.md) | A fix to a false claim is a high-yield target, not a closed item — same author, same topic, same reassuring register, less scrutiny because everyone is relieved. The §14(5) law text landed correct while the prose announcing it carried a NEW false mechanism, and it was the prose that reached two rings as instruction. Read artifact and announcement separately; grep the correction for stand-down clauses; measure instead of arguing; lead with what is right so a narrowing doesn't invite the opposite over-correction |
| [an-incident-offered-as-corroboration-must-reproduce-your-mechanism](an-incident-offered-as-corroboration-must-reproduce-your-mechanism.md) | A seat reported my declined finding had "a live instance, and it is mine" — real data loss, wrong mechanism: git CONFLICTS loudly on those branches, and the reported row count equalled one side's own count, so no merge occurred at all (a wholesale blob take). **A merge result equal to one side's own count means no merge happened.** Corroboration arriving as credit is the hardest evidence to audit; make the incident reproduce your mechanism, not merely resemble it, and prefer post-conditions on the artifact over guards keyed on a mechanism story |
| [audit-a-detector-against-the-one-case-whose-answer-you-already-know](audit-a-detector-against-the-one-case-whose-answer-you-already-know.md) | The stall watchdog had missed four times, always a false IDLE; I reproduced a fifth on the pane whose state is certain — my own — while running the command that produced its output. The `❯` prompt box renders WHILE BUSY (a constant, not a signal) and the spinner sits ~7 lines up, so a `tail -4` window misses it **no matter how many arms the disjunction has**. ⇒ The missing thing was not arms but an **oracle**: a watchdog's own pane is BUSY by definition, so a detector calling it IDLE is falsified every cycle at zero cost. When auditing any classifier, first ask which instance you know the answer to *without* consulting it |
| [run-it-from-a-seat-that-is-not-the-authors](run-it-from-a-seat-that-is-not-the-authors.md) | A self-test oracle whose pane identity came from `${MOOT_ROLE:-steward}` was correct for the author and wrong for every other seat — so authoring, a both-directions falsification, review and CI all passed it, all standing in the same vantage. From my seat a fully broken detector passed its own self-test and reported confident verdicts. **A construct correct from the authoring vantage cannot be caught by the author's rigor, because that rigor is applied through the vantage that hides it.** Run the artifact from your own seat; grep every default for "whose value makes this true?"; and attack the plumbing, since scrutiny concentrates on the subject of review while defects concentrate in what was merely infrastructure to it |
| [hunt-the-stand-down-clause-it-lives-in-prose-no-gate-reads](hunt-the-stand-down-clause-it-lives-in-prose-no-gate-reads.md) | A rule whose function is to tell a reader *not to look*, justified by something with no bearing on whether anything is there — three in one session, one of them mine. Highest-yield target this seat has, because they live in **messages, not artifacts**: no CI job reads a kickoff and no gate fails on a sentence, so a correct artifact can ship with wrong prose carrying it. The tell is an instruction that **pre-classifies a future observation** (*"if you see X it isn't real"*) — a prediction about evidence that doesn't exist yet. Hunt them *before* the signal arrives, since afterwards the suppression already worked and generated no evidence |
| [audit-the-operative-line-not-the-stated-principle](audit-the-operative-line-not-the-stated-principle.md) | **evidence corrected in-file** — the gap was an ABSENCE, not a contradiction; I read two senses of `re-run` (test *scope* vs probe *provenance*) as a conflict. A playbook asserted QA independence twice in its role framing and was then **silent on** how to derive a mutation — and **re-running is not re-deriving**: an inherited mutation carries the implementer's blind forms, which for a text mechanism is the whole failure surface. The stated principle is not evidence the instructions implement it, and the failure leaves no trace (an inherited proof is indistinguishable in the verdict from a derived one). Grep a process doc for its **verbs**, not its nouns; and prefer a disclosure clause to a prohibition — *if you can only re-run theirs, say so* |
| [preventive-findings-are-unfalsifiable-so-keep-them-cheap](preventive-findings-are-unfalsifiable-so-keep-them-cheap.md) | A finding that prevents a defect has **no observable payoff** — no red CI that didn't happen, no suppressed signal to point at — so neither seat can audit it afterwards. Left alone that ends in the Steward either trusting blindly (seat unaccountable) or discounting (preventive findings lost first, and they are the highest-value thing this seat makes). ⇒ **Cost is the observable proxy: a preventive finding must be CHEAP to act on, and an expensive one gets weighted LOWER, not higher.** Agreed with the Steward and binding — a future adversary should expect it and not read it as distrust |
| [differential-oracle-is-blind-to-a-shared-premise](differential-oracle-is-blind-to-a-shared-premise.md) | A differential/parity test is a RELATIVE oracle — structurally incapable of catching a defect BOTH sides share. Six interp↔native differentials passed green while both sides derived `TransferCount.remaining` from the raw request instead of the host's clamped `effective` value, tracing to the spec's own inconsistent restatement of the bound. On any parity gate, read both formulas and check the agreed value against the spec, not against each other |
| [disable-the-mutation-mechanism-to-find-vacuous-controls](disable-the-mutation-mechanism-to-find-vacuous-controls.md) | To find committed mutation controls green BY VACUITY, force every mutation read to `Exact` and run the suite — any control test that still passes is measuring nothing (13 enums neutralized reds 16 of 617, all control tests, so the shape was clean). Grepping for the `find(x != exact).unwrap_or(exact)` fallback idiom only finds CANDIDATES: a site can carry it and still be sound if the test asserts the mutation was OBSERVED to perturb. Caveat: this proves each DRIVER TEST is live, not each VARIANT. And never probe every mutation against ONE fixture — measure each against its own driver's |
| [a-validator-whose-expected-value-is-its-own-builder-re-run](a-validator-whose-expected-value-is-its-own-builder-re-run.md) | A gate that computes its expected value by RE-RUNNING the builder that produced the value it checks is self-oracled and cannot see a defect in that derivation. Establish it by closure — exactly one writer for the population, and the builder reads only state final before the assignment (check that specifically against whatever runs BETWEEN the two call sites, or you overstate). Measure with a defect INJECTED INTO THE BUILDER, never by deleting the gate: removing a correct gate always leaves a green suite. 114 of 611 reddened while the gate's own message fired ZERO times. The tell is a comment claiming the populations are "independently re-derived" |
| [a-ruling-that-widens-a-shared-map-names-only-the-consumer-it-was-about](a-ruling-that-widens-a-shared-map-names-only-the-consumer-it-was-about.md) | A ruling that resolves a spurious rejection by WIDENING a shared field silently widens every OTHER reader of that field, and the ruling names only the consumer that provoked it — the implementer re-derives the mechanism the ruling names, never the one it did not, because passing the ruling *is* done. The tell is a field with two readers and no doc comment, its contract stated only inside one reader's error string. Grep every reader of a widened population and ask what each one's rejection CLAIMS; and report the axis (the check's strength) not an alarm, since a widened check has no repro until a program reaches it |
| [line-closure-check-must-compare-whole-files-not-diff-hunks](line-closure-check-must-compare-whole-files-not-diff-hunks.md) | A removed-line closure check run over `git diff` hunks reported 1,339 unaccounted lines on a 7,400-line file split — git re-pairs retained code around big deletions, so hunks describe a minimal edit script, not what actually left the file. Compare whole-file content multisets on both sides instead; a closure residue that comes out implausibly large means suspect the oracle, not the change |

| [a-narrowed-check-lands-on-the-node-not-on-the-value-that-reaches-it](a-narrowed-check-lands-on-the-node-not-on-the-value-that-reaches-it.md) | An over-broad check corrected to the right POSITION lands keyed on that position's own node, not on what can flow into it — so `If { scrutinee: true, then: imported, else: imported }` passed an exclusion that refused a bare `imported`. A repair is shaped by the counter-example that motivated it, so it stops at the node the example sat on and skips the correct middle: *any occurrence whose value can REACH the position*. ⇒ Attack every correction by wrapping the excluded thing in a **binder-free** transparent parent, then enumerate the position's siblings (a frame has a capture slot AND a result slot — the second hole needed no wrapper). Bound the claim to the layer you measured |

| [a-derived-records-doc-names-a-population-its-producer-loop-does-not](a-derived-records-doc-names-a-population-its-producer-loop-does-not.md) | Derived metadata is documented by the **population** it summarizes (*"any negative use in the declaration"*) while its producer loop covers one sub-population — four positions in the declaration, one in the loop, so the identical term `A -> Bool` read `NonPositive` in a constructor arg and `StrictlyPositive` in the three unscanned ones. Producer and doc ship in one commit, which makes the loop look like the doc's definition rather than a subset. ⇒ Attack by putting the **identical term in every position the grammar allows** and diffing the readings, with the position whose answer you already know as a control; take the population from the type's own field list. File in the window between *derived* and *consumed* — the first consumer used `== StrictlyPositive` as the permissive branch of a safety gate. ⚠ A mutation proves a gap only for the population it added: mine flipped two rows and only one was attributable |

| [a-green-mutation-does-not-tell-you-which-blindness-let-it-through](a-green-mutation-does-not-tell-you-which-blindness-let-it-through.md) | I built a compensating swap to prove a census count was region-blind; it came back green and I nearly filed it. The **positive control** — an unbalanced 3→4 change that *must* redden — was green too: the file was never on the roster, so my evidence measured nothing. The hypothesis was true, but only on a different file. ⛔ Green is a DISJUNCTION (my blindness ∨ another ∨ the instrument not reaching here ∨ the probe never ran), and **the reasoning that picks the target also supplies the explanation**, so it collapses to the disjunct you already believed. Mirror of the redden-attribution problem: red needs attribution, green needs discrimination, and green is worse because there is no error text. ⇒ Ship an unbalanced control on the SAME target with every blindness probe; if it is also green you have found something bigger — re-site the experiment, do not re-argue it |

| [a-source-text-oracle-defect-is-non-actionable-so-report-the-broken-promise](a-source-text-oracle-defect-is-non-actionable-so-report-the-broken-promise.md) | Standing operator ruling: tests asserting facts about source/catalog/doc TEXT are disfavoured, so a measured defect in one buys **no repair** — my census finding was correct, mutation-proven, and spawned no node. ⛔ What survived was not "two pins over one roster disagree" (aesthetic) but "**the instrument does not do what its comment claims**" — its stated job was to make a future roster addition visible, and a single count over a mixed population cannot, because a compensating swap holds it constant. ⇒ Recognise the class early (`source.matches`, `include_str!`+`contains`, name inventories) and confirm it CHEAPLY; lead with the broken promise, since a non-actionable oracle is still cited as coverage. ★ When a finding lands in a ruled-non-actionable class, the finding rarely dies — its framing does; ask what a reader would do differently knowing it |

| [suppressing-a-row-can-empty-a-whole-named-gate](suppressing-a-row-can-empty-a-whole-named-gate.md) | An annotation campaign is verified against the AGGREGATE package (`0 FAILED`), which is structurally blind to what it did to any individual gate — a gate is a CI job with its own `--test <binary>` selector. Two touched binaries held exactly one `#[test]`; ignoring it left their named jobs selecting **zero tests** and reporting success, and a third lost the 6 of 7 rows that were the differential oracle. The workflow comment had already written down the harm ("ran nowhere — a green CI carried no information"), and the job existed to end that state. ⇒ Grep the CI config for every job whose selector names a touched file, count surviving live rows per selector, and read the survivor's body to see if it exercises the mechanism the job is NAMED for. Companion gap: if `--ignored` appears nowhere, the suppression is write-only — neither over-annotation nor a landed repair is ever observable |

| [a-node-that-closes-without-discharging-inverts-the-doc-that-named-it](a-node-that-closes-without-discharging-inverts-the-doc-that-named-it.md) | A doc passage that names the node it waits on, precisely so it "will not silently invert", inverts the moment that node CLOSES WITHOUT DISCHARGING — a reader checks the named node, finds it closed, and concludes the suppressed rows are re-armed. The anticipatory clause *"needs another pass when X lands"* assumes land ⇒ discharge, and the uncovered branch is the one where closure looks identical to success from outside. ⇒ On any node-closure notice, `git grep` the node id across `library/` and `docs/`; read the suppressed rows' own reason strings rather than the node's status. Here `crates/` restated honestly while `library/` went false — the two surfaces move independently. ⚠ A paragraph that needs the same correction twice is trying to track state that lives elsewhere; say that instead of supplying a third wording |

| [a-citation-that-does-not-resolve-needs-checking-against-every-tree](a-citation-that-does-not-resolve-needs-checking-against-every-tree.md) | Finding that a cited coordinate does not resolve at HEAD is half a diagnosis — the number is usually true SOMEWHERE, and which tree names the mechanism: true at the base means the citing change moved it (a stale operand, not a typo), true at neither means a mid-edit number from an uncommitted state, true at both means the file is wrong. A bare `:line` with no filename gets one attached by whoever reads it next, so the misattribution belongs to nobody — do not blame the renderer, I did and it was the wrong party. Two `git show <tree>:<file> | sed -n 'Np'` calls settle it, and the gap between "your citation is wrong" and "your citation is your own pre-edit tree" is the gap between a correction and a promotable rule. ⚠ The author re-derived my finding rather than accept it and got further; I stopped at the first tree that answered the question I asked |

| [a-carried-obligation-gated-on-a-merge-event-fires-on-an-accepted-partial](a-carried-obligation-gated-on-a-merge-event-fires-on-an-accepted-partial.md) | A deferred control gated on *"when node X lands"* assumes land implies capability; the uncovered branch is the **accepted partial**, where the named event fires and the capability does not arrive. Its `panic!` body correctly stopped a vacuous pass and was irrelevant — a fail-closed body and a wrong gate are independent surfaces, and I had cleared this very control one merge earlier by auditing only the body. Worse, the doc carried three coextensive phrasings and divergence **promoted the weakest to the labelled `Release condition:`**, which is what a reader hunting the gate reads first — so fix every occurrence, not the labelled one. ⇒ On every merge notification, `git grep <NODE-ID> -- crates/` for previously-landed carried obligations naming it; no single ring's vantage spans author-ring and release-event. Ancestry is worthless under squash-merge — measure content, and an **exact numstat inversion** proves the target sits at the pre-change state |

| [a-corrections-sweep-population-is-its-own-diff-scope](a-corrections-sweep-population-is-its-own-diff-scope.md) | An *"all five occurrences agree"* AC is evaluated against the population the fix could TOUCH — a comment-only one-path candidate has a diff scope of one file, so the sweep is bounded before anyone counts, and the **authority document sits in another directory, structurally outside it**. `D7` re-keyed a carried control from merge event to capability across all five `crates/` surfaces while the node's own `AC-5` row kept the phrasing its own diagnosis table (`:405`) records as **FALSE** — the document that refutes a predicate carries it in its operative row, because diagnosis and operative rows are written in different passes and only the diagnosis is proofread as a claim. Worse, `AC-D7-1` offered that refuted phrasing as a free choice, so the author's care is what made it pass and the AC's defect left no trace. ⇒ On any correction landing as a one-path candidate, grep the predicate's WORDS against the correcting document and hunt whatever declares itself the authority — **before its node closes**, which here was two minutes later. ⚠ My own sweep still missed one: the **coining site** (the frame that minted the phrase, both cells event-keyed) is a DIFFERENT target from the authority site, and it is what re-seeds the phrase into the next node — `git log -S'<phrase>'` for provenance, and name the population you enumerated rather than reporting a partial sweep as a clean catch |

| [a-pin-built-from-your-finding-inherits-your-enumeration](a-pin-built-from-your-finding-inherits-your-enumeration.md) | A ring turned my preventive finding into a 760-line pin that pins **exactly the three facts I named, against exactly the three manifests I named** — and the premise rests on a **fourth** (no sibling workspace member enabling the feature on a NORMAL `[dependencies]` edge; resolver 2 withholds unification for dev edges only, and CI builds `--workspace`). Nobody failed: the pin was faithful to the finding, and the finding was the specification. ⇒ **When your finding becomes a pin, its population is YOUR population**, so enumeration at finding-time is gate-authoring-grade and you are not told when the promotion happens. *"Rests on exactly N facts"* is a closure claim with no proof — take the population from the artifact's own declared list (`[workspace] members`) so it closes by construction. Both instruments were keyed on `ken-cli`'s graph (the companion builds `--bin ken`, where cross-member unification structurally cannot appear), so two greens were **one measurement**. `cargo tree -e features,no-dev` resolves WITHOUT building, so the whole A/B is affordable: baseline 0, one-token sibling mutation 1, pin green throughout |

| [a-capability-gate-operationalized-by-a-snapshot-is-still-event-keyed](a-capability-gate-operationalized-by-a-snapshot-is-still-event-keyed.md) | Re-keying a gate from *"when node X merges"* to *"when the capability is on `main`"* fixes the **predicate** and not the **gate**, if the capability's only operational test is a path-state snapshot — a snapshot is event-shaped, so the next accepted partial moved all four cited paths and the doc's own test flipped to **GO**, which is verbatim the harm that same paragraph predicts. Ask **which way** a snapshot fails: *"the paths have NOT moved"* means any movement reads as arrival, and movement is exactly what a partial produces. The tell is **present tense doing past-tense work** (*"each remain at their pre-change state"*) — defensible as history, unusable as a current test, so report *"the artifact supplies no current test"* rather than calling it false. ⇒ At the **third** correction to one paragraph, stop rewording: make the condition decidable by execution, or let the owning node carry the gate. And on every merge, `git log <merge> -- <paths some other artifact cites as evidence of a state>` |

| [a-retracted-number-survives-in-its-consumers-with-the-direction-flipped](a-retracted-number-survives-in-its-consumers-with-the-direction-flipped.md) | Retracting a derived quantity **where it was produced** does not retract it. A withdrawn `14 → 15` census — already promoted to a closure check, already having blocked a correct candidate — was fixed at the producing node and kept by two consumers, and a consumer **restates a number in its own frame** (a delta becomes a predicted end-state), so the inherited copy was wrong in a *different* direction: the successor node predicted `14 → 15` where landing it actually gives `14 → 13`. ⇒ The **successor / "what this unblocks" node is the predictable inheritor** — grep it first, it must state what its own landing does. Separately, a quantity that was promoted to a gate leaves **imperatives** behind (*"a candidate reporting 14 has not done the recut"* — an instruction to reject the CORRECT value, which generates no evidence when it fires); hunt those, not just the restatements. And name the population: `19 → 14` and `20 → 15` were both correct |

| [attack-what-the-mechanical-half-structurally-cannot-see](attack-what-the-mechanical-half-structurally-cannot-see.md) | A report pairing a **mechanical control** with a **prose enumeration** can miss the same surface in both, and each half reviewed alone reads complete. `trusted_base()` iterates `self.decls`, so the support authority (`GlobalEnv` **struct fields and methods**, not `Decl`s) is unobservable by any outcome of that control — and the prose omitted its whole file. ⇒ **Procedure:** write down the control's observable set, compute the complement inside the change's surface, and check the prose against *exactly that* — the author's attention followed the instrument. Make the omission measurable with `git log -S` on the enumerated symbol vs the omitted ones: same merge ⇒ a gap, not a scoping decision. Lead with the **authority relation** (a wrong check is loud, a wrong authority silent), especially when an AC against it is already open. And **confirm the invited row out loud** — on a report-only channel, silence is indistinguishable from not having looked |

| [attack-an-impossibility-claim-at-module-scope-not-only-the-signature](attack-an-impossibility-claim-at-module-scope-not-only-the-signature.md) | A narrowed signature closes the **IN-edge only** — never what the body already reaches. Test four axes: parameters (read the **struct definition**, not the type name), body (spelling literals / name-keyed `find`), delegation to a wider-typed sibling, and **module scope** (statics, `use`d registries, free `fn(name) -> Id`). The last is the row a signature argument cannot cover by construction and the one its author is least likely to have checked. ⚠ Also: a mis-description can run in the **strengthening** direction — a notification credited an "`E0423` compile-failure control" where the real guarantee is the type system and no such control exists in the tree. Report it anyway: **a phantom control is a liability whichever way the error runs**, once it reaches a retro or a control inventory. And say plainly you ran a *structural* pass and executed nothing |

| [construct-the-positive-control-yourself-and-calibrate-on-the-excluded-example](construct-the-positive-control-yourself-and-calibrate-on-the-excluded-example.md) | For any `assert!(x.is_err())`, **build the nearest should-succeed input and run it** — one probe turns "a negative check needs a positive control" from a heuristic into a measurement. Here the matching-types twin failed **identically** (`"cannot infer type of lambda without annotation"`), so elaboration never reached the type relationship and the row had **zero cover**. Then find the repair by grepping the same file for a sibling of that form which succeeds, and diff the spellings — a return-type annotation, one token, measured. ⚠ And **calibrate on the excluded example**: told "do not re-derive `sec61`/`sec62`", I skipped *reading* them too, and my `cover|conformance|certif` sweep came back clean and wrong — the claims are **bare row ids**. *"Do not re-derive it" bounds the work, never the calibration.* Finally, say the direction: the behaviour was CORRECT and only the instrument was broken |

| [apply-a-rulings-criterion-to-the-next-consumer-yourself](apply-a-rulings-criterion-to-the-next-consumer-yourself.md) | A ruling that turns on a **structural property** hands you a reusable **classifier** — extract it and run it against every other artifact in the class, especially one that landed after the ruling and does not cite it. Here "direct guest leaf vs recursive carrier" was decidable from `List`'s own shape (`Nil \| Cons a (List a)`), so the next increment's whole recursion surface plausibly needs the unreleased capability while the landed prose promised "ordinary structural recursion". Constructive twin of [[a-ruling-that-widens-a-shared-map-names-only-the-consumer-it-was-about]]; the yield is a **forward** dependency caught at declaration time. ⚠ Also: **a `None` proves nothing until your key is in the lookup's domain** — I read `all_support_origin(host)` as "no support generated" when the query takes a *generated family* id, so `None` was the right answer to a question I never asked; report such probes as unresolved, never as coverage. And a **negative repro is a deliverable**: bound it, and remember a normal-form file is a formatter's least informative input |

| [the-demonstration-instance-can-be-the-extremal-one](the-demonstration-instance-can-be-the-extremal-one.md) | ⭐ **THE STANDING QUESTION** (named across two findings, 2026-08-11): *"the instances chosen are the ones where the distinction collapses."* Two rounding ties both extremal the same way; one witness whose producer construct has exactly one child. **For any control set, ask what distinction it must survive, then check whether every instance lands on the same side.** Cheap, and it applies to controls already green and already reviewed. **This class beats a defect because a defect gets fixed and this shape gets REBUILT** whenever a control set is written without that question. Repair has **three** outcomes, not one: guard it (count **plus the reason it buys**), record where another instrument establishes it, or say why the fact is not sensitive — and **adding an assertion in the third case is worse than the gap**. | A suite proved "validation subsumes the identity check" on `Nat` — **the unique roster family with exactly one nullary constructor**, i.e. the single most favourable instance. `IOError` has 11, `FileOperation` 10. ⇒ Ask whether the demonstration instance is representative or **extremal**, and answer from the population's **own declarations**, never from the suite under audit. **The tell is a justification containing "the only …"** — a statement about one family wearing the grammar of a general law. ⚠ Companion: **population coverage and axis coverage are independent.** Roster-wide loops existed (provenance, family-uniqueness) and both are blind to sibling substitution *by construction*, so a loop over every member checking the wrong property reads as coverage — write down the axis each iteration discriminates, then ask which axis the threat moves along |

| [build-the-operation-by-law-incidence-for-any-lawful-class](build-the-operation-by-law-incidence-for-any-lawful-class.md) | Enumerate a dictionary's **operations** against its **law statements** and build the incidence — `CursorOps` has four operations and all three laws quantify over `remaining` only, so **`locate` appears in zero laws** and any instance may define it as anything. That is a finding only if there is a **generic consumer**; there was (`decoder_fail` reports `cursor_locate`), and the two instances oriented it **oppositely** (one increases on advance, one decreases), so a rejection at "7" means opposite things. ⇒ **The divergence is invisible at one instance** — with a single implementation the instance simply *is* the definition — so **the second instance of any lawful class is the high-yield moment**. Attribute the missing law to the *interface*, not the newcomer, and note when the newcomer disclosed its choice (the defect is then "local disclosure, generic consumer"). ⚠ Don't read a `trusted_base()` identity assertion as evidence laws are real — a hollow law has zero trust delta; **read the proof terms** for real terms vs constants |

| [anchor-a-claim-census-to-position-and-validate-it-against-a-reference-count](anchor-a-claim-census-to-position-and-validate-it-against-a-reference-count.md) | A substring census mixes populations: grepping `surface/` for row-id claims returned 157 hits including 29 `//!` lines I read as an uncounted certifying population — **all 29 were file-path citations** (`spec/30-surface/…`), because the claim vocabulary is a substring of the path vocabulary. **Anchor to POSITION** (`^\s*/// surface/`, doc content *beginning* with the id): `///`=29, `//!`=0, `//`=3 — and the 3-line residue was the real finding, two claims resolving to zero headings. This is the CI-IGNORED-SWEEP mixed-population shape reproduced by me while hunting that same mechanism — **having the lesson did not prevent it; printing the matched lines did.** ⇒ Reconcile the corrected count against a number the instrument already publishes (29 = its own report) so the probe is validated; and resolve ids by **prefix with a positive control**, since real headings carry trailing `(soundness)`/`[NODE]` markers and exact match reports the whole population broken |

| [use-parametricity-to-close-a-reachability-question](use-parametricity-to-close-a-reachability-question.md) | Asked whether a decoder could reach a blocked kernel capability, the sweep answer ("read it all, saw nothing") is worth almost nothing — it does not generalise and is only as strong as one hour's attention. **The closure: every `Json`-typed value flows into `Json` constructors or into combinators taking `(a : Type)` ABSTRACT, and a term parametric in `a` has NO eliminator for `a`** — so the capability, supplied only by `Json`'s eliminator, is unreachable by construction. ⇒ Whenever the question is *"can path P reach capability C"*, ask whether P goes through anything abstract in C's type; if so the answer is a structural no **that holds for code not yet written**, which is what saves the ring the next audit. The type system is the gate, in the sense of [[an-enumeration-needs-a-proven-closure-not-a-better-grep]]. ⚠ Also: say **"at least N"** when you enumerated by inspection — I showed two instances of a class, the ruling found three |

| [ask-the-tool-do-not-model-its-input-syntax](ask-the-tool-do-not-model-its-input-syntax.md) | A CI comment reasoned *"every fence carries a non-Rust marker, so `cargo test --doc` collects zero tests"* — premise about input syntax, conclusion about tool behaviour. My audit scanned fence markers and said **10**; running the tool said **14**. Three numbers, and only the collector's is the answer — **my probe was wrong in the same direction as the comment, because I reproduced its method.** ⇒ When a claim is about what a tool collects/emits/accepts, **the tool is the oracle**; the tell is a comment containing a derivation where one command exists. ⚠ Companion: **dead test coverage is its own class** — collected and never executed reads as coverage; grep an unexecuted surface for prose asserting it *runs* (`values.rs:129` justified a capability with "It also **runs**"), and check the instruction resting on the false premise, since that is where the cost lands. Bound it: listed ≠ ran, one crate ≠ workspace, so "at least 14" |
| _(correction folded into [anchor-a-claim-census…](anchor-a-claim-census-to-position-and-validate-it-against-a-reference-count.md))_ | **A reference count validates your probe only on axes derived INDEPENDENTLY.** I reconciled my census to the checker's published 30 and called it validation — but my probe hardcoded `surface/`, the same token the checker hardcodes, so the agreement was a tautology. Real population was ~74 lines across four namespaces; the checker governs ~40%. **Derive the population from the artifact's grammar, never from the instrument's vocabulary**, and name the premise you might share before citing agreement as corroboration |

| [run-the-reconciliation-census-in-the-reverse-direction](run-the-reconciliation-census-in-the-reverse-direction.md) | A node reconciling claims against an artifact fixes **claim to artifact** — its frame, its ACs and its checker all quantify that way — and **nothing asks artifact to claim**, though the mirror is one `comm` over the two populations it already assembled. It returned **one** unclaimed row, whose `expect: accepts` the landed elaborator refuses unconditionally because the capability is deferred under an open decision: **a requirement with no test attached is exactly where an unlanded capability survives**, since the missing test is what would have failed. The frame's own §2c *named that row by hand* — the artifact was in front of the enclave and only the question was missing. ⇒ Read a frame for the **direction its ACs quantify over** and ask what the opposite quantifier returns; scope the denominator to where the convention holds (17/18 in the file, 66/665 corpus-wide, and only the first is a signal); and cite the artifact's **own** stated rule — here a preamble demanding expectations be grounded in the *landed* elaborator, and a deferral-tagging convention **keyed on one open decision** so the next one got no tag. ⚠ Attack the row's self-reassurance (*"guard-gated, not coincidental"*) and **report the refutation when it HOLDS** — `resolve.rs:1604` gates one layer above the deferral fence, which gave a premise resting on three readings of one spec section a fourth, non-textual corroborator, free. ⚠ And measure a suspected novelty at the **pre-merge tree**: 5 of 6 multi-claimed rows predated the merge, so the duplicate was convention — what survived was that the convention's `(a)`/`(b)` labels were dropped and the two claimants **partition** the row's conjunctive `expect` |

| [compose-your-own-measurements-against-the-artifacts-relational-claims](compose-your-own-measurements-against-the-artifacts-relational-claims.md) | I measured **guard present** (`resolve.rs:1604`) and **capability absent** (`elab.rs:5584`) and stated both adjacently. **Composed, they say both sides reject at distinct gates** — refuting the adjacent row's own *"Verdict-flips … space-op resolves / pure-view rejects"*, three lines above the sentence I did audit. I went there to attack a **reassurance**, it HELD (correctly), and I reported the row clean: **the hunting frame selected a sentence, the artifact carried two claims, and clearing one said nothing about the other.** The Architect caught it on the candidate; one round-trip. ⇒ **Name the SENTENCE you audited, not the row** — *"I attacked X and it held"* reads as *"the row is sound"*. ⇒ General form: **when your finding is "guard present, capability absent", every artifact asserting a relation needing BOTH is false** — verdict flips, discriminating pairs, differential oracles, accept/reject twins all collapse when one side is unlanded, so **the composition is the finding and the two measurements are only its inputs**. The tell: I had already written *"the guard landed and the capability did not"*, aimed at attributing a residual instead of auditing a neighbour. ⚠ The repair beat my ask by fixing the **key** of the deferral convention (*"the tag names implementation availability, not whether the design decision is open"*) rather than tagging one more row — when a convention fails for the **second member of its class**, request the key, or you invite the per-instance patch |

| [a-type-narrowing-discharges-one-clause-and-a-producer-enumeration-carries-the-rest](a-type-narrowing-discharges-one-clause-and-a-producer-enumeration-carries-the-rest.md) | *"Implicit fallback is structurally unreachable"* delivered by `Option<&T>` → `&T`. **The type supplies exactly one clause — that whoever calls THAT function supplies a value.** It says nothing about which function a package-backed compile calls, which is the whole property, so **everything after the semicolon is a producer enumeration in prose** — and it was wrong in both directions: the cited producer `program_authority` exists in **no tree** (introduced and renamed *inside the same branch*; `git log -S` pinned it), and a **third ungated production producer** was omitted. ⚠ **The citation defect PROTECTS the enumeration defect**: a reader greps the name, gets zero, silently repairs it to the obvious neighbour, and never re-audits the claim the citation was supporting — so when a citation fails, audit the sentence, not just the symbol. ⇒ **Report property and argument separately, property first** — I tested four axes (parameter, wider-typed sibling, module scope, call graph) and all four HELD; leading with the defect on a merge whose safety survives every attack reads as an alarm. ⇒ **A non-`Option` parameter is a REDIRECTION, not a closure** — chase the value to a producer you can see (I followed `&entrypoint.process_symbols` to its single production writer), or name the hop you stopped at. ⚠ **The UNQUALIFIED statement is the outlier and that is why it reads as law** — the tree carried the correct qualified sentence **twice** and the over-claiming form once; **majority-correct is the dangerous configuration**, because the qualified siblings do not refute the outlier, they read as *it plus detail*, so confidence gets read as authority. Count the restatements and compare their **qualifiers** — the variance is the signal. ⚠ And the **durable half was the framing stated in passing**: three ACs were the repairs I named, the fourth was the sentence explaining *why* the enumeration mattered — site-level repairs close on one file, the mechanism-level sentence binds the next author |

| [a-detector-that-re-derives-its-mechanisms-lookup-is-blind-where-the-two-disagree](a-detector-that-re-derives-its-mechanisms-lookup-is-blind-where-the-two-disagree.md) | A boundary sentinel's whole value is fidelity to the filter it stands in for, and the two joins were **not the same operation**: production `BTreeMap::collect` + `.get()` (**last** write wins, `None => true` **retains**), sentinel `iter().find()` (**first** match wins, `continue` **drops from the scan**). ⇒ **The set production treats specially is the one set the detector cannot see**, invisible because both spellings read as "look the callee up" — write the two lookups side by side as **operations** and ask what each does on a duplicate key and on a miss; ask for **reuse of the production expression**, which makes the question moot rather than answering it. ⚠ **A non-vacuity guard bounds EMPTINESS, never COVERAGE** — `joined > 0` says nothing about how many edges were skipped, and `> 0` is what gets written because it is what passes today; assert `== total` or print the skipped ones so an exclusion is visible. (The *other* guard, `superseded > 0`, WAS sound — because it and the detector are computed **in the same loop iteration over the same element**; check that sameness explicitly.) ⚠ **Bound a fidelity finding to fidelity** — I constructed no duplicate id, so the severity is "the detector does not mirror the mechanism", which needs no reachability argument. ⚠ And the over-claim **recurred inside the recut that was fixing an over-claim**, in the **assertion message** rather than the doc: read the failure message against the predicate separately, since it is written last and is the only text a future engineer sees when it fires |

| [the-operative-artifact-must-carry-the-claim-whichever-pass-wrote-it](the-operative-artifact-must-carry-the-claim-whichever-pass-wrote-it.md) | **The pass asymmetry runs BOTH ways and I had only ever seen one.** Loose summary in operative text ⇒ a confusing **red** (someone reads it, looks, is annoyed). Precise obligation stranded in **prose** while the operative row stays silent ⇒ a clean **GREEN** — nobody looks, nothing fires, the AC discharges. **Only the first generates evidence.** Here a code comment correctly delegated its release condition to a tracked AC; the pointer resolved and the node genuinely owned it, but the obligation sat 142 lines below the AC row with no forward reference, and that row is the table's **last** — so a discharge check stops exactly short of it. A stand-down clause with no author: nobody wrote *"don't look"*, the operative artifact just never said there was anything to look at. ⇒ **Hunt it at the DELEGATION seam** — deleting a local statement is usually right and is also the moment the whole obligation rests on one hop: confirm the pointer (1) **resolves**, (2) **answers**, and (3) answers **in the artifact the reader will consult**. I would have stopped at (2) and reported clean. ⚠ **Time it to the window** — the AC's blocker cleared the same day, so say why the window is open now or a one-cell repair reads as tidiness. ⚠ And on a report about your OWN repaired finding, **state the two ways it could have been hollow and were not** (the injected reader was not a test-only seam; the redirect did not dangle), or it reads as never satisfied. ⚠ **An empty per-path `git show` means you are on ONE COMMIT of a multi-commit tip** — the near-miss that nearly cost me the finding: the notification's exact SHA was a branch tip carrying both deliverables as separate commits, so `git show <sha> -- <path>` showed nothing and read as *"this deliverable did not touch its declared path"*. **Empty output on a declared path is a signal about your RANGE, not the change**; the declared path count is the cross-check, and the fix is to diff from the **declared base**. Failure direction is the bad one for this seat — I would have reported one deliverable and the silence on the other would have read as *hunted and clean* rather than *never looked* |

| [rank-a-controls-assertions-by-what-survives-a-redundancy-trim](rank-a-controls-assertions-by-what-survives-a-redundancy-trim.md) | Three assertions offered as load-bearing; two were (a denominator whose counter has **exactly one increment site**, and a **committed** suppression A/B falling through to the true pre-repair path). The third, `assert_eq!(forwards, arrivals)`, is **`x == x`** — between the two increments sits no branch, no fallible step, no early return, so the only thing that could redden it is the test's own flag. ⇒ **The finding is not the tautology, it is which one gets KEPT.** The equality **passes at `0 == 0`** and is the more impressive-looking of the pair (an equality, not a `> 0` — the very shape I had argued *for* hours earlier), so a reader tidying two redundant-looking assertions keeps it, drops the denominator, and **the control goes vacuous with a green suite**. ⇒ Ask **two** questions of every control: *what would have to be wrong for each assertion to redden?* (correctness) and *if trimmed to one, which survives — and does it pass vacuously?* (**durability, and nobody asks it**, because at review time all the assertions are present and green). ⚠ Where no failure mode exists, prescribe **labelling, not strengthening** — the equality cannot be made informative, only annotated. ⚠ And when an author names a failure mode they avoided (*"a repair that deleted the sentence would make `!contains` true for free"`*), **confirm the avoidance against the tree** — one command showed the refusal string still present twice in production. ⚠ **A durability risk with a MOTIVATED actor outranks one needing an accident**: both counters are themselves `cfg(test)`, so a pass whose *stated purpose* is stripping test-only machinery from production aims at this exact block — name the class of maintenance work that targets your construct. ⛔ **And "it can only be labelled" was my own under-enumeration** — true of the *predicate*, false of the *control*: the second route is to make the two assertions **inseparable**, durability by construction rather than instruction. I varied the predicate's content while the answer varied the assertions' **granularity**, which is [[no-option-works-name-the-axis-you-enumerated]] recurring on me while I held it. State the **property** the repair must achieve and leave the mechanism to whoever is in the file. ⚠ Name the **record** too — the doc listing the two assertions as equals is where the mis-weighting was taught, and fixing only the code reproduces the finding one layer up. ⛔ **FOLLOW-UP: the repair achieved durability by NAMING.** `let established_arrivals = arrivals;` is a plain `usize` copy that reads nothing from the guard — delete the denominator and it all still compiles and passes at `0 == 0`, the very trim the property forbade. **A rename is not a coupling**: ask *what happens to the compiler if I delete the guard?*, and if the answer is "nothing" it is instruction wearing construction's vocabulary (`NonZeroUsize::new(...).expect(...)` + `.get()` makes removal a **compile error**, two lines). ⚠ Its comment — *"cannot be quoted as evidence in the all-zero case"* — is FALSE and is exactly what a durability audit reads before stopping: **the claim inoculates against the check it describes**. ⚠ Credit the half that landed, or a follow-up reads as never satisfiable. ⚠ Same merge: **an unchanged function can still be wrong if its INPUTS widened** — byte-identity is one command and is not the interesting half; check what the change now routes in (here the new construct is not a variant of the walked enum, and the sibling match has no `_` arm, so a future one is a compile error). ⛔ **SECOND FOLLOW-UP: the `NonZeroUsize` construction landed and is genuine — on the ONE leg I named.** The sibling leg thirty lines below is byte-untouched with the identical shape (deletable denominator, then `assert_eq!(s_forwards, 0)` which passes *more* easily at zero), **and the repaired leg's own comment names it**. That is on me: the repair's population is the instances the finding named, so **grep the enclosing function for the SHAPE before filing** — every `assert_eq!(x, 0)`, `is_empty()`, `!contains()` downstream of a separately-deletable guard — or expect exactly one to be fixed. The clincher is the motive test: **a leg-specific repair against a non-leg-specific hazard is an instance patch**, since the `cfg(test)`-stripping pass reaches both legs equally. ⛔ **THIRD FOLLOW-UP: a coupling through the MESSAGE is not a coupling.** The sibling leg got the same `NonZeroUsize` but consumes it in the **format string only** — the comparison is against a literal, so the value has no semantic role. Deleting the binding is `E0425` *in the format argument*, but **shortening the message** (an innocuous tidy) makes it merely `unused` and the line freely deletable again — and I checked: no `deny` in the crate root, no `-D warnings` in CI, so **an `unused_variable` you have not confirmed is denied is not a guard**. ⇒ Ask of any *"removal is a compile error"* claim: is the value an **OPERAND** or an **argument to a message**? Only the first survives a message edit. Scoped to the shape across both legs this time — the previous follow-up's correction actually applied |

**Also on `0a441619`** — a grounding record kept *"grounding, not an identity"* scrupulously in its **operative text** (population bounded, full table printed, *"establishes only that…"*) and dropped it in its **headings** (*"is projectable and unique"*, *"is projectable"* — unqualified properties from one witness), plus *"independently confirming"* which is true of instrument independence but not of population. The record's own ⛔ guard was attached to the tuple, not to the sections feeding it. **Headings are written in a different pass and are what a scanner reads** — same pass-asymmetry as [[the-operative-artifact-must-carry-the-claim-whichever-pass-wrote-it]], and worth one qualifier each.

| [a-name-free-slot-beats-a-reserved-spelling-and-the-alias-path-is-the-second-reader](a-name-free-slot-beats-a-reserved-spelling-and-the-alias-path-is-the-second-reader.md) | A wildcard binder had leaked a capability by being resolvable as `_`. The repair was **not** *"lookup refuses `_`"* — `push_anonymous` pushes an **empty name list**, and every reader tests `names.iter().any(...)`, so the slot is unpublishable by **any** spelling, not the one someone remembered to ban. **A reserved-spelling guard is a blacklist and inherits every normalisation/encoding/aliasing question; an empty capability set has none**, while keeping the positional structure the slot exists for. ⇒ **Check the ALIAS/RENAME path — it is the second reader**, and the way a name-free binding gets a name *back*; here it closes because it locates its target *by name*. Enumerate every **writer** of the name field (alias, rename, re-export, import, shadow), not just the lookup the author checked. ⇒ **Close a string sentinel by counting its PRODUCER's callers**, not the sentinel's occurrences — `resolve_pattern` had exactly two, one being its own recursion feeding the guarded one, which answers *"is there another path?"* with a number. ⚠ **The repaired population may not be the whole population**: the token is a wildcard only in *pattern* position, so in binder position it is an ordinary identifier producing a resolvable binding — ask where else the same token is legal and NOT special, and file it as a **bounded question** with the cheap test named when you have not read the deciding path. ⚠ **A clean result is a deliverable when it carries the reason** — the construction, the three readers, the closed count — whereas *"looked, found nothing"* expires immediately; and say which axes you did not reach |

| [a-non-degenerate-pair-fails-to-fail-if-the-assertion-cannot-tell-the-halves-apart](a-non-degenerate-pair-fails-to-fail-if-the-assertion-cannot-tell-the-halves-apart.md) | COORDINATION §7b's discriminator law says a shared-input pair protects you because *"a flipped boundary inverts BOTH and the pair fails"*. The control built the pair correctly and then asserted `Err(Variant { .. })` on a **single first-error return** — so if the mutation made the enclosing scope inactive, the **positive comparator** would raise the identical variant and the test would still pass. ⇒ **A flipped boundary inverts both and the test PASSES**; the pair's whole protection lives in the payload saying *which half spoke*, and `{ .. }` throws it away. **Building the pair is half the work** — ask *if the positive comparator started failing the same way as the negative, would this assertion notice?* Two candidates, one channel: the channel must carry identity. ⇒ **Look one function away for the strong idiom** — the mechanism's own unit test already bound both spans, which makes the ask concrete and kills the *"is that even expressible?"* objection. ⚠ **Refute your own conflation hypotheses first**: I thought the mutation varied two things (no — the association derives from the kernel telescope, so the removed sibling selector is inert) and that the variant had two raise sites (no — the second was inside `cfg(test)`). Both refutations are what made the finding narrow, and both are worth stating. ⚠ **Verify the claim at the RAISE SITE, not from the control's outcome** — `name: _` explicitly discarded settles "identity not spelling" in one read, and lets you report *mechanism sound, control under-resolved*, which beats either verdict alone |

| [a-single-site-claim-is-checkable-by-counting-the-operation-it-names](a-single-site-claim-is-checkable-by-counting-the-operation-it-names.md) | *"Why the order lives in exactly one place — `for_case` performs **the one** `.rev()`; no consumer re-derives it, so a lowering change is a **single-site correction rather than a hunt**."* **A claim of that shape names its own probe.** Grepping `.rev()`: **five** sites — four in production, plus `for_case`, whose only readers are four call sites inside `#[cfg(test)] mod tests`. The type carried **`#[cfg_attr(not(test), allow(dead_code))]`**, which is a **free oracle: a type that needs it has no production consumer**, whatever its doc says. ⇒ *"Exactly one place"*, *"the single source"*, *"no consumer re-derives"* are **countable** — grep the operation, bucket by whether production reaches it. **Two harms:** it is a **stand-down clause** telling the next implementer not to hunt the other four; and the tests over it are **self-oracled** — `for_case` recomputes the reversal from the same inputs rather than observing production's output, so deleting production's `.rev()` leaves every assertion green. ⚠ **Scaffolding is a fair reading, so attack the TENSE, not the type** — "delete this" would be refutable, "this sentence is false today and tells a reader not to look" is not. ⚠ **Credit a population discipline when it lands** (the doc printed the degenerate and two-position fixtures side by side and named the length-one blindness *in the claim*) — then note that **a measurement in a doc comment does not redden**. ⚠ And **know when to stop**: I declined to file a fourth round on the same three lines, and said so |

| [an-unreachability-argument-covers-one-route-and-the-catch-all-covers-another](an-unreachability-argument-covers-one-route-and-the-catch-all-covers-another.md) | A diagnostic shipped **defined, Display-pinned, never constructed**, on a sound four-premise unreachability argument. I verified both the zero and the argument — and the `match` it belongs to has a **third arm covering a different residual**: *"the classifier did not reduce to a universe at all"*, not *"a metavariable left it ambiguous"*. That arm **blames the kernel** (`KernelRejected` when `kernel_infer` had already succeeded — the elaborator's own failure wearing another component's name), **fabricates its expectation** (`Type ?0`, never solved or requested — and substantively wrong, since `Omega` is equally admissible there), and **its condition is what the unconstructed diagnostic names**, so the tree holds two paths for one condition with the accurate one unused. ⇒ **When a zero is defended by an unreachability argument, read the `match` and ask which arm covers what** — the argument is about the interesting route; the catch-all is where the uninteresting residual goes, unproofread because it "cannot happen". ⚠ **Reachability is not the hinge — say so**, or the report gets answered with *"that's unreachable"*, which is true and irrelevant: if it cannot run it is exactly as dead as the diagnostic it displaced. ⚠ **A rejection removes the instance; the IDIOM moves** — the prior candidate was rejected for fabricating `Term::Type(Level::Var(LevelVar(7)))` in a *control*, and the same fabrication survives in the *production* catch-all of the candidate that fixed it. Grep the successor for the SHAPE. ⚠ **Concede a ruling you cannot dispute, then bound its scope** — it was a claim about today's data structures and is not self-enforcing |

| [adjacent-arms-over-near-identical-shapes-are-the-cheapest-diff-in-a-recursive-walk](adjacent-arms-over-near-identical-shapes-are-the-cheapest-diff-in-a-recursive-walk.md) | In a ~15-arm hand-written walk, `Closure` recursed into its **body only** while the adjacent `LexicalClosure` also walked its **captures under the OUTER environment** — and a capture is evaluated in the enclosing scope, which is why the second is right. ⇒ **The highest-yield read in any recursive walk is sibling arms over near-identical shapes, side by side**: a difference in *whether an arm recurses at all* is either a real child-layout fact or an omission, and **the code never says which**. You are not checking N arms against a spec — you are checking one arm against its twin, and **the twin IS the spec**. ⚠ **Bound it on the fact you did not establish** (does the quiet form even have capture child origins?) and file one question, not a defect. ⚠ **Name the DIRECTION** — under-reporting hypotheses into an identity key means two distinct producers key the same, the fail-open direction; an asymmetry with no direction reads as tidiness. ⚠ **A disclosed population-of-one still has POSITIONS it cannot reach**: the record honestly disclosed *"all three depths"* — `Let`, nested `Match`, closure **body** — and a capture is a different **position** from a body, so the honest disclosure still left the cell empty. Ask which positions the named shape does not contain. ⚠ And **say where you stopped in a long match** — "I reviewed the walk" is a claim you cannot support at fifteen arms. ⛔ **TWICE NOW the deciding evidence was OUTSIDE the arms**: `Closure`/`LexicalClosure` was settled by a **consumer** (downstream), `f32`/`f64` literal construction by a **guard eleven lines above both branches** (upstream) making the thinner arm unreachable. ⇒ **The twin is the specification for what the arms DO, and says nothing about what REACHES them** — look upstream for a guard and downstream for a consumer before filing. Both stayed cheap because I filed them as **questions with the deciding fact named**, costing one lookup each; an asserted defect on the same evidence would have cost credibility. ⚠ A refuted question can still be the thing that gets a real limitation written down |

| [a-pre-existing-twin-distinguishes-a-convention-from-an-omission](a-pre-existing-twin-distinguishes-a-convention-from-an-omission.md) | **My adjacent-arms asymmetry was a FALSE ALARM**, and how it resolved is the reusable part. Two things settled it, neither in the arms I was reading: **the deciding evidence sat in a CONSUMER** (a site built the quiet form's provenance with *no child lookup at all*, while its sibling branch looked children up by ordinal and **errored when absent** — if the children existed, that code would read them), and **a pre-existing twin carried the identical asymmetry**, predating the change. ⇒ **Before filing a structural asymmetry, grep for an older site with the same shape**: an older twin means **convention**, no twin means it arrived with this change. One command, and it is the difference between a finding and a false alarm. When two producers disagree about a shape, **the answer is written in whatever consumes it**. ⚠ **The bound is what kept it cheap** — naming the deciding fact made the answer one lookup rather than an argument; filing a bounded question is legitimate, filing it without having looked for the twin is not. ⚠ **A DIRECTION argument can outlive the finding it came with** — the instance died and "under-reporting into an identity key is fail-open" was kept as load-bearing text; state direction and instance as two claims so one survives the other. ⚠ Same merge: **a reachability defence is only as good as its enumeration** — an unchecked index in a *kernel* function defended as *"the caller can no longer reach it"* (singular) had **~20 callers**, two inside the kernel's own checker; report the **scope of the sentence**, and prefer the repair that dissolves the question. ⚠ **Check the axis you were handed FIRST and say so when it holds** — it did, and stronger than described (a computed value through the full pipeline, three witnesses, **two different expected values**), which is what makes a bounded observation read as a weight rather than an alarm |

| [a-lexer-guard-on-character-adjacency-is-defeated-by-one-space](a-lexer-guard-on-character-adjacency-is-defeated-by-one-space.md) | A scanner had swallowed `p.1.2` into `Ident, Dot, FloatLit(1.2)`; the repair reads the character immediately before the numeric. **I expected a stale mutable flag and there is none** — derived fresh from the source each scan, so no staleness surface exists at all, which is worth recognising as the right construction. **But it tests CHARACTER adjacency, so `p. 1.2` restores the defect** behind whitespace the grammar probably ignores. ⇒ **For any lexer guard keyed on neighbouring characters, re-write the input with a space at the boundary**: the guard's condition is lexical, the language's rule is grammatical, and **whitespace is exactly where those come apart** — a scanner being the one place a whitespace difference changes meaning silently instead of being normalised away. ⚠ **Weight it by what else lives in the crate** — the formatter and lossless printer are in the same twelve paths, so a space emitted after a projection dot is a **meaning-changing reformat**; a lexer's input is not only what users type. ⚠ **State the bound as the deciding parse** (is the spaced form even grammatical — if not, this is nothing) and give the control in the property's own terms: the two spellings must produce the **same token stream**, which is what the guard reaches for and states nowhere. ⛔ **CORRECTIONS, both mine.** (1) I called it a **silent** mis-parse; it is a **loud** `ParseError` — the projection loop's lookahead admits only `Ident`/`Nat(1|2)`, so a `FloatLit` builds no projection. **I asserted a direction from the producer and never asked the consumer**, one hop away; "silent" is a claim about *every* stage, not about the token, and flagging my severity as conditional was doing work the measurement should have done. (2) **The formatter axis I weighted it on is EMPTY** — a lossless printer can only reproduce input that already parsed, and the failing spelling never parses, so the hazard class has no member. **Ask a tool's reachable OUTPUT SET, not its capability**; for any round-tripper the range is bounded by what its input side accepted. ⚠ The weighting was my stated reason for raising it and the finding stood without it — **a finding that needs an amplifier is worth re-reading for whether the amplifier is the claim** |

| [an-establishing-check-can-be-DOWNSTREAM-of-the-use-a-third-branch-i-did-not-pose](an-establishing-check-can-be-DOWNSTREAM-of-the-use-a-third-branch-i-did-not-pose.md) | I bounded a finding on *"is this key field established upstream, or not at all"* and named **two** branches. **Neither: it is established twenty-five lines LATER**, so the derivation is sound **inductively** — on another member's independence plus the caller's whole-key equality. ⇒ ***"Established" is a question about ORDER, not a yes/no about a function***: validated **before** this use, **after** it, or **not at all** — and the middle answer is the interesting one because it is sound *and* non-obvious. **The third branch is what selects the repair**: upstream ⇒ fix the comment, absent ⇒ fix the code, **downstream ⇒ the comment must become CONDITIONAL**, separating the unconditional locators from the one resting on a later step. A two-branch bound could not have named the right fix even by luck. ⚠ **The framing survived the enumeration being incomplete** — *"a comment asserting an independence the line beneath it does not have"* held on all three branches, so **lead with the property that survives every branch of your own bound** and let the branches decide only severity and repair. ⚠ And **do not re-derive a measurement offered with its coordinate on a closed finding with no attack surface** — I tried, hit line numbers shifted by the added comments, and stopped; posting anyway to show I looked is the always-produce-something failure this seat is most exposed to |

| [a-confirming-first-instance-is-when-the-sample-size-matters-most](a-confirming-first-instance-is-when-the-sample-size-matters-most.md) | Hunting misdescribing comments, I found one in the **first place I looked**, matching a just-confirmed shape — real, cheap, and flattering to the pattern. The population was three; I read the other two and **both were correct**, one stating the true mechanism eight lines above the constant. ⇒ The diagnosis **inverted**: not systematic drift but *the artifact was right and the summaries written away from it were wrong*. **A confirming first draw is the strongest reason to read the rest, not the weakest** — a disconfirming one sends you looking anyway; a confirming one is what makes an unrepresentative sample feel like a class, and it arrives with the reward attached. This is [[the-demonstration-instance-can-be-the-extremal-one]] turned on **my own evidence-gathering**. ⚠ **What the unread population would have left attached is a wrong DIAGNOSIS, and the diagnosis is what gets routed** — "systematic" is what supported extending a gate, which would have destroyed a deliberate design property recorded two lines above the comment I was citing. Rank by what travels: a finding travels as a fix request, its framing as the reason, and the framing is what a reader cannot check cheaply. ⚠ I made the same error I diagnosed — read `:2165-2172`, stopped at the top of the block, and `:2163-2164` held the reason not to prescribe. ⚠ **Report a population you cannot audit AS a population**: six lines, two spellings, **zero read** — enumeration without audit is a real deliverable provided the report says so |

| [a-context-flag-multiplies-lines-per-match-so-head-drops-matches](a-context-flag-multiplies-lines-per-match-so-head-drops-matches.md) | I reported **three** call sites and there were **four** — `git grep -A6 … | head -24` shows about three matches because `-A6` costs seven lines each, and **the output ends cleanly at a match boundary so it looks complete**. The missed site was **the one the whole equivalence argument turned on**, so the enumeration was the finding. Same defect as [[a-probe-truncated-before-the-grep-is-not-a-measurement]] wearing context flags; the multiplier is what hides it. ⇒ **Never take a COUNT from a pipeline that truncates** — `grep -c` for the number, a separate bounded read for the text. ⇒ With `-A`/`-B`/`-C` the budget is matches × context; bound by **matches** (`-m`), not lines. ⚠ **The dropped tail is not random** — grep walks in order, so `head` always loses the LAST matches, i.e. the latest-added or outermost caller, which is exactly where a differing sibling lives. ⚠ **Rank a wrong count above a wrong inference in your own output**: an inference is offered as reasoning and invites checking, a count is offered as measurement and is taken |

| _(fold into [a-repro-is-evidence-not-a-completion-oracle](a-repro-is-evidence-not-a-completion-oracle.md))_ | **A BOUND THAT NAMES THE DECIDING READ IS AN INSTRUMENT, NOT JUST HONESTY.** Twice now the actionable part of my report was the bound, not the finding: *"I did not read the mint site, so the lane-independence question is open rather than answered against"* named **the one read that would decide it**, and the Steward ran it and it settled the attribution — on a ground the committed control did not state. ⇒ **Write bounds as the next step, not as a disclaimer**: name the specific artifact whose reading flips the verdict, and say which way each outcome falls. *"Unhunted"* transfers nothing; *"one read of X, and if X is unconditional the inference holds without any witness"* transfers the whole task. ⚠ Corollary from the same exchange: when a conclusion is written into a frame as fixed input **without its licence**, the conclusion travels and the ground does not — and marking the OLD ground insufficient matters more than adding the new one, since a wrong reason left beside a right one is what the next reader inherits |

| [a-rejected-aggregate-recurs-inside-the-key-that-repaired-it](a-rejected-aggregate-recurs-inside-the-key-that-repaired-it.md) | An Architect rejection killed a compile-wide scalar with *"a count over a population is not a pairing within it"*; the repair keyed every relation by occurrence — and the entry doc's **justification for keying** is *"the same occurrence can be lowered more than once … each transport owes its own consumption"*. ⇒ **The aggregate is intact INSIDE the key**: `consumptions == rebinds` closes green at `2 == 2` with one transport consumed twice and its sibling dropped. **When a count-vs-pairing rejection is repaired by adding a key, ask whether one key holds several members — and the admission is in the repair's own doc, because the only reason to key is that the same member arrives twice.** Generalises to any `HashMap<K, Count>` replacing a bare `Count`. ⚠ **The control's near miss is the tell**: its row 3 builds `rebinds=2, consumptions=1` and asserts refusal — the author reached the hard case and tested **the direction the representation can see**; the entry holds two `usize`s so the unsound direction is unrepresentable. **That is not a missing row, it is the strongest claim the representation supports** — file the gap between the doc's requirement and the check's granularity, which needs no reachability argument. ⚠ **Verify a self-retiring premise's SIBLINGS one by one** (three cleared premises make the fourth feel cleared) **and sweep the phrase family OUTSIDE the merge's declared paths**, since your enumeration became the repair's population — same-phrase/different-subject is the common case, so report the population checked, not the finding you didn't make. ⚠ **An unqualified positional clause** (*"runs before emission"*) **can be true of one arm and false of its sibling** — count the call sites of anything a doc describes by position; rank it low when the safety rests on the next sentence instead |

| [the-population-in-a-deciding-read-selects-its-branch](the-population-in-a-deciding-read-selects-its-branch.md) | I filed a deciding read with **both branches named** — the instrument shape this corpus already prefers to a disclaimer — and specified its population as *"four `bound_constructor_fields` call sites"*. **The reach is six**: the one `rebind` site sits in `constructor_field_bindings`, which has **two** callers, and **every repeated descent the instrument measured was at the sibling route I omitted**. ⇒ **A wrong population does not produce a wrong number, it selects the wrong BRANCH by a method sound end to end** — instrument my four, measure honestly, find nothing, return `no`, and route to *"the premise is unreachable, correct the doc"*: the opposite conclusion with a green measurement behind it and my own framing supplying the interpretation. **When you specify a deciding read, the population IS the specification**, and it is the half nobody re-checks because the branches look like the hard thinking. ⚠ **The mechanism: I took the doc's SINGULAR NAME as the population** — the prose named one binder, I enumerated its callers, and my own grep had returned two call sites plus the definition. ⇒ **Enumerate callers of the function that PERFORMS the operation, never of the function the prose names**; one hop at a time, no hop skipped because a doc summarised it. ⚠ **My quantifier was wrong-shaped too** — I asked whether *two distinct sites converge*; what decides it is *multiplicity of descent*, and the trace shows **one site descending twice**. **Write the deciding read as the property, not as a mechanism you imagine producing it**: a mechanism-shaped question answers `no` while the property is `yes`. ⇒ **The ring re-derived instead of inheriting, and that is what saved it — credit that explicitly**, since it is the counter-measure whose absence I file against others weekly. Attribute honestly: derived from my own grep, so mine, not the artifact's |

| [the-unlawed-link-is-the-join-between-two-maps](the-unlawed-link-is-the-join-between-two-maps.md) | A ledger rebuilt from two counters into four maps asserts four relations — **every one keyed on its own map's keys**. The middle link, that a `transitioned` VALUE is a key of `minted`, has **no law**, and is supplied in prose as *"in bijection because one transition mints exactly one transport"* — a claim about a function body the closeout cannot fail on. ⇒ **Enumerate a relational ledger's laws by what each QUANTIFIES OVER**: four laws over four maps reads as complete and covers the *nodes*; the **edges are values-into-keys**, and a loop over a map reaches for its keys, so the shape of the code steers you off the join. ⚠ **The tell is a loop that BINDS the value and uses it only in the error message** — inverse of the destructure-discards-the-join-key tell. Failure state: `transitioned[r] = T` with `T ∉ minted` passes all four laws leaving `r` with no consumption required — **fail-open, one direction of one edge; say which**. ⭐ **THE STRONGER MOVE — hunt where a STATED standard was not applied.** The doc says *"a law worth stating is worth being able to fail"* and re-checks a relation **already enforced at the call site**, while the unlawed join is enforced nowhere. **The finding needs no argument for the standard: the artifact supplies it two loops away**, so the usual *"that's unreachable today"* answer is pre-refuted by the author's own reasoning. Cheaper than any finding against an unstated standard. ⚠ Don't upgrade it to a live defect — one writer, two infallible inserts, no branch between; repair is one line in a loop that already binds the value. ⚠ **State the hypotheses you REFUTED, especially your first guesses** (a silent `insert` overwrite; an ordering the caller must respect — both died on reading, and the refutations are what located the loop that *was* unlawed); a report listing only the survivor hides whether the obvious attacks were run |

| [an-inequality-over-an-option-confines-its-own-pair-control](an-inequality-over-an-option-confines-its-own-pair-control.md) | Two provenance guards read `recognized.scope != scope` over an `Option<FuncId>`, so **`None != None` is false and absence passes vacuously**. Every control row used `Some(0)`/`Some(1)` — **and could not have done otherwise**: a discriminator pair needs two differing values and the absent value is one value, so **no pair control for this guard can be built on the vacuous side.** ⇒ **The guard's shape forces its own control onto the inhabited side** — not the author sampling badly, so *"add a row where the distinction collapses"* does not exist as a repair, and a control set that looks complete on the axis is complete on the only half it can reach. ⇒ **Ask separately what value PRODUCTION supplies**, in two structural reads with no run: where the operand comes from at every call site (here one field at all three), and what its single setter is and whether the live path reaches it (callers entirely in the module governing *one* of two emission arms ⇒ unset for the whole descent on the other, both guards inert). ⚠ **The fact was already written down**, in a comment justifying the test-only constructor — *"production always passes `defining_function_id`, which is `None` outside the emission pass"* — with no consequence drawn. **Read a fixture's justification comment as a claim about production**; *"production actually passes X"* beside a control built on not-X is the finding, already measured, waiting for the composition. ⭐ **REUSABLE: to refute redundancy among N refusals, exhibit for each one a state the OTHERS admit** — three sentences, no run, and it answers a different question than a mutation grid (dropping K and reddening row K shows row K *depends* on K; the exhibit shows K catches what nothing else does). Report a flagged risk that turns out **absent** with full weight when it carries the reason. ⚠ **A synthetic control can be NECESSARY and still owe a label** — writing the maps directly was forced (a row through the one writer would assume the property under test) and identities stayed issuer-minted, but nothing said the states are production-unreachable today |

These are **lessons, not law** — verify a named file/flag/function still exists
before acting on one.
