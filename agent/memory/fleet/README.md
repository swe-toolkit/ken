# fleet — lessons every agent applies

Loaded by **every** role (referenced from root `AGENTS.md`). Keep this scope
small and universal: coordination law, attention discipline,
closure/ground-truth verification, clean-room, compaction. A lesson that only
some roles need belongs in a narrower scope.

| Lesson | One-line |
|---|---|
| [a-candidate-handoff-that-skips-the-leader-deadlocks-the-ring](a-candidate-handoff-that-skips-the-leader-deadlocks-the-ring.md) | A handoff mentioning the next WORKER but not the LEADER deadlocks the ring — QA correctly holds for its leader's review request, the leader never learns a candidate exists, and both idle for individually correct reasons. Not a strand (delivered and read), so the composer sweep is blind; the detector is a FROZEN finished-turn counter beside a fresh artifact. Rouse the LEADER, not the waiter |
| [a-claim-accurate-about-something-narrower-than-its-reader-infers](a-claim-accurate-about-something-narrower-than-its-reader-infers.md) | The most plausible dress a false handoff wears is a TRUE sentence answering a narrower question than the reader is asking — name the anchor in the same sentence as the claim |
| [a-correction-on-main-does-not-reach-a-candidate-cut-before-it](a-correction-on-main-does-not-reach-a-candidate-cut-before-it.md) | A correction landed on `main` does NOT reach an in-flight candidate cut before it — the merge stays clean, the content stays stale, and nothing flags it. Re-bind reviewers by BLOB, not by ref, and re-issue the correction to every held candidate |
| [a-deferral-is-honest-a-deferral-that-reads-as-delivery-is-not](a-deferral-is-honest-a-deferral-that-reads-as-delivery-is-not.md) | A WP can merge with its headline deferred and no test fails — ground GATE status in the gate's own text, never roll it up from WP status |
| [a-dependency-is-met-when-you-can-write-the-obligation](a-dependency-is-met-when-you-can-write-the-obligation.md) | "Merged" ≠ "unblocked": a bridge gives you the SPINE, not the ELEMENTS — try to WRITE the obligation before you kick the dependent |
| [a-differential-over-an-aggregate-is-an-existential-not-a-universal](a-differential-over-an-aggregate-is-an-existential-not-a-universal.md) | "Perturb the authority, observe the artifact change" proves SOMEONE consumed it, not that EVERY consumer did — 1 of 5 call sites reverted to a literal, `439 passed; 0 failed`. Coverage is PER-SITE, or must be NAMED probe-unreachable |
| [a-frozen-api-is-frozen-at-its-type-not-its-behavior](a-frozen-api-is-frozen-at-its-type-not-its-behavior.md) | "All the old operations still work" is NOT signature compatibility — a spec-pinned API is frozen at its TYPE, and a green suite is blind because the same diff migrates every in-repo caller. Grow a frozen surface ADDITIVELY under a new name |
| [a-fix-can-reproduce-its-own-bug-one-layer-up](a-fix-can-reproduce-its-own-bug-one-layer-up.md) | A fix can reproduce its own bug ONE LAYER UP, invisibly to its own tests — the corrected code passes while the same defect reappears in the artifact describing or gating it. Read the fix and its companion prose as two independently-failing surfaces |
| [a-fix-that-closes-the-named-counterexample-need-not-close-the-class](a-fix-that-closes-the-named-counterexample-need-not-close-the-class.md) | A repair read off a named counterexample excludes that POINT, not the CLASS — the prescribed `range(transitioned) ⊆ dom(minted)` was satisfied by two recognitions sharing one transport, so the same fail-open state returned green by another route. Derive the law from the PROPERTY and check the named state falls out; and put the prescribed fix itself in the mutation grid, or you tested only the fix you wrote |
| [a-full-disk-presents-as-a-test-regression-and-df-slash-cannot-see-it](a-full-disk-presents-as-a-test-regression-and-df-slash-cannot-see-it.md) | A full `/workspaces/ken` surfaces as a wave of test failures reading as a regression from your own change — and `df /` measures a DIFFERENT device (`nvme0n1p7` overlay vs the repo's `nvme0n1p6`), reporting steady free space throughout, so the instinctive check confirms the wrong conclusion. `ld: No space left on device` and linker `SIGBUS` are the tells. Always `df -h /workspaces/ken`; spend logs and `tmp/ken-*` before any `target/`; your own `target/` is yours, another seat's is the Steward's |
| [a-green-pilot-is-not-evidence-for-a-shape-it-never-produced](a-green-pilot-is-not-evidence-for-a-shape-it-never-produced.md) | A pilot's value is its COVERAGE, not its greenness — draw the matrix of shapes the ROLLOUT will emit and mark which cells the PILOT produced. Draw axes on what the MECHANISM forks on, not what the SYNTAX makes salient; a perfect small-sample split is correlation, not cause |
| [a-later-note-saying-a-deliverable-is-false-does-not-replace-the-deliverable](a-later-note-saying-a-deliverable-is-false-does-not-replace-the-deliverable.md) | Appending a clarification that a deliverable is false doesn't replace it — both readings live in the doc and the superseded one is what an implementer reads first. Even a correction banner at the very TOP loses to the operative AC line; delete the stale claim from the requirement itself |
| [a-list-of-same-shape-guards-reads-as-complete-because-the-shape-is-what-made-them-findable](a-list-of-same-shape-guards-reads-as-complete-because-the-shape-is-what-made-them-findable.md) | Censusing "what actually guards this?" finds guards sharing ONE shape, because the shape is what your search keyed on — three `find().unwrap_or_else(panic)` MECHANISM guards read as complete while the only CONSEQUENCE guard (a `contains` on formatter output, the one asserting the user-visible harm) stayed invisible. Two published corrections in a row were wrong the same way. Ask "what breaks for a USER?" as a separate query; say which guards are assertions vs setup lookups; name the sweep's bound |
| [a-mechanism-claim-in-a-comment-is-structurally-exempt-from-execution](a-mechanism-claim-in-a-comment-is-structurally-exempt-from-execution.md) | The same wrong belief about your code self-corrects in a minute in an assertion and survives review in a comment — an untestable doc comment claimed BREADTH-first traversal where `Vec::pop` makes it LIFO depth-first. Name the operation that makes a mechanism claim true |
| [a-negative-check-passes-for-any-reason-so-it-needs-a-positive-control](a-negative-check-passes-for-any-reason-so-it-needs-a-positive-control.md) | `compile_fail`/`expect_err`/`should_panic` pass when the target fails for ANY reason — a generic-type inference error masqueraded as a visibility check, forever. Seven compounding instances in one week: pair every negative with a positive control, check freshness, never ship a rejection-only control set |
| [a-mutation-that-passes-when-it-should-fail-means-a-stale-input](a-mutation-that-passes-when-it-should-fail-means-a-stale-input.md) | When a mutation proof passes where it should fail, doubt the INPUT first — a harness selected a stale rlib (of 15 built that day) by filename hash, and its positive control passed happily on hours-old code. Freshness is a THIRD axis a control can't prove; never select a built artifact by name order |
| [a-line-local-operation-lies-about-hard-wrapped-text](a-line-local-operation-lies-about-hard-wrapped-text.md) | A sentence is not a line in an 80-column corpus — a verification grep came back empty on byte-identical content (2x one PR), and a line-scoped fix produced "a an" because the article wrapped to the prior line. Verify against normalized text + a word-diff; never shorten a probe to make it pass |
| [a-p-scoped-run-and-cis-workspace-run-compile-different-feature-sets](a-p-scoped-run-and-cis-workspace-run-compile-different-feature-sets.md) | A `-p <pkg>` run activates that package's DEFAULT features; CI's `--workspace` run activates the UNION every member demands, dev-dependencies included — so the mandatory targeted local run can compile a different binary than CI from the same source. Measured: two rows "passed locally, failed in CI" because a sibling's `[dev-dependencies]` enabled a non-default feature. `#[cfg(any(test, feature = "x"))]` does not discriminate; only `#[cfg(feature = "x")]` does. Carry the matching `--features` on BOTH ends |
| [a-pin-cannot-disagree-with-its-own-source](a-pin-cannot-disagree-with-its-own-source.md) | Four green pins in one WP each compared a value against another expression of the SAME value — ask what would have to be WRONG for it to redden, and whether that thing is the AUTHORITY or a restatement of it. Exhaustiveness ≠ independence; only independence makes a pin evidence |
| [a-probe-truncated-before-the-grep-is-not-a-measurement](a-probe-truncated-before-the-grep-is-not-a-measurement.md) | `| tail -N` upstream of a grep converts "absent from the last N lines" into "absent" — a gate reported `warnings: 0` from a buffer with no compiler output at all (real count: 35). The PIPELINE lies, not the grep. Search the full stream, truncate the RESULT |
| [a-scope-exclusion-bounds-edits-not-verification](a-scope-exclusion-bounds-edits-not-verification.md) | A frame's do-not-touch list is about EDIT AUTHORITY, not whether your inputs are still true at your base — "not mine to change" was read as "not mine to re-check", carrying a constant 4× low. Label every carried premise MEASURED HERE or INHERITED |
| [a-squash-merged-branch-cannot-be-continued](a-squash-merged-branch-cannot-be-continued.md) | A squash-merged branch dangles AHEAD of `main` while its content is already in — "continue" it and you hand off a stale ref; stay parked on it and you HARD-BLOCK the receiving team's checkout |
| [a-review-range-of-sha-caret-to-sha-reads-only-the-last-commit-and-the-scope-check-agrees](a-review-range-of-sha-caret-to-sha-reads-only-the-last-commit-and-the-scope-check-agrees.md) | Reading a multi-commit candidate as `<SHA>^ <SHA>` reads only its LAST commit, and the prescribed cross-check — path count against declared scope — SILENTLY PASSES when the commits touch the same files, so the scope sentence is true of both ranges while a third of the diff goes unread. Measured on both sides of the gate: verifying (DS-9 `D1`, one of two paths) and reviewing (an Architect verdict of `+115/-5` on a true `+187/-19`). Enumerate from the declared merge-base, cross-check the DIFF SIZE not just the path count, and state the range in the verdict |
| [a-risk-register-is-a-grep-list-not-a-forecast](a-risk-register-is-a-grep-list-not-a-forecast.md) | Every entry in a "Risks" section is a QUESTION — `is this already true?` — not a forecast; and audit any gate (`cfg`, flag, predicate) WIDER than the fact it guards |
| [a-precise-fact-can-live-in-an-artifact-its-reader-never-opens](a-precise-fact-can-live-in-an-artifact-its-reader-never-opens.md) | Three times in one arc the fact was correct and the operative artifact was silent: prose block vs AC, assertion message vs doc header, COMMIT MESSAGE vs source. A commit message is immutable and unreachable from the code, so "someone will sweep it when they next edit that comment" cannot fire -- there is no comment. Name the reader, then ask which artifact that reader has open |
| [a-requirement-in-an-advisory-section-is-never-discharged](a-requirement-in-an-advisory-section-is-never-discharged.md) | A real requirement placed under "hazards/notes" gets READ, not DONE — a frame said "evasion-test each pin," only one of several ran, and two of three later review folds were exactly that family. Only an AC with a named positive control gets discharged; right content, wrong carrier |
| [a-red-base-gate-is-not-your-bug-hold-your-green-candidate](a-red-base-gate-is-not-your-bug-hold-your-green-candidate.md) | A red gate on files you do not own is not your bug — hold the green candidate, report both verdicts separately, route the red; never buy green by editing an upstream owner's files |
| [a-stand-down-clause-lives-in-prose-where-no-gate-can-reach-it](a-stand-down-clause-lives-in-prose-where-no-gate-can-reach-it.md) | An instruction telling someone NOT to look ("if CI shows X, it isn't yours") lives in a message, not an artifact — if it works, no evidence of the suppression is ever generated. Have someone who must OBEY it read it before it ships, and retract to the whole tree |
| [a-tools-silence-is-scoped-to-the-question-it-asks](a-tools-silence-is-scoped-to-the-question-it-asks.md) | An `unused_imports` warning answers "does anything name this today", never "what is the declared surface" — promoting a correct answer to a narrower question into authority on a broader one is a distinct defect class from a buggy instrument |
| [a-two-clause-finding-gets-discharged-on-whichever-clause-its-author-phrased-as-an-ask](a-two-clause-finding-gets-discharged-on-whichever-clause-its-author-phrased-as-an-ask.md) | A finding with an EVIDENCE heading and a REMEDY body gets a deliverable derived from the remedy sentence only -- it is quotable, while the evidence half describes a state of the world and has no ask. The node then closes COMPLETE on half the finding. Enumerate a finding's clauses before writing deliverables, write one per clause or an explicit declination, and check whether discharging one PRICES UP the other |
| [a-vacuous-law-has-zero-trust-delta](a-vacuous-law-has-zero-trust-delta.md) | A gate keyed on trust movement is BLIND to vacuity — a hollow conditional law adds no Axiom and has zero delta. Ship the reaching lemma, and when arguing a gate can be skipped, name the RISK it exists to catch |
| [the-evidence-framing-you-route-a-fork-with-can-be-too-weak-to-decide-it](the-evidence-framing-you-route-a-fork-with-can-be-too-weak-to-decide-it.md) | I routed a clean binary grounded on "there are two constructor occurrences", and the ruling opened by correcting the description instead of choosing — a COUNT is weaker than a POSITION, and my evidence was symmetric between both answers, so neither could be ruled out. Before routing a fork, ask which of your two answers your evidence would rule OUT; if neither, you are routing a measurement, so say that. Worse half: I published the relayed pair into the frame as a FIXED INPUT before anyone re-derived it, and one row's origins were reversed on `main` for an hour. A relayed measurement becomes a fixed input the moment you publish it — re-derive it, or write "as measured by X at `<sha>`, not re-derived". Correct by REPLACING the operative table, never appending beside it. SECOND INSTANCE upgrades this from hygiene to CORRECTNESS: a relayed population ("four call sites", really six) went into a frame as a deciding read's specification, and the four named sites were DISJOINT from where the answer lived — zero repeats there, all of them in the omitted route. An instrument built to spec would have measured cleanly, passed its mutation control, and returned the WRONG BRANCH. A relayed count in a specification does not just go unverified, it SELECTS WHAT GETS LOOKED AT. Ask: if this enumeration is wrong, does the method still find the answer? For a search spec that is always no |
| [agreement-is-not-corroboration-when-a-premise-was-inherited](agreement-is-not-corroboration-when-a-premise-was-inherited.md) | Two seats agreeing is not corroboration when one inherited the other's premise — six wrong measurements of one file across three seats, each arriving as "support" for the last correction. Ask which operand they re-used from you; state what you re-derived vs. took on trust |
| [an-assertion-whose-expected-value-is-computed-from-the-thing-under-test-is-a-theorem](an-assertion-whose-expected-value-is-computed-from-the-thing-under-test-is-a-theorem.md) | `assert_eq!(f(x).len(), g(x).len())` where `f` and `g` share the predicate under test is INVARIANT under every mutation of that predicate — it states a theorem and reads as coverage. Read every assertion's right-hand side and ask where the number came from; prefer a LITERAL. When retiring one, do not name the nearest fixture as the new guard without running the mutation against it |
| [an-enumeration-needs-a-proven-closure-not-a-better-grep](an-enumeration-needs-a-proven-closure-not-a-better-grep.md) | An enumeration needs a PROVEN CLOSURE, not a better grep — enumerate the PRODUCERS of a kind, not instances of it (find the gate, prove nothing bypasses it). Four instances in one day, the fourth while knowing and having just written the fix |
| [adding-a-file-to-a-globbed-corpus-trips-oracles-you-did-not-enumerate](adding-a-file-to-a-globbed-corpus-trips-oracles-you-did-not-enumerate.md) | Adding a file to a globbed corpus (`catalog/`, `examples/`, `conformance/`) trips oracles in crates you never touched — enumerate them at frame time, or meet them as red CI at publish |
| [amending-a-frame-mid-flight-must-sweep-its-guardrails-section](amending-a-frame-mid-flight-must-sweep-its-guardrails-section.md) | When the substrate moves under a held WP, sweep the WHOLE frame — the guardrails section is obeyed without re-derivation, so a stale one FORBIDS the correct build in your own voice |
| [an-acceptance-property-listing-forbidden-verbs-permits-losing-the-thing](an-acceptance-property-listing-forbidden-verbs-permits-losing-the-thing.md) | A preflight forbidding carry/allocate/store/join/project/return/publish lists every way a thing can be USED and none of the ways it can be LOST — four of five rows went green by DROPPING the field, satisfying all seven vacuously. The list is the wrong instrument, not too short. Take the total over the DISPOSITION SPACE (consumed / erased-under-proof / refused) with "none is dropped" as the closure — my first replacement named only `consumed` and was still too narrow. And an absence is not an authority: "zero observed" measured the route's reach, not the world |
| [an-oracle-that-greps-a-name-fires-on-prose-that-denies-it](an-oracle-that-greps-a-name-fires-on-prose-that-denies-it.md) | A "zero-X" oracle that greps the NAME fires on the prose that DENIES it — check declarations, not substrings |
| [an-ordered-validators-first-stage-subsumes-most-of-its-later-laws](an-ordered-validators-first-stage-subsumes-most-of-its-later-laws.md) | An ordered validator's first stage subsumes most of its later laws — 12 named laws, 5 live detectors; probe each arm for a witness, never count them |
| [catalog-sources-are-literate-ken-md-not-ken](catalog-sources-are-literate-ken-md-not-ken.md) | `catalog/**/*.ken` is (almost) vacuous — catalog sources are literate `.ken.md`; a "should be zero" grep against the wrong glob returns zero for the wrong reason |
| [check-main-via-git-object-store-not-find](check-main-via-git-object-store-not-find.md) | Check main via the git object store, not `find` |
| [citation-closure-a-consistent-corpus-can-be-collectively-wrong](citation-closure-a-consistent-corpus-can-be-collectively-wrong.md) | A corpus can be internally consistent and collectively wrong — citations closing on each other is not grounding |
| [cleanroom-is-role-discipline-not-host](cleanroom-is-role-discipline-not-host.md) | Clean-room protection is a role discipline, not a model-host property |
| [codex-seats-do-wake-real-gap-is-reply-to-eventid](codex-seats-do-wake-real-gap-is-reply-to-eventid.md) | Codex/gpt build seats DO auto-wake on convo mentions via the channel_runner tmux daemon — the real dropped-comms bug is `reply_to` rejecting the event_id (platform-side). Check the daemon log before asserting a seat "can't wake" |
| [compact-verify-survey-can-eat-the-compact-command](compact-verify-survey-can-eat-the-compact-command.md) | A Claude Code survey prompt can eat a `/compact` command |
| [compact-wiped-memory-reflog-first](compact-wiped-memory-reflog-first.md) | After a `/compact`, check git reflog before concluding you're stalled |
| [compaction-is-manual-no-clean-seam](compaction-is-manual-no-clean-seam.md) | Team compaction follows the playbook as-is; the manual-seam problem is singleton-specific |
| [compaction-render-delay-escape-aborts](compaction-render-delay-escape-aborts.md) | `/compact` has a render delay; Escape aborts it, don't send it |
| [convo-space-not-active-is-really-the-10k-event-cap](convo-space-not-active-is-really-the-10k-event-cap.md) | A convo "space not active" error is really the 10k event cap |
| [corpus-property-gate-only-as-strong-as-the-corpus](corpus-property-gate-only-as-strong-as-the-corpus.md) | A property gate run OVER A CORPUS is only as strong as the corpus — a formatter passed whole-catalog parse-preservation + locked CI + QA and still shipped two meaning-changing bugs the catalog never exercised. Token-content-changing axes need adversarial fixtures, not just corpus coverage |
| [correcting-scope-must-sweep-whole-doc](correcting-scope-must-sweep-whole-doc.md) | Correcting a false claim in a doc must sweep the whole document |
| [definitional-equality-does-not-imply-executability-under-cbv](definitional-equality-does-not-imply-executability-under-cbv.md) | A lawful class dict carries proof fields; CBV forces them and the interpreter returns `Unknown` — route the executable path through the runtime field |
| [deleting-a-check-has-a-text-surface-and-it-outlives-the-check](deleting-a-check-has-a-text-surface-and-it-outlives-the-check.md) | Remove the claim in the same edit as the check — a success message kept printing "the checker is green" for weeks after the checker itself was deleted. Enumerate what the checker CHECKED, not the bug you were fixing; split RETIRED from DEFERRED |
| [deriving-from-the-contract-cannot-detect-a-defective-contract](deriving-from-the-contract-cannot-detect-a-defective-contract.md) | A faithful derivation transmits a defective spec with full authority and freezes it in an oracle — a human-visible deliverable needs one AC only a READER, not a mechanical test, can discharge |
| [deliverability-is-part-of-mechanism-selection](deliverability-is-part-of-mechanism-selection.md) | A design can be fully correct and non-executable — the publisher credential can't push `.github/workflows/**`, so a branch dies at push, before a PR exists, and no local signal sees it. "Can the publisher push this?" is a DIFFERENT axis from "which reviewer lane?"; ask at mechanism selection |
| [formatter-soundness-gates-are-blind-to-layout-conformance](formatter-soundness-gates-are-blind-to-layout-conformance.md) | A formatter's meaning-preservation gates can't see bad layout; gate what it emits against the layout spec |
| [frame-pinned-preservation-oracle-is-a-discharged-one-shot-proof](frame-pinned-preservation-oracle-is-a-discharged-one-shot-proof.md) | A preservation oracle pinned to a frame-time golden proves operation X preserved meaning ONCE — keeping it live as a permanent gate forbids all future legitimate change. Retire it at the first authorized evolution; don't re-baseline |
| [frame-pseudocode-diverges-from-landed-mechanism](frame-pseudocode-diverges-from-landed-mechanism.md) | Even Architect-authored "normative" frame pseudocode can differ from what the team actually implemented — twice in a row. Ground mechanism prose on the landed function, cite it by name, never the frame |
| [freezing-a-canonical-form-name-the-ambiguity-that-would-have-stopped-you](freezing-a-canonical-form-name-the-ambiguity-that-would-have-stopped-you.md) | Derive a canonical form from the contract, and NAME the ambiguity that would have made you escalate instead — a laundered preference and a derived rule look identical in the diff |
| [git-author-not-per-agent](git-author-not-per-agent.md) | Git author is shared, not per-agent |
| [git-checkout-ref-dot-silently-reverts-uncommitted-edits-worktree-wide](git-checkout-ref-dot-silently-reverts-uncommitted-edits-worktree-wide.md) | `git checkout <ref> -- .` inside a "just reading" command silently reverts every uncommitted tracked edit — three independent incidents, one from a playbook idiom applied to an uncommitted file. Never mutate the worktree to read a ref; commit before you route |
| [grep-rust-prelude-emission-for-landedness](grep-rust-prelude-emission-for-landedness.md) | A landedness grep must also check the Rust-emitted prelude, not just `.ken` sources |
| [grep-the-producer-not-the-cited-proxy](grep-the-producer-not-the-cited-proxy.md) | Verify against the real producer, not a cited proxy |
| [handoff-scope-count-must-match-full-thread](handoff-scope-count-must-match-full-thread.md) | Verify a handoff's scope count against the full thread |
| [hard-stop-chain-escalate-to-research-advisory](hard-stop-chain-escalate-to-research-advisory.md) | On the 3rd hard-stop of a design chain (then every 3rd), hold the Architect fully and call in research for an in-thread prior-art advisory |
| [held-branch-scaffolding-is-load-bearing-evidence](held-branch-scaffolding-is-load-bearing-evidence.md) | A held branch's scaffolding is load-bearing evidence |
| [live-review-candidate-goes-stale-reanchor-sha](live-review-candidate-goes-stale-reanchor-sha.md) | A live review candidate can go stale; re-anchor the SHA |
| [making-a-concrete-path-generic-enumerate-every-op-against-the-trait](making-a-concrete-path-generic-enumerate-every-op-against-the-trait.md) | Making a concrete path generic: "is the trait public?" is the wrong question — enumerate every op the path calls against what the trait declares; the difference is the blocker |
| [markdown-80col-reflow](markdown-80col-reflow.md) | 80-column markdown reflow: the recurring gotchas |
| [mechanism-citation-needs-own-empirical-probe](mechanism-citation-needs-own-empirical-probe.md) | A plausible mechanism citation ("same wall as X") needs its OWN empirical probe — a catalog entry cited a familiar-shaped gap and was wrong about the actual error variant, undetected through two independent reviews |
| [mention-discipline](mention-discipline.md) | Mention IFF question or next-action; grep the participant-id; the `mentions` array is reliable |
| [model-swap-does-not-reset-the-seat](model-swap-does-not-reset-the-seat.md) | A model swap keeps the seat's context; rouse to continue, don't tell it to re-orient |
| [mootup-posting-from-agent](mootup-posting-from-agent.md) | How a build-tier agent posts to mootup |
| [multi-worktree-cwd-drift-phantom-diff](multi-worktree-cwd-drift-phantom-diff.md) | Multi-worktree cwd drift produces a phantom diff |
| [mutation-proof-injection-point-is-a-reachability-tell](mutation-proof-injection-point-is-a-reachability-tell.md) | If a mutation must be injected past the real guards to reach your check, the check is unreachable in production — QA's natural-site mutation never arrived because an upstream cardinality gate panics first. Mutate at the production site, never a convenient injection point |
| [narrowing-a-counts-scope-never-turns-a-tally-into-a-pairing](narrowing-a-counts-scope-never-turns-a-tally-into-a-pairing.md) | One error class caught FOUR times in one increment — forbidden verbs, a one-disposition total, a compile-wide scalar, then a per-origin scalar. Each catch correctly narrowed the count's SCOPE and kept the COUNT. A tally answers "how many"; a pairing asks "which one", and an entry holding two `usize`s cannot separate `2 = 1+1` from `2 = 2+0`. The repair is an IDENTITY per obligation, never a smaller counter. Tell: the comment says "each" three lines above a `==`. A short-count control row is the near miss that hides the unsound equal-count case. Fail-open with no live witness is SCHEDULED, not low-severity — fix it in the increment that arms it |
| [never-pin-a-shape-that-cannot-state-its-own-contract](never-pin-a-shape-that-cannot-state-its-own-contract.md) | Never pin a shape that cannot state its own contract — the expressibility audit (PRINCIPLES #14, widened) |
| [no-error-in-the-output-passes-when-there-is-no-output](no-error-in-the-output-passes-when-there-is-no-output.md) | A build that never RAN emits no failure token, so grepping for error/failed/test-result reports CLEAN — a relative-path `ken-cargo` call exited **127** with none of those words present. Assert the POSITIVE token + predicted count + `$?`, never the absence of a negative one |
| [no-new-red-is-measured-against-the-last-checkpoint-not-the-merge-base](no-new-red-is-measured-against-the-last-checkpoint-not-the-merge-base.md) | A regression introduced at a WP's FIRST checkpoint becomes "inherited debt" by its second and is never re-measured — every later "no new red" is true while the object diverges from its base. Six "preserved" reds measured at the gate: base 83/0, candidate 84/7. Zero inherited, seven caused, one never named. Demand the ABSOLUTE reading at candidate AND merge-base |
| [opaque-primitive-constructible-not-destructible-format-gap](opaque-primitive-constructible-not-destructible-format-gap.md) | Ken's opaque `Int`/`String`/`Bytes` are constructible but not destructible — parse (`String → Int`) is buildable, format (`Int → String`) hits a substrate gap with no destructor to case-split on. Ship the structural formatter, defer the opaque hop as a named gap |
| [orphaned-background-task-loops-leak-cpu](orphaned-background-task-loops-leak-cpu.md) | Hand-rolled background bash loops can orphan and leak CPU |
| [pane-suggestion-text-is-not-agent-state](pane-suggestion-text-is-not-agent-state.md) | The tmux pane's gray suggestion text is not agent state |
| [plumbing-commit-onto-held-branch-and-its-desync-risk](plumbing-commit-onto-held-branch-and-its-desync-risk.md) | A trivial fix can be plumbed onto a branch another worktree holds via hash-object/commit-tree/update-ref with no checkout — but the holder's disk desyncs from `HEAD` silently; they must hash-verify before re-trusting `git log` |
| [primitive-ops-do-not-reduce-under-conversion](primitive-ops-do-not-reduce-under-conversion.md) | Primitive ops compute at RUNTIME and are OPAQUE to conversion — Refl cannot discharge a prim-op equation |
| [publish-a-coordinate-from-the-git-object-and-name-the-sha-you-read](publish-a-coordinate-from-the-git-object-and-name-the-sha-you-read.md) | A `file:line` handed to another seat is consumed by someone on a different tree — read it with `git show <sha>:<path>` and state that same SHA beside it; two seats cited one file to the Architect within five minutes, from two trees, and both resolved to real unrelated code |
| [publisher-app-cannot-push-workflow-file-changes](publisher-app-cannot-push-workflow-file-changes.md) | SUPERSEDED 2026-07-22 (permission since granted) — kept for the meta-lesson: a recorded capability constraint is evidence about the PAST; for mutable external state, test at point of use, never cite a stale note |
| [re-read-latest-events-immediately-before-a-stall-nudge](re-read-latest-events-immediately-before-a-stall-nudge.md) | Re-read latest events immediately before a stall-nudge |
| [reason-in-agent-team-hours-not-human-days](reason-in-agent-team-hours-not-human-days.md) | Reason in agent-team wall-clock hours, never human work-days |
| [reviewers-review-branches-not-prs](reviewers-review-branches-not-prs.md) | Federation reviewers review branches, not PRs |
| [safety-check-modal-defaults-to-a-model-downgrade](safety-check-modal-defaults-to-a-model-downgrade.md) | The "additional safety checks" modal needs NO answer — the seat keeps working. Option 1 "retry with a faster model" is PRE-SELECTED, so the stranded-seat repair (bare `Enter`) silently downgrades a seat the operator tiered by name. Before any pane-level keypress, capture the tail: a numbered option list means leave it alone. AND THE DETECTIVE HALF — once the modal clears it leaves no trace but the model footer, so every pane read captures the footer too: the modal says the seat is ALIVE, the footer says at what TIER, and a liveness check reports all-clear over a downgrade every time. One seat on luna while its peers are sol/terra is the signal; act at a turn boundary, holding the handback rather than the code |
| [scripted-publisher-target-is-head-branch-never-main](scripted-publisher-target-is-head-branch-never-main.md) | `scripted-pr-automerge.sh --target` is the HEAD branch to PUSH, never `main` — base `main` is hardcoded. Passing `--target main` once force-pushed a stale local `main` over `origin/main`, dropping several landed PRs |
| [self-contained-handoff-paste-verbatim-no-event-id](self-contained-handoff-paste-verbatim-no-event-id.md) | Hand a ruling/artifact verbatim in-thread; never make a seat fetch it by event-ID |
| [surface-the-seam-need-not-your-preferred-mechanism](surface-the-seam-need-not-your-preferred-mechanism.md) | Surface a cross-author need; leave the mechanism to the owner |
| [terminal-gate-resolve-race-resolving-on-cast](terminal-gate-resolve-race-resolving-on-cast.md) | A terminal-gate resolve can race the last voter's own resolve |
| [the-consumer-edge-ledger-spine-element-obligation-only](the-consumer-edge-ledger-spine-element-obligation-only.md) | Before migrating off a representation, classify EVERY use-site spine / element / obligation-only — a closure decision procedure you can run before writing code |
| [the-law-you-re-orient-against-is-your-branch-s-base-not-main](the-law-you-re-orient-against-is-your-branch-s-base-not-main.md) | Re-orienting from your own worktree reads COORDINATION as of your BRANCH'S BASE — a directive published since your last rebase is invisible; cross-read `origin/main` |
| [the-workaround-fossil-tells-you-what-the-language-could-not-say](the-workaround-fossil-tells-you-what-the-language-could-not-say.md) | Contorted code is EVIDENCE of a missing capability — and when the capability lands, go find the fossils it obsoletes |
| [thin-flow-directive-dont-cc-the-room](thin-flow-directive-dont-cc-the-room.md) | Thin-flow directive: one reviewer per lane, don't cc the room |
| [use-tier-labels-never-model-names](use-tier-labels-never-model-names.md) | Use tier labels, never model names |
| [verify-a-tmux-rouse-actually-submitted](verify-a-tmux-rouse-actually-submitted.md) | A tmux rouse can land in the input buffer and never submit — verify the pane flips to Working |
| [verify-field-order-arity-against-declaration-not-prose](verify-field-order-arity-against-declaration-not-prose.md) | Verify field order/arity against the declaration, not prose |
| [verify-the-report-is-real-before-explaining-it](verify-the-report-is-real-before-explaining-it.md) | A confident explanation of a MEASUREMENT ERROR launders it into a finding — check the report reproduces before you build a mechanism for it |
| [when-closure-cannot-be-proven-install-a-known-answer-oracle](when-closure-cannot-be-proven-install-a-known-answer-oracle.md) | For OPEN-WORLD sets where closure can't be proven: five false-IDLE watchdog misses were each "fixed" by adding an arm — unfalsifiable maintenance. Fix: assert against an input whose answer you know independently, and refuse to report when it's falsified |
| [withdraw-and-relocate-test-different-properties](withdraw-and-relocate-test-different-properties.md) | DROP and REMAP test different properties, and a ruling's "X and Y" is not "X or Y" — a consumer intersecting the authority with its own table honours every removal but ignores every relocation. Only removing a whole ROW reaches a fail-closed default |
| [a-git-query-answers-a-different-question-correctly-and-never-errors](a-git-query-answers-a-different-question-correctly-and-never-errors.md) | Two git queries that answer a DIFFERENT question correctly and never error. A bare tip SHA read with `git show` shows ONE COMMIT (2 files) not the change (10 files), so a reviewer finds none of what the notification described and gets no error — name the landed squash or `merge-base...tip`. A ref-less `git log -S` measures YOUR OWN branch, never `main`. Implausibility of the answer was the only detector in both cases |
| [exhaustiveness-comes-from-an-unguarded-arm-not-from-the-match](exhaustiveness-comes-from-an-unguarded-arm-not-from-the-match.md) | "Delete the variant and the compiler catches every site" is FALSE for guard-heavy matches — Rust IGNORES GUARDS for exhaustiveness, so a guarded arm never breaks. Measured: five guarded arms carried no guarantee at all and the whole property rested on ONE unguarded arm naming the variant; guard it or write `_ =>` and the retirement goes SILENT. Name the mechanism that would break, then check it exists at every site |
