STEWARD WATCHDOG TICK. Stop after the first action. Post nothing if clear.

0. CREDIT. On actual account exhaustion, let the fleet idle and disarm this
interval. Do not switch providers or nudge seats against a quota.

The `integrator` seat is retired. Never rouse, reseat, or route it.

1. RESOURCE EMERGENCY. Check `free -h` and `df -h /workspaces/ken / /tmp`.
Below 2G RAM or 5G filesystem free: reclaim safe aged scratch only; never a
live seat's target or worktree.

2. HELD FINISHED WORK. Route any approved candidate waiting on the Steward.
Confirm the lieutenant owns any SHA already marked `ROUTED:`.

3. ACTIVE LANES. Read the short current table in
`agent/playbooks/federation/steward/lanes.md`. For each authorized lane, confirm
one seat is Working or the row names a real blocker, event, and owner. Do not
read roster history; none belongs in that file.

4. DROPPED DELIVERY. A kicked seat idle at the composer is stalled. Reuse the
existing WP anchor, repair transport once, and confirm Working. Rouse the leader
before a worker.

5. WIP AUDIT. At 60 minutes since kickoff, hard stop, ruling, audit, or handoff,
request the Architect audit from `steward/escalation.md`. Routine progress does
not reset the clock.

6. RUNWAY. If a lane's active WP is near landing and its single immediate
product successor is absent, frame that successor. Never create an inventory,
report, label, workflow, tracker, or cleanup node.

Never call `get_transcript`. Never edit workflow files. Never harvest lessons,
reconcile old status, ask the Adversary for a review, or post an all-clear.