STEWARD WATCHDOG TICK. Run in order; stop as soon as one produces an action.

ARMING SOURCE. KEEP UNDER 4096 CHARS -- set_interval rejects more. `wc -c` before arming; never grow a step without trimming another.

0. CONTEXT GATE FIRST. `tmux capture-pane -t moot-steward -p | grep -oE 'ctx [0-9]+%' | tail -1`. At or near 33%, commit durable state, update `local/steward-resume-checkpoint.md`, then `moot compact steward` from /workspaces/ken. Announcing is not doing.

0b. DISK (operator, 2026-08-18). `df -h /workspaces/ken /` -- BOTH, they are different filesystems. Either below 5G avail: reclaim before any other work, in this order: `.moot/truncate-logs.sh`; aged `tmp/ken-*`; `git gc` if `git count-objects -vH` shows garbage; a RETIRED seat's `target/`. Verify a seat dead 3 ways -- no tmux session, no `ps` match, no live `/proc/*/cwd` -- and NEVER touch a live ring's target. Axis is imminent work, never size.

1. RUNTIME IS THE ONLY LANE (operator, 2026-08-17). Residuals in order: carrier recut, the ready RT-* nodes, RT-DESCENT-RETIRE D6a. RT-CHECKED-IH-REALIZATION-AUTHORITY is ready + dependency-free and STAYS HELD -- it looks startable every tick. Every other ring stays idle; that is the cost of the priority, not a defect to fix. Finished work still merges; filings queue.

2. ARE THE LANE'S CRATES MOVING? ANCHOR THE PATHSPEC: `git log --since='12 hours ago' --oneline origin/main -- ':/crates/' | wc -l`. A bare `-- crates/` is cwd-relative and the Bash cwd PERSISTS, so after any `cd` it silently reports 0 without erroring. Measured: 0 twice while the truth was 10.

2b. STEP 2 IS AN AGGREGATE -- A MOVING LANE MASKS A DEAD ONE. Check each `status: active` node separately: is its ring working its next deliverable? Read the NODE, never a seat's status line -- it decays silently, in the direction that HIDES unblocked work. Measured: three rings idle on ALREADY-LANDED work, step 2 green throughout. Confirm a landing by BLOB IDENTITY (`git rev-parse origin/main:<path>` vs `<SHA>:<path>`), never ancestry -- the publisher squashes.

2c. A DISPATCHED NODE LEFT AT `ready` IS INVISIBLE TO 2b. On release flip it `active`. A ring being awake is not the record.

3. HELD FINISHED WORK IS TOP OF QUEUE (COORDINATION 10-). Any candidate handed back and unpublished? Publish it: M1-M9 from `agent/playbooks/federation/steward/merge-procedure.md`. Read the file. Check none runs first: `ps -eo pid,ppid,etime,args >f; grep scripted-pr-automerge.sh f`; read the LISTING, skip `bash -c` lines. pgrep + `grep -v claude` HID a live publisher and I started a second. Never fetch or commit while one holds its window; never pipe it through tail/grep -- it block-buffers, poll `gh pr view <N>`. M1 needs a RESOLVED Decision read fresh over HTTP; the API IGNORES `limit`, so save to a file and filter. M8a: COMPACT ADVERSARY before the code-merge notify -- nothing else in the fleet does -- then rouse its pane.

4. WIP AUDIT, 60 MIN -- VERIFY DELIVERY POSITIVELY BEFORE FIRING. Resets: Architect ruling, QA verdict, handoff, leader routing, my own kick. Read the thread first. A reset you never looked for cannot reset a clock.

5. ONE RELEASE AHEAD. Grep other nodes' `depends_on` for the in-flight id (not `blocks` -- they disagree; gen-progress.sh reads depends_on). Successors must be `ready` and shovel-ready. A MEASUREMENT-ONLY node never reaches `merged` -- close it, or successors gate on a landing that cannot happen.

5b. A KICK MUST CARRY THE ANCHOR SENTENCE VERBATIM: "This message is the thread anchor for <WP-ID>. Reply with parent_event_id set to this event's id; thereafter use its thread_id." A kick CANNOT quote the thread it opens -- the first reply mints it.

6. RESOLVE ACTOR IDS BEFORE COMPOSING MENTIONS: `scripts/moot-actor-id.sh <role>`. A remembered id wakes the wrong seat or nobody, silently.

7. NEVER call get_transcript. Use get_recent_context/get_mentions with detail="standard"; poll adversary reports with since_event_id from the last tick.

If nothing above produces an action, post nothing and stop. A quiet tick is a correct tick.
