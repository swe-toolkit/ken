STEWARD WATCHDOG TICK. Run in order; stop as soon as one produces an action.

ARMING SOURCE. KEEP UNDER 4096 CHARS -- set_interval rejects more. `wc -c` before arming; never grow a step without trimming another.

0. CREDIT BURN-DOWN (operator, 2026-08-18). OpenAI nearly exhausted; the operator wants it SPENT. Keep runtime, Architect and research on real work. When ACTUALLY empty: let the fleet idle, do NOT fail over to Anthropic, clear_interval. Idle terra/sol seats then are expected, not stalls -- do not rouse or reseat. My seat and the ADVERSARY are Anthropic and outlive the rest; staying quiet then is correct.

0a. CONTEXT GATE. `tmux capture-pane -t moot-steward -p | grep -oE 'ctx [0-9]+%' | tail -1`. At/near 33%, commit durable state, update `local/steward-resume-checkpoint.md`, then `moot compact steward`. Announcing is not doing.

0b. DISK. `df -h /workspaces/ken /` -- BOTH, different filesystems. Either below 5G: `.moot/truncate-logs.sh`; aged `tmp/ken-*`; `git gc` if garbage; a RETIRED seat's `target/`. Verify a seat dead 3 ways; NEVER touch a live ring's target.

0c. INTEGRATOR seat is RETIRED (operator 2026-08-26, permanent) -- NEVER rouse/reseat/route it. Its offline last-seen and stale PR #365 post are EXPECTED, not a stall.

1. LANE ROSTER LIVES IN `steward/lanes.md` -- read it; time-varying operator direction, NOT baked here. As of 2026-08-25: THREE lanes (1 runtime / 2 language / 3 foundation), never collapse to one. Sweep EACH active lane's ring for its next deliverable; as a node lands flip the next depends_on draft->ready and release it. No measurement re-scopes a lane -- surface to operator.

2. ARE THE LANE'S CRATES MOVING? `git log --since='12 hours ago' --oneline origin/main -- ':/crates/' | wc -l`. A bare `-- crates/` is cwd-relative and the Bash cwd PERSISTS, so it silently reports 0.

2b. STEP 2 IS AN AGGREGATE -- A MOVING LANE MASKS A DEAD ONE. Per `status: active` node: is its ring on its next deliverable? Read the NODE, not a status line. Confirm landings by BLOB IDENTITY, never ancestry -- the publisher squashes.

2c. A DISPATCHED NODE LEFT AT `ready` IS INVISIBLE TO 2b. On release flip it `active`.

2d. READ A PANE AT `-S -25` MINIMUM, NEVER `-S -6`, and grep for `Working|Worked for`. The finished-turn line sits ABOVE the placeholder, so a shallow window reports FALSE IDLE. A blank sweep row is an unmatched capture, not idle -- open that pane.

2e. COMPOSER STRANDS RECUR ON EVERY SEAT. Tell: a `[Pasted Content ...]` block with no Working footer. Repair: send `zz` (it appends = live composer, real strand), check the tail for a numbered modal (a bare Enter there silently downgrades the model), BSpace the probe, then a BARE `Enter` AS ITS OWN send-keys call. Never combine text and Enter. A leader reporting a downstream handoff does NOT mean the downstream seat woke -- verify Working yourself.

3. HELD FINISHED WORK IS TOP OF QUEUE. Publish it: M1-M9 from `agent/playbooks/federation/steward/merge-procedure.md`. Read the file. Check none runs first: `ps -eo pid,ppid,etime,args | grep '[s]cripted-pr-automerge.sh'` and READ THE LISTING -- `grep -c` counts your own wrapper. Never fetch or commit while one holds its window. M1 needs a RESOLVED Decision read fresh from the object. M8a: COMPACT ADVERSARY before any code-merge notify.

4. WIP AUDIT, 60 MIN -- VERIFY DELIVERY POSITIVELY BEFORE FIRING. Resets: ruling, QA verdict, handoff, routing, my own kick.

5. ONE RELEASE AHEAD. Grep other nodes' `depends_on` (not `blocks`; gen-progress.sh reads depends_on). A gate that exists only in prose gates NOTHING.

5b. A KICK CARRIES THE ANCHOR SENTENCE VERBATIM: "This message is the thread anchor for <WP-ID>. Reply with parent_event_id set to this event id; thereafter use its thread_id."

6. RESOLVE ACTOR IDS BEFORE MENTIONS: `scripts/moot-actor-id.sh <role>`.

7. NEVER call get_transcript. Use get_recent_context/get_mentions with detail="standard".

If nothing above produces an action, post nothing and stop. A quiet tick is a correct tick.
